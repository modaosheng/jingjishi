# 《经师 AI》前端工程方案 · 技术选型建议

> 配套：`中级经济师App_PRD_v1.0.md`（v1.1）、`本地存储技术方案.md`
> 阶段：前端方案评审稿 · 2026-09-03
> **本文档为选型建议，待确认后再进入编码。**

---

## 0. 先看三个必须先讲的坏消息

在进入技术选型之前，有三条平台限制会**直接影响 PRD 的核心设计**。如果不提前处理，做出来的东西会在真机上翻车。

### ⚠️ 限制一：iOS 会删你的数据（对本项目是致命的）

| 事实 | 影响 |
|---|---|
| Safari ITP：**未安装到主屏幕的站点，7 天不活跃即清空** IndexedDB / localStorage / Cache API | 用户出去玩一周回来，学习记录全没了 |
| **添加到主屏幕后豁免**（获得独立存储分区） | 所以"添加到主屏幕"不是体验优化，**是数据安全的前提** |
| 即便已安装，长期（数周，Apple 未公开阈值）不用仍会被清理 | 备份是唯一可靠的兜底 |
| **Safari 存储上限约 50MB**（Chrome 可到数百 MB） | 上传的 PDF 原件不能全量存 |
| `navigator.storage.persist()` **在 iOS 未实现**（no-op） | 无法用代码申请持久化 |
| 删除主屏幕图标 = 删除存储分区；"清除历史记录"也会清空 | 无保护措施 |

**这对 PRD 的冲击**：PRD 的核心价值主张是"数据存本地、隐私、安全"，而 iOS PWA 把这个承诺打折了。

**对策（必须做，不是可选项）**：
1. **强制安装引导**：iOS 检测到未安装时，首页顶部常驻提示条 + 首次学习前的强引导页。话术要诚实：**"添加到主屏幕，否则 7 天不打开数据会被系统清空"**——这不是营销文案，是事实。
2. **备份从"建议"升级为"核心保障"**：PRD 里是"答满 500 题提醒备份"，在 iOS 上要更激进（首次学习完成即提醒一次）。
3. **存储配额监控**：设置页显示已用/配额，接近上限（如 40MB）时提示清理附件。
4. **附件策略调整**：上传的 PDF/图片**默认只保留结构化题目，不保留原件**（可手动开启保留，并提示占用）。

### ⚠️ 限制二：iOS 上架 App Store 有政策风险

- Apple App Review Guidelines **4.2**：*"Apps that are simply web sites bundled in a native shell will be rejected"*。
- 纯 WebView 套壳会被拒。**要上架必须至少接入一个原生能力**（如原生 SQLite、推送、Face ID、Share Extension）。
- 好消息：我们本来就打算用原生 SQLite 插件（Capacitor），这算一个合理的原生能力，但不保证过审。
- **务实建议**：iOS 先不做 App Store 上架，用 Safari「添加到主屏幕」分发（零审核、零成本、即时更新）。用 Capacitor 打包 iOS 属于"以后再说"的事项。

### ⚠️ 限制三：iOS 上很多能力拿不到

| 能力 | Android Chrome | iOS Safari |
|---|---|---|
| 安装方式 | 自动提示 + WebAPK | 仅手动"添加到主屏幕" |
| 推送通知 | ✅ | ⚠️ iOS 16.4+ 且需已安装；**欧盟区 iOS 17.4+ 不支持** |
| Background Sync | ✅ | ❌ |
| Periodic Sync | ✅ | ❌ |
| 存储持久化申请 | ✅ | ❌ |
| 文件系统访问 | ✅ | ❌ |
| 通知/相机/蓝牙 | ✅ 多数可用 | ❌ 多数不可用 |

**对 PRD 的影响**：PRD 里的"学习时段推送提醒"（onboarding S4）在 iOS 上基本不可用。需要降级为**应用内提醒**（打开 App 时提示）+ 可选的 PWA 推送（仅 Android 与 iOS 16.4+ 非欧盟区）。

---

## 1. 结论速览：推荐技术栈

| 层 | 推荐选型 | 一句话理由 |
|---|---|---|
| **框架** | **Vue 3.5 + TypeScript** | 单文件组件写页面快，Vite 冷启动秒级，内存占用低 |
| **构建** | **Vite 7** | 冷启动 <1s，HMR 极快，构建内存占用约为 Webpack 的一半 |
| **UI 组件库** | **Vant 4**（有赞） | 专为移动端设计、中文文档、组件覆盖本项目 90% 需求 |
| **路由 / 状态** | Vue Router 4 / Pinia | 官方标配，体积小 |
| **本地数据层** | **`@sqlite.org/sqlite-wasm`**（opfs-sahpool VFS）+ Web Worker | ⭐ **与 PRD 的 SQLite 表结构完全一致，SQL 可直接复用到未来原生端** |
| **数据层抽象** | **Repository + DataSource 模式** | 满足"便于后续替换为真实接口"：换实现不改业务代码 |
| **PWA** | **vite-plugin-pwa**（Workbox 7） | 零配置生成 Service Worker + Manifest |
| **图表** | **@antv/f2**（移动端图表）+ 手写 CSS Grid 热力图 | F2 专为移动优化，体积约为 ECharts 的 1/3 |
| **图标** | unplugin-icons（按需编译） | 不打包整个图标库 |
| **打包 Android** | **Bubblewrap / PWABuilder（TWA）** | 不需要 Android Studio；产出 AAB 直传 Google Play |
| **打包 iOS** | 暂不做（用 Safari 添加到主屏幕） | 需 Xcode，本机无法承载；且 App Store 4.2 有拒审风险 |

---

## 2. 技术选型详解

### 2.1 框架：Vue 3 还是 React 19？

| 维度 | Vue 3.5 | React 19 |
|---|---|---|
| 本机资源占用 | ✅ 更低（无虚拟 DOM diff 开销，编译期优化） | 稍高 |
| 构建速度 | ✅ 快 | 快 |
| 单页面代码量 | ✅ SFC 一个文件搞定模板+逻辑+样式，本项目 25+ 页面时优势明显 | 需拆 JSX + CSS Module |
| 移动组件库 | ✅ Vant 4（有赞，专为移动）、Varlet | antd-mobile |
| 上手成本 | 低（模板语法接近 HTML） | 中（JSX + Hooks 心智负担） |
| 你作为全栈的熟悉度 | ？ | ？ |

**推荐 Vue 3.5**，理由：本项目页面多（约 25-30 个）、交互密集（答题、模考），Vue SFC 的书写效率与可读性优势会累积成显著的时间节省；且在低配机器上 dev server 内存占用更低。

**但如果你更熟 React，选 React 19 + Vite + antd-mobile 也完全可行**——本方案的所有架构设计（数据层抽象、PWA、目录结构）与框架无关。**这一项由你定，我默认按 Vue 3 出方案。**

### 2.2 本地数据层（最关键的选择）⭐

这是本方案最需要你拍板的一项，因为它决定了"后续替换为真实接口"的成本。

#### 方案 A：SQLite WASM（推荐）

```
@sqlite.org/sqlite-wasm + opfs-sahpool VFS + 运行在 Web Worker
```

| 优点 | 缺点 |
|---|---|
| ⭐ **SQL 与 PRD §11 表结构完全一致**，未来换 Capacitor 原生 SQLite 插件，**SQL 语句一行不用改** | WASM 体积：约 1.2MB（gzip 后约 600KB） |
| 真正的 SQL：JOIN、索引、聚合都能用，答题记录统计/Filters 很自然 | 必须在 Web Worker 中运行（OPFS sync handle 限制），架构多一层 |
| OPFS 增量持久化，不像 sql.js 那样每次全量导出 | opfs-sahpool 是**单连接**（同时只能一个页面写），符合本场景但多标签会有问题 |
| 官方维护（sqlite.org），2026-04 仍在更新（3.53.0） | Safari 无痕模式不支持 OPFS，需降级到 IndexedDB VFS |

**关键实现要点（已踩过的坑）**：
1. ⚠️ **包名陷阱**：npm 上的 `sqlite-wasm` 是 2020 年的无关废弃包（0.0.2）。**必须装 `@sqlite.org/sqlite-wasm`**。
2. VFS 选 `opfs-sahpool`：不需要 COOP/COEP 响应头（部署友好），性能最好。
3. 必须在 **Dedicated Worker** 中初始化（`createSyncAccessHandle` 只在 Worker 暴露），主线程通过 postMessage 通信。
4. 浏览器支持：Chrome 108+ / Safari 16.4+ / Firefox 111+。
5. 降级路径：检测不到 OPFS 时，退回 `IDBBatchAtomicVFS`（IndexedDB，Safari 15.4+）。

#### 方案 B：Dexie.js（IndexedDB 封装）

| 优点 | 缺点 |
|---|---|
| 零额外体积、零 WASM | 查询是 NoSQL 风格，未来迁移到原生 SQLite 需重写查询层 |
| API 简洁，开发快 | 复杂统计（如"各模块掌握度聚合"）要手写多次遍历 |
| 兼容性最好 | 与 PRD 的 SQL 表结构对不上，mock 阶段的数据结构无法直接复用 |

#### 我的推荐：**方案 A（SQLite WASM）**

理由：你明确要求"数据层要便于后续替换"。方案 A 让**前端 mock 阶段就跑真实 SQL**，将来无论换成 Capacitor 原生 SQLite，还是换成远端 HTTP 接口，都只替换 DataSource 实现层，业务代码与 SQL 不动。这是明显的长期收益。

同时我会用 Repository 模式把风险兜住——**即使 SQLite WASM 在某个浏览器上跑不起来，换成 Dexie 也只改 `infrastructure/db/` 一个目录**。

#### 数据层架构（三态可切换）

```
业务代码（页面/Store）
      │  只依赖接口，不关心实现
      ▼
┌─────────────────────────────────┐
│  Repository 层（接口契约）        │
│  QuestionRepo / PlanRepo /       │
│  UserStateRepo / ExamRepo ...    │
└────────────┬────────────────────┘
             │ 依赖注入，运行时决定用哪个
   ┌─────────┼─────────┐
   ▼         ▼         ▼
MockSource  SqliteSource  HttpSource
(内存数组)  (SQLite WASM)  (将来对接后端)
```

- **MockSource**：纯内存数组，用于 UI 快速开发与演示，启动零成本。
- **SqliteSource**：默认，跑真实 SQL + 真实表结构（本次实现的重点）。
- **HttpSource**：预留空实现，将来对接接口时填充。

切换方式：环境变量 `VITE_DATA_SOURCE=mock|sqlite|http`，一行配置。

### 2.3 PWA：vite-plugin-pwa

- 基于 Workbox 7，生成 Service Worker + Web App Manifest。
- **缓存策略**（按资源类型区分，这很关键）：
  - HTML：**Network First + 3 秒超时**（保证能拿到更新，断网时回落缓存）
  - JS/CSS/图标：**Cache First**（带内容 hash，文件名变即失效）
  - 题目数据与题库：**不走 SW**，由 SQLite/OPFS 自己管理
- ⚠️ **坑**：precache 要用 `cache.add()` 逐个加并 `catch`，**不要用 `cache.addAll()`**——后者是原子的，一个图标 404 会导致整个安装失败，用户拿不到离线壳。
- 需要 HTTPS 托管（本地开发用 `localhost` 例外）。

### 2.4 打包为可安装 App

#### Android：Bubblewrap / PWABuilder（TWA）

```
前提：PWA 已部署在 HTTPS 域名，配置 /.well-known/assetlinks.json 完成域名所有权验证
  → Bubblewrap CLI 或 PWABuilder 网页端生成 AAB
  → 上传 Google Play Console（$25 一次性费用）
```

| 优点 | 缺点 |
|---|---|
| ✅ **不需要 Android Studio**（符合你的约束） | 需要 HTTPS 域名 + Digital Asset Links 验证 |
| ✅ 真 Chrome 引擎，Service Worker 全部能力可用 | 用户禁用 Chrome 时降级为 Custom Tab |
| ✅ **更新即时**：刷新网页即可，不用重新发版 | Google Play 要求 AAB 格式（不是 APK） |
| ✅ PWABuilder 可在线生成，零本地构建 | 无法深度调用原生 API |

**命令**：
```bash
npm i -g @bubblewrap/cli
bubblewrap init --manifest https://your-domain.com/manifest.webmanifest
bubblewrap build      # → app-release-signed.aab
```
或用 PWABuilder 网页端（https://www.pwabuilder.com）输入 URL 直接下载，更省事。

#### iOS：两条路

| 路径 | 说明 | 建议 |
|---|---|---|
| **A. Safari 添加到主屏幕**（推荐本期） | 零成本、零审核、即时更新、支持离线 | ✅ **先做这个** |
| **B. Capacitor + Xcode 上架** | 需要 macOS + Xcode（本机跑不了），且 App Store 4.2 有拒审风险 | ❌ 本期不做，留作未来选项 |

#### Capacitor 的定位（重要澄清）

Capacitor **不是**本阶段的必需品，它是"将来需要原生能力时"的升级路径：
- 写代码阶段：纯 Web，**不需要任何原生工具链**
- 需要原生 SQLite / 推送 / 上架 iOS 时：`npx cap add ios/android`，届时才需要 Xcode / Android SDK
- 如果走 TWA，Android 端可以永远不碰 Android Studio

---

## 3. 资源占用预估

### 开发期（你的机器）

| 项 | 内存占用 | 说明 |
|---|---|---|
| Node.js 运行时 | 80-150 MB | 基础 |
| **Vite dev server** | **250-500 MB** | Vue 3 项目典型值 |
| 浏览器（调试） | 300-800 MB | 取决于标签页数 |
| **合计（常规开发）** | **约 0.6-1.5 GB** | 8GB 内存机器可流畅运行 |
| 生产构建峰值 | 0.8-1.5 GB | 短暂峰值 |

### 磁盘占用

| 项 | 体积 |
|---|---|
| `node_modules` | 约 350-550 MB |
| 项目源码 | < 5 MB |
| 内置题库数据（2 科，压缩） | 约 8-15 MB |
| SQLite WASM | 约 1.2 MB |
| **构建产物 `dist/`** | **约 3-6 MB**（首屏 JS 约 300-500KB gzip） |

### 对比（这就是选这条路的原因）

| 工具链 | 内存 | 磁盘 | 备注 |
|---|---|---|---|
| **本方案（Node + Vite）** | **0.6-1.5 GB** | **< 1 GB** | ✅ 可跑 |
| Android Studio + 模拟器 | 4-8 GB | 10-20 GB | ❌ 本机难承载 |
| Xcode + iOS Simulator | 4-8 GB | 20-40 GB | ❌ 本机难承载 |
| Flutter（Dart + Gradle） | 3-6 GB | 8-15 GB | ⚠️ 偏重 |

### 运行时（用户手机）

| 项 | 占用 |
|---|---|
| 首屏 JS（gzip） | 约 300-500 KB |
| SQLite WASM | 约 600 KB（gzip） |
| 题库数据 | 8-15 MB（按需加载，可分科下载） |
| 存储空间占用 | 建议控制在 **40MB 以内**（避开 iOS 50MB 红线） |

---

## 4. 主要取舍（诚实版）

| # | 取舍 | 说明 |
|---|---|---|
| 1 | **iOS 数据可靠性打折** | 见 §0 限制一。对策：强制安装引导 + 激进备份提醒 + 配额监控。**这是本方案最大的软肋** |
| 2 | **iOS 上架困难** | 见 §0 限制二。本期接受"iOS 只做 PWA 分发" |
| 3 | **无原生能力** | 无后台同步、无系统级推送（iOS 受限）、无原生文件系统。对本产品影响有限（PRD 里没有强原生需求），但"学习提醒"要降级为应用内提醒 |
| 4 | **WebView 渲染性能** | 长列表（如 1000+ 题的错题本）需要虚拟滚动。Vant 有 List 组件，或引入 `vue-virtual-scroller` |
| 5 | **SQLite WASM 的复杂度** | 多一层 Worker 通信 + WASM 体积。换来的是 SQL 可复用。如果觉得重，可降级为 Dexie |
| 6 | **必须 HTTPS 托管** | TWA 与 PWA 都要求。本地开发用 localhost 可以，真机测试需要 HTTPS（可用 ngrok/内网穿透或免费静态托管） |
| 7 | **Google Play 上架有门槛** | $25 注册费 + 需提供隐私政策 + 内容分级问卷。本地存储型 App 的隐私政策反而简单（不收集数据） |

---

## 5. 目录结构（建议）

```
jing shi-ai/
├── public/
│   ├── manifest.webmanifest
│   ├── icons/                     各尺寸图标（PWA 必需）
│   └── data/                      内置题库（可分科懒加载）
│       ├── econ-base.json.gz      经济基础
│       └── hr.json.gz             人力
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── router/                    路由
│   ├── layouts/
│   │   └── TabBarLayout.vue       底部 5 Tab 布局
│   ├── pages/                     页面（对应 PRD 页面结构）
│   │   ├── onboarding/            5 步引导
│   │   ├── study/                 学习 Tab
│   │   │   ├── TodayPage.vue      今日任务
│   │   │   ├── RoadmapPage.vue    上岸地图
│   │   │   ├── KnowledgeTreePage.vue
│   │   │   └── KnowledgeDetailPage.vue  精讲
│   │   ├── practice/              题库 Tab
│   │   │   ├── ChapterPage.vue
│   │   │   ├── SpecialPage.vue
│   │   │   ├── RealExamPage.vue
│   │   │   ├── WrongBookPage.vue
│   │   │   ├── QuestionSetListPage.vue
│   │   │   ├── ImportPage.vue     上传导入
│   │   │   └── ImportPreviewPage.vue  预览确认
│   │   ├── quiz/                  答题页（共用组件）
│   │   │   ├── QuizPage.vue
│   │   │   └── ExplanationDrawer.vue
│   │   ├── ai/                    AI Tab
│   │   │   ├── ChatPage.vue
│   │   │   ├── GeneratePage.vue
│   │   │   └── AiSettingsPage.vue  BYOK 配置
│   │   ├── exam/                  模考 Tab
│   │   │   ├── ExamHomePage.vue
│   │   │   ├── ExamPreparePage.vue    模式选择
│   │   │   ├── ExamRoomPage.vue       ⭐全真机考界面
│   │   │   ├── ExamReportPage.vue     AI 复盘
│   │   │   └── ExamHistoryPage.vue
│   │   └── mine/                  我的 Tab
│   │       ├── DashboardPage.vue  数据看板
│   │       ├── PredictionPage.vue 过线预测
│   │       ├── BackupPage.vue     备份与恢复
│   │       ├── StoragePage.vue    存储管理
│   │       └── SettingsPage.vue
│   ├── components/                业务组件
│   │   ├── question/              题目渲染、选项、解析
│   │   ├── exam/                  题号栏、计算器、分栏切换、标记
│   │   └── common/                进度环、掌握度条、空状态
│   ├── stores/                    Pinia
│   │   ├── plan.ts  quiz.ts  exam.ts  user.ts  ai.ts
│   ├── domain/                    ⭐领域层（与数据源无关）
│   │   ├── entities/              Question, KnowledgeNode, StudyPlan...
│   │   ├── repositories/          QuestionRepo 等接口定义
│   │   └── services/              FSRS 调度、掌握度计算、计划生成
│   ├── infrastructure/            ⭐基础设施（可替换）
│   │   ├── db/
│   │   │   ├── sqlite/            SQLite WASM 实现 + Worker
│   │   │   ├── dexie/             （备选）IndexedDB 实现
│   │   │   └── schema.sql         ⭐与 PRD §11 完全一致
│   │   ├── mock/                  mock 数据生成器
│   │   └── http/                  （预留）真实接口实现
│   ├── styles/                    设计令牌（对齐 PRD §10）
│   └── utils/
├── src/worker/
│   └── sqlite.worker.ts           ⭐SQLite 运行在 Worker 中
└── vite.config.ts                 含 PWA 配置
```

**关键分层**：`domain/` 不依赖任何数据源，`infrastructure/` 可整体替换。这就是"便于后续替换"的保证。

---

## 6. 页面清单（对齐 PRD）

| Tab | 页面 | PRD 依据 | 优先级 |
|---|---|---|---|
| — | Onboarding 5 步引导 + 作战地图 | §9.1 | P0 |
| 学习 | 今日任务（进度环 + 任务包） | M1-F2 | P0 |
| 学习 | 上岸地图（阶段甘特图） | M1-F1 | P0 |
| 学习 | 知识树（三级 + 热力图） | M2-F1 | P0 |
| 学习 | 知识点精讲（五段式） | M2-F2 | P0 |
| 题库 | 章节练 / 专项练 / 真题 / 高频必刷 | M3-F1 | P0 |
| 题库 | 错题本（按知识点聚合） | M4-F1 | P0 |
| 题库 | **自建题库 + 上传导入 + 预览确认** | M7 | P0 |
| 共用 | **答题页**（确信度自评 → 作答 → 反馈 → 四级评分） | M3-F2 | P0 |
| 共用 | 解析抽屉（逐项解析 + 陷阱词 + 举一反三） | A2 | P0 |
| AI | AI 私教对话（上下文注入 + 流式） | A4 | P0 |
| AI | AI 出题配置（题型/认知层次/风格） | A1 | P0 |
| AI | AI 设置（BYOK Key 管理） | §M9-F2 | P0 |
| 模考 | 模考首页 / 准备页（严格/宽松模式） | M5-F2 | P0 |
| 模考 | **全真机考作答页** ⭐ | M5-F1 | P0 |
| 模考 | 交卷确认 + AI 复盘报告（含遗憾分分析） | M5-F5 | P0 |
| 我的 | 数据看板（热力图/雷达图/趋势） | M8-F1 | P1 |
| 我的 | 过线预测 | M8-F2 | P1 |
| 我的 | **备份与恢复**（时间机器 + 导出导入） | M10-F3/F4 | P0 |
| 我的 | 存储管理 / 设置 | M10-F5/F6 | P1 |

合计约 **22-25 个页面**。

---

## 7. 数据层设计（重点）

### 7.1 表结构完全对齐 PRD

`infrastructure/db/schema.sql` 直接采用 PRD §11 与《本地存储技术方案》§4 的 DDL，包括：
- `question`（含 `source_level` 四级可信度、`owner_type`、`ai_metadata`）
- `user_question_state`（FSRS 的 D/S/R + `due_date`）
- `answer_log`、`user_knowledge_state`、`study_plan`、`daily_task_pack`
- `backup_snapshot`、`attachment`、`ai_response_cache`

### 7.2 双库 → 双数据源

PRD 里是 `bundled.db`（只读）+ `user.db`（读写）。Web 端对应为：

| PRD（原生） | Web 端实现 |
|---|---|
| `bundled.db`（只读，内置题目） | 打包的 JSON.gz 题库，**首次启动导入 SQLite**（或按需分科导入） |
| `user.db`（读写，用户数据） | SQLite WASM 主库（OPFS 持久化） |
| ATTACH 跨库查询 | 导入后同库，用 `owner_type` 字段区分 |

简化点：Web 端直接把内置题导入主库，用 `owner_type='official'` 标记，避免跨库查询的复杂度。更新内置题库时按 `content_version` 做增量补丁。

### 7.3 Mock 数据规格

按 PRD 数据字段生成结构一致的假数据：

| 数据 | 规模（建议） |
|---|---|
| 经济基础知识点 | 6 模块 → 约 37 章 → 约 220 个知识点 |
| 人力知识点 | 3 部分 → 19 章 → 约 120 个知识点 |
| 题目 | 每科约 600-1000 题（覆盖各章），mock 阶段够用 |
| 答题记录 | 预置约 800 条（让数据看板有内容可看） |
| 错题 | 预置约 60 道（分布在薄弱模块） |
| 模考记录 | 预置 3 次（让趋势图有数据） |
| 学习计划 | 预置一份进行中的 T-90 计划 |

**Mock 数据要"有故事"**：预置的答题记录要能让知识树热力图呈现出合理的强弱分布（如会计薄弱 31%、法律掌握 85%），这样一打开就能看到 PRD 描述的效果，而不是一片均匀。

---

## 8. 需要你确认的问题

在动手之前，以下几项需要你拍板（我会按推荐值执行，除非你另有指定）：

| # | 问题 | 我的推荐 | 备选 |
|---|---|---|---|
| **1** | **前端框架** | **Vue 3.5 + TS** | React 19 + antd-mobile |
| **2** | **本地数据层** | **SQLite WASM**（SQL 可复用到原生端） | Dexie（更轻更简单） |
| **3** | **本期是否包含 Capacitor 工程** | **不搭建**（纯 Web + PWA，需要时再加） | 现在就搭好骨架 |
| **4** | **iOS 策略** | **只做 PWA 分发**（Safari 添加到主屏幕） | 现在就考虑 Capacitor + 云端构建 |
| **5** | **页面范围** | **先做 P0 的 15 个页面**（见 §6），数据看板等 P1 放第二批 | 一次性全做 |

---

## 9. 确认后的实施步骤

```
Step 1  项目脚手架 + 设计令牌（对齐 PRD §10）+ TabBar 布局
Step 2  数据层：schema.sql + SQLite Worker + Repository + Mock 生成器
Step 3  核心答题闭环（答题页 + 解析 + FSRS 评分）—— 这是 App 的心脏
Step 4  学习 Tab（今日任务 + 知识树 + 精讲）
Step 5  题库 Tab（练习模式 + 错题本 + 自建题库导入）
Step 6  模考 Tab（全真机考界面 + 复盘报告）⭐差异化重点
Step 7  AI Tab（对话 + 出题 + BYOK 设置）
Step 8  我的 Tab（备份恢复 + 数据看板）
Step 9  PWA 配置 + 安装引导 + 离线验证
Step 10 构建优化 + 真机测试 + 部署
```

每一步我会跑通构建并做冒烟验证再往下走。

---

**一句话总结**：这套方案让你在**不装任何原生工具链**的前提下，得到一个可离线、可安装、数据结构与 PRD 完全一致的 App；代价是 iOS 上的数据可靠性依赖"添加到主屏幕"与用户主动备份，这一点需要用产品手段（强制引导）而非技术手段来兜底。
