# 《经师 AI》——中级经济师上岸 App 产品需求文档（PRD）

| 项目       | 内容                                                       |
| -------- | -------------------------------------------------------- |
| 文档版本     | **v1.5 定稿（免费 + 开屏广告变现 + AI 纯 BYOK，全面回归无服务端）**              |
| 文档状态     | ✅ **已定稿，可进入功能实现阶段**                                       |
| 撰写日期     | 2026-09-03（v1.2：2026-09-20 / v1.3：2026-09-20 / v1.4：2026-09-20 / **v1.5 定稿：2026-09-20**） |
| 产品定位     | 以「学习科学 + 全真机考 + AI 私教」为核心的**纯本地、完全免费**中级经济师备考 App      |
| 目标考试     | 全国经济专业技术资格考试（中级）2026 年度，考试日 11 月 7-8 日                   |
| **科目范围** | **《经济基础知识》+《人力资源管理》**（其余 8 个专业后续版本支持）                    |
| **存储架构** | **纯本地存储，无远程数据库、无账号、无云端同步、无任何服务端**（v1.5 起无例外）                     |
| **商业模式** | **App 完全免费 + 开屏/激励视频广告变现 + 付费去广告（¥68 一次性买断）**（见 §M9）      |
| **AI 模式** | **纯 BYOK：用户自备 API Key，设备直连服务商。我们不中转、不代理、不经手任何请求**（见 §M9-F7）     |
| 交付形态     | iOS / Android 原生 App                                     |
| 阅读对象     | 产品、设计、研发、教研、法务合规、运营                                      |

---

## 📌 版本变更摘要

本产品经历了**五次重大调整**。**以下变更为产品设计的前提，优先级高于任何早期冲突表述。**

### 变更 E+（v1.5 实现期补充）：广告模块落地时发现并修正的两处问题

> 本节记录**进入编码阶段后**发现的、需要回写 PRD 的实质问题。均不改变产品方向，属**实现期校正**。

| # | 问题 | 修正 |
| - | ---- | ---- |
| 1 | **自然日兜底会产生"意外加速"** —— 兜底逻辑无条件生效，导致「只用了 1 天就放置、19 天后再打开」的用户被自然日兜底**直接顶到阶段 3 完全体**，与 M9-F4「放置 30 天开始用应从阶段 0 开始」的明文要求直接冲突 | 兜底增加前置条件：**距上次活跃 ≤ 3 天**才生效；且活跃天数为 0 时兜底不生效。详见 M9-F4 关键设计约束 5 |
| 2 | **广告位数量与代码不一致** —— v1.4 撤销 AI 额度体系后，代码中仍残留 `ai_quota` 广告位（类型定义、释放表、单测断言、UI 文案共 4 处） | 全部清理，与 v1.5「3 个广告位」对齐。**`ai_quota` 禁止重新引入**（已在代码注释中写明） |

> ⚠️ 记录此节的原因：这两处都是**"文档正确、实现走偏"**的典型。PRD 的意图是清晰的，但实现时若不逐条对齐就会悄悄偏离 —— 兜底那条尤其危险，它不会报错，只会在真实用户身上表现为"怎么突然全是广告"。

---

### 变更 E（v1.5 定稿）：撤销 AI 中转，全面回归无服务端

> ⚠️ **本节推翻 v1.4 变更 E，v1.4 的"AI 双路径"设计作废。**
> 保留此记录是为了说明**为什么推翻**，避免后续再次反复。

| 维度          | v1.4（BYOK + 中转双路径）        | **v1.5（纯 BYOK，零服务端）**            |
| ----------- | ------------------------- | --------------------------------- |
| **服务端**     | 需要维护一个无状态转发层              | **完全没有**（回归 NG9 原始口径）             |
| **AI 首次使用** | 选服务商 → 点"直接使用" → 立即可用     | 配 API Key 后才能用（**这是有意识的取舍，不是缺陷**） |
| **平台 AI 成本** | ≈ ¥1.0/新用户                | **¥0**（永久）                        |
| **运维负担**    | 7×24 服务可用性、成本监控、限流维护      | **零运维**                           |
| **法务风险**    | R-20：转发层可能触发大模型备案义务       | **R-20 撤销**（我们不在链路上）              |
| **AI 产品定位** | 核心差异化能力，力求 100% 新用户触达     | **可选增强**（见下"定位降级"）                |
| **合规复杂度**   | 中（备案判定 + 分路径披露 + 不留存证明）   | **低**（不收集、不转发、不存储）                |
| **待决问题**    | ✅ 全部决断（§16.1）             | ✅ **全部决断**，待决项 **0 项**            |

**改动理由（四句话）**：

1. **中转与产品基石冲突，这个冲突无法调和** —— 本产品的立身之本是「**无账号、无云端、设备即身份、数据 100% 本地**」。v1.4 试图用"无状态转发层"的窄口径修订绕开 NG9，但**只要链路上存在一台我们的服务器，就必然带来 7×24 可用性责任、成本监控责任、备案合规责任**——这些都不因"无状态"而消失。
2. **对个人开发者而言，这是不该背的运营负担** —— 没有法务、没有运营团队、没有 SRE。一台线上服务的真实成本不是服务器租金，而是**长期的心力占用**：服务挂了要立刻爬起来修，成本超了要立刻调参，被投诉了要立刻应对。
3. **"成本可控"的论证只算了钱，没算人力** —— v1.4 说"一次性 50 次 = 获客成本，不是运营黑洞"。这个判断在**有团队**的前提下成立；对个人开发而言，**任何需要值守的服务都是黑洞**。
4. **AI 从"核心功能"降级为"可选增强"，是更诚实的定位** —— 与其承诺一个人手不足的产品必然维护不好的服务，不如坦白：AI 只对愿意配置 Key 的用户可用，**不配置不影响任何学习功能**。核心价值始终是刷题、错题本、记忆曲线、全真模考。

##### 定位降级：AI 从"核心功能"→"可选增强"

> 🔧 **这是 v1.5 最重要的产品判断，必须贯穿所有文案与交互。**

| 维度        | v1.4 口径            | **v1.5 口径**                       |
| --------- | ------------------ | --------------------------------- |
| AI 的地位    | 三大核心之一（学习科学 + 机考 + AI 私教） | **核心之外的增强项**（核心 = 刷题 + 错题 + 记忆曲线 + 模考） |
| 未配置 Key 时 | 视为「转化漏斗的断点」，需用中转兜住 | **视为正常状态**，不是断点、不是缺陷             |
| 必须向用户明示  | —                  | **「不配置不影响任何学习功能」**（`AI_IS_OPTIONAL_NOTICE`，不可删除）  |
| 目标触达率     | 接近 100% 新用户        | **不设目标**——愿意配置的人自然配置，我们不做诱导       |

##### 零成本替代方案（v1.5 新增，替代中转承担的职责）

中转原本要解决的是「**用户不知道配了 Key 能换来什么，所以没动力配**」。v1.5 用三个**零服务端成本**的方案解决同一问题：

| 方案                | 实现方式                                    | 服务端成本      | 对应实现                       |
| ----------------- | --------------------------------------- | ---------- | -------------------------- |
| **① 静态 AI 示例解析**  | 内置几组真实考点的样例问答（把教材黑话翻译成大白话 / 编口诀 / 错因诊断），打包进 App，零网络请求 | **¥0**     | `src/domain/ai/samples.ts` + `AiSamplePage.vue` |
| **② Key 配置图文教程**  | 四步流程 + 服务商申请入口 + 常见报错对照表，全部纯静态           | **¥0**     | `AiKeyTutorialPage.vue`    |
| **③ 明示「不影响学习」**   | 在设置页、对话页、示例页三处明示 AI 是可选增强，消除"功能缺失"误解     | **¥0**     | `AI_IS_OPTIONAL_NOTICE`（常量，不可删） |

> 💡 **核心洞察**：中转做的事（让用户看见 AI 的价值）**不需要服务器也能做**。预置样例 + 图文教程 = 同样的说服力，成本为零，且永远不会挂。

##### ⛔ 明确放弃的东西（避免反复）

- **不做任何形式的 AI 转发/代理/中转** —— 本版之后不再重新评估，除非产品从"个人开发"变成"有专职运维的团队产品"。
- **不做 AI 额度体系** —— 没有中转，就没有"平台额度"这一概念（BYOK 的额度由用户自己的服务商账号决定，我们无从知晓也不该假装知道）。
- **不做 AI 会员/订阅/付费解锁** —— NG13/NG14 红线不变，且现在连"额度"这种形态也一并取消。

### 变更 D（v1.3）：广告结构重构 + AI 退出中间商

| 维度          | v1.2（免费 + 广告版）        | **v1.3（开屏驱动 + 纯 BYOK）**                |
| ----------- | --------------------- | --------------------------------------- |
| **广告主力**    | 激励视频（用户主动触发）          | **开屏广告（稳定曝光）+ 激励视频（高 eCPM）**           |
| **广告位数量**   | 5 个                   | **4 个**（删除结算页加练位、内容页原生卡片）              |
| **新用户策略**   | 48 小时零广告              | **D0 纯净 → D1 开屏 → D3 成果卡 → D6 完全体**（渐进释放） |
| **考前策略**    | ❌ 考前 7 天全部关闭广告（已取消）   | ⚠️ 考前 7 天**只保留开屏**（减少而非消失，保持认知一致）       |
| **AI 模式**   | 平台每日 5 次免费额度 + BYOK   | **纯 BYOK + 一次性 20 次体验额度**（平台不做中间商）<br>⚠️ *此列为 v1.3 历史态，v1.4 曾被变更 E 改为双路径，**v1.5 已撤销中转、回归纯 BYOK***      |
| **AI 成本**   | ≈ ¥9 万/月（最大成本项）       | **≈ ¥0**（仅获客成本 ¥0.4/新用户）*（v1.3 历史态；v1.5 为 ¥0，无获客成本项）*      |
| **ARPDAU 目标** | ≥ ¥0.11              | **≥ ¥0.15**                             |
| **核心风险**    | 广告伤体验、AI 成本失控         | **开屏对启动速度的影响**（R-15）（v1.5 起 R-20 已撤销）        |

**改动理由（三句话）**：
1. **激励视频收入过于依赖用户主动行为**（实测大多数人打开 App 直奔任务，不会点奖励入口）→ 需要开屏提供**稳定、可预测**的收入底盘。
2. **v1.2 的"考前 7 天停广告"会让广告的出现变得不可预测**，用户在后期才发现"原来可以没有广告"，反而产生被欺骗感 → 改为**由少到多的渐进释放**，一开始就坦诚。
3. **平台做 AI 中间商成本高、风险大、责任重，而毛利极薄** → 退出这条链，改为**只搭桥**（引导用户直连服务商），成本归零、责任厘清、隐私叙事更纯净。

### 变更 C（v1.2）：商业化换轨 —— 免费 + 广告变现

| 维度      | v1.1（本地化买断版）          | **v1.2（免费 + 广告版）**                       |
| ------- | --------------------- | ---------------------------------------- |
| 获客门槛    | ¥168 全科买断 / ¥98 单科   | **¥0，无付费墙**                              |
| 核心能力    | 锁在付费墙后                | **全部免费**（题库/模考/复盘/复习调度/计划/过线预测）           |
| 主要收入    | 买断                    | **广告** + 付费去广告                     |
| 去广告     | —                     | **¥68 一次性买断**（本考季）/ ¥128（终身）             |
| 反暗黑模式约束 | §5.3 通用原则             | **升级为 M9-F1 三条铁律**（学习心流零广告为硬约束）           |

**改动理由**：付费墙挡住的恰好是最需要这个产品的人（预算敏感的宝妈考生、二战单科考生）。广告模式把使用门槛降到零，让"科学备考"惠及更多人，代价是我们必须在广告设计上极度克制。**详见 §M9。**

### 变更 A（v1.1）：纯本地存储，无远程数据库

| 维度    | v1.0（C/S 架构）  | **v1.1（本地优先）**                |
| ----- | ------------- | ----------------------------- |
| 数据存储  | 云端数据库 + 客户端缓存 | **设备本地数据库（SQLite）**           |
| 账号体系  | 手机号/微信登录      | **无账号，开箱即用**                  |
| 多端同步  | 实时同步          | **取消**。跨端通过「备份文件」迁移           |
| 离线能力  | 部分功能离线        | **全部功能离线可用**（AI / 广告除外）       |
| 商业化   | 服务端订阅校验       | **平台 IAP + 无自有服务端**（见 §M9）    |
| 数据风险  | 服务端负责         | ⚠️ **数据丢失风险转移给用户，备份成为 P0 功能** |
| AI 服务 | 服务端代理调用       | **客户端直连 LLM API**（见 §8.8）     |

**本地化带来的三个意外收获**（要作为产品卖点放大）：

1. **隐私优势**：学习数据、笔记、上传的资料**永不上传服务器**——备考类产品最有力的信任状。
2. **自建题库能力提升**（M7）：资料存在本地，无云端存储成本、无版权上传顾虑，容量限制可大幅放宽（从 500/2万 题 → 建议放开到 10 万题）。
3. **模考更可靠**：本地计时不依赖网络，断网/弱网不影响考试流程。

**本地化带来的三个必须解决的问题**：

1. **数据丢失 = 全没了** → 备份/恢复/换机迁移升级为 P0（见 M10）。
2. **AI 必须联网** → 需明确联网边界与 API Key 归属（见 §8.8），这是本地架构下唯一的网络依赖。
3. **⚠️ v1.2 新增：广告必须联网** → 广告 SDK 依赖网络，而本产品的核心场景恰恰包含弱网（地铁通勤）。**广告的加载失败绝不能影响任何学习功能**（见 M9-F11）。

### 变更 B（v1.1）：科目范围收窄为两科

- **《经济基础知识》**：六大模块全量覆盖（见 §1.1）
- **《人力资源管理》**：按 **2026 版新教材框架**（三大部分 19 章，教材框架已重构，见 §1.1-B）全量覆盖
- 其余 8 个专业（工商管理、金融、财政税收等）本期不做，但**数据模型保留多科目扩展能力**（§11）

> ⚠️ **重要提示**：2026 版《人力资源管理》教材框架已重构（原四部分 → 新三部分，章节重排），网上大量资料仍在沿用旧框架。**本产品的知识树必须按新框架构建**，否则学习顺序全错。详见 §1.1-B。

---

## 0. 一页纸看懂这份文档

我们要做的不是一个"题库 App"，而是一台 **把考生有限时间兑换成分数的机器**。

市面上的经济师 App 有三类：视频课搬运工（学完就忘）、题库堆砌机（刷完不总结）、押题贩子（靠玄学）。它们共同的失败点在于——**把"学过"当成"学会"，把"刷过"当成"会做"**。

这个产品的核心主张是三条：

1. **用学习科学替代经验主义**——间隔重复（FSRS-6）决定你今天该复习什么，主动回忆决定你怎么练，交错练习决定你怎么混，认知负荷理论决定界面长什么样。不是"我觉得你应该刷第三章"，是算法算出来你今天必须复习第 3.2.1 节。
2. **用全真机考消解考场陌生感**——1:1 还原中国人事考试网机考界面，包括那个最要命的"按题型分段锁定、进入下一题型不可返回"。练过 5 遍的人，考场上不会手忙脚乱。
3. **用 AI 补齐"人"的缺口**——真人私教太贵，录播课不会回答你的问题。AI 负责出题、解析、诊断、追问、编口诀、动态规划。但 AI 会编造，所以我们要建一套分级可信度体系和校验流水线，这是本产品的生命线。

**一句话定位**：给在职考生的中级经济师备考操作系统——科学计划 + 自适应刷题 + 全真模考 + AI 私教。

---

## 1. 问题陈述（Problem Statement）


### 1.1 考试事实基线（已核实，作为产品设计的硬约束）

以下数据来自中国人事考试网考务通知及各省人事考试中心 2026 年度文件，是产品逻辑的地基。**任何需求不得与以下事实冲突。**

| 维度       | 《经济基础知识》                      | 《专业知识和实务》                                                  |
| -------- | ----------------------------- | ---------------------------------------------------------- |
| 考试形式     | 电子化考试（机考），客观题                 | 同左                                                         |
| 考试时长     | 90 分钟                         | 90 分钟                                                      |
| 题型题量     | 单选 70 题 × 1 分 + 多选 35 题 × 2 分 | 单选 60×1 + 多选 20×2 + 案例 20×2                                |
| 总题量      | 105 题                         | 100 题                                                      |
| 满分 / 合格线 | 140 分 / **84 分**（60%）         | 140 分 / **84 分**                                           |
| 单题平均时限   | 约 51 秒                        | 约 54 秒                                                     |
| 专业方向     | 公共必考                          | 10 选 1：工商管理、农业经济、财政税收、金融、保险、运输经济、人力资源管理、旅游经济、建筑与房地产经济、知识产权 |
|          |                               |                                                            |

**关键规则（直接影响产品设计）：**

- **R1 评分规则**：单选 4 选 1，选对得 1 分。多选 5 选项，正确答案 2-4 个（不存在 5 个全对），**错选整题 0 分，少选且无错项时每个正确选项得 0.5 分**。案例题为不定项选择，评分规则同多选。  
  → 产品启示：必须内置"多选保守策略训练"，这是 84 分线上最大的捡分口子。
- **R2 成绩滚动**：两科独立计分、独立合格，**连续两个考试年度内**通过两科即可拿证。公共科目合格成绩在滚动期内可跨专业沿用。  
  → 产品启示：必须支持"单科备考模式"和"二战用户档案继承"，这是高频刚需。
- **R3 两科连考**：同时报考两科的考生，两科在同一批次内分 2 个场次连续组织，**间隔 40 分钟**。  
  → 产品启示：需要"连考体力/节奏训练"，两科连做的模考场景。
- **R4 入场与交卷**：开考 5 分钟后禁止入场；考试结束前 15 分钟可提前交卷。草稿纸考场统一发放，禁止自带计算器（使用系统内置科学计算器）。
- **R5 机考系统界面**（据中国人事考试网模拟系统操作指南）：
  - 布局：上方标题栏（考生信息 / 考试名称 / 科目 / 机位号 / 剩余时间 / 交卷按钮）、**左侧题号栏**、右侧试题栏、底部作答栏
  - 题号栏状态色：**白色=未作答，蓝色=已作答，橙色=当前作答，三角形符号=已标记**
  - 案例题支持**左右分栏 / 上下分栏切换**
  - **强调显示**：选中文字后标黄背景（切换试题后标记取消）
  - 文字缩放、内置科学计算器、标记按钮
- **R6 ⚠️ 题型分段锁定（待最终核实，见 §16 待决问题 Q1）**：部分资料称机考系统**按题型锁定，做完单选进入多选后无法返回修改单选**；案例题内部小题可回退，但进入下一案例后不可返回。另有资料称可随时点题号跳转修改。  
  → **产品设计对策（保守优先）**：模考模块默认启用 **「严格模式」**（按题型分段锁定，最严约束），同时提供 **「宽松模式」** 开关。**在真实环境下训练最严约束，永远不会吃亏**——练严格模式的人到了宽松考场是超配，反之则是灾难。
- **R7 经济基础六大模块分值权重**（决定学习优先级排序）：

| 模块    | 分值      | 占比   | 特征                  | 学习优先级策略               |
| ----- | ------- | ---- | ------------------- | --------------------- |
| 经济学基础 | 29-30 分 | ~21% | 逻辑性强、理解门槛高、是其他模块的基础 | **最先学**（分值最高 + 是理解底座） |
| 财政    | 23-25 分 | ~17% | 考点细碎、记忆量大           | 中期（配合口诀记忆）            |
| 货币与金融 | 22-23 分 | ~16% | 贴近生活、理解难度低          | 早期（快速建立信心与分数）         |
| 统计    | 20-22 分 | ~15% | 公式为主、套路固定           | 中期                    |
| 会计    | 20-22 分 | ~15% | 分录与报表、需理解           | 中期偏后                  |
| 法律    | 20-22 分 | ~15% | 纯记忆、遗忘速度快           | **最后学**（临近考试背，遗忘损失最小） |


### 1.1-B 《人力资源管理》科目结构（2026 版新框架）⚠️

> **2026 版教材框架已重构**：原四部分 → **新三部分**，章节全面重排，变动内容关联分值约 **8-10 分**。  
> 网上仍在流传的"组织行为学/人力资源管理/经济分析/社保政策"四部分结构**是旧版，不可采用**。

**新框架三大部分分值分布**

| 部分          | 章节        | 分值         | 特征                                           | 学习策略                     |
| ----------- | --------- | ---------- | -------------------------------------------- | ------------------------ |
| **第一部分**    |           |            |                                              |                          |
| 人力资源与社会保险政策 | 第 1-6 章   | **约 29 分** | 法规内容为主，**考核不深但知识点极度分散**；2026 年新增内容最集中、出题概率最高 | 中期偏后（法条记忆，临考回炉）          |
| **第二部分**    |           |            |                                              |                          |
| 人力资源管理专业理论  | 第 7-12 章  | **约 56 分** | 理论密度最高（激励/领导/组织设计/劳动力市场/人力资本投资），**计算题主要来源**  | **最先学**（分值最高 + 需理解，非纯记忆） |
| **第三部分**    |           |            |                                              |                          |
| 人力资源管理实务    | 第 13-19 章 | **约 55 分** | 人力规划/甄选/绩效/薪酬/劳动关系，**案例分析题集中出题区域**           | 中期（配合案例题训练）              |

**近 5 年真题高频章节（决定"高频必刷"清单与冲刺优先级）**

| 章节                         | 考查次数            | 核心考点                                                 |
| -------------------------- | --------------- | ---------------------------------------------------- |
| **第 9 章 组织设计与组织文化**        | **15 次** 🥇     | 组织设计类型（职能制/矩阵制/事业部制）、组织文化类型（学院型/俱乐部型/棒球队型/堡垒型）——高频多选 |
| **第 10-12 章 劳动力市场与人力资本投资** | **合计约 30 次** 🥇 | 劳动力供求弹性计算、高等教育投资决策、在职培训成本收益分摊                        |
| **第 16-17 章 绩效与薪酬管理**      | 约 15 次          | 战略性薪酬设计、股权激励、绩效评价方法及常见误区——**案例题核心区**                 |
| 第 8 章 领导行为理论               | 10 次            | 路径-目标理论四种领导风格、管理方格图、生命周期理论对应关系                       |
| 第 1 章 劳动合同管理与特殊用工          | 9 次             | 劳动合同解除法定情形、劳务派遣期限、非全日制用工细节                           |
| 第 7 章 组织激励理论               | 7 次             | 需要层次/ERG/期望理论；**区分内容型 vs 过程型激励理论**                   |

**2026 年新增/变动考点（≈8-10 分，性价比最高，务必单独建专题）**

| 章节     | 变动内容                                                   |
| ------ | ------------------------------------------------------ |
| 第 1 章  | 劳动合同无法继续履行的判定标准（新增）                                    |
| 第 3 章  | 城乡居民基础养老金最低标准、医保基金先行支付适用情形、工伤认定中"非本人主要责任"与"工作时间"界定（新增） |
| 第 4 章  | 劳动仲裁特殊处理情形（调整）                                         |
| 第 6 章  | 科技项目资金管理规范、"突出业绩奖励"相关奖项规定（新增）                          |
| 第 8 章  | 领导技能中"人际技能"相关知识点                                       |
| 第 15 章 | "履历分析"更名为 **"传记资料分析"** ⚠️ 术语变更，旧资料会误导                  |
| 第 16 章 | **战略性绩效管理**（核心重点）                                      |
| 第 17 章 | **股票期权激励对象相关规定**（核心重点）                                 |

**产品落地要求**：

1. 知识树严格按新三部分框架构建，章节编号 1-19。
2. 建立 **「2026 教材变动专题」** 题集，收录全部新增/变动考点，作为**全量题库的一部分**（v1.2：**免费开放，不再作为付费专属内容**——在广告模式下，把高频价值内容锁起来会严重伤害免费用户的通过率，进而伤害口碑。新考点首年命题概率高，**让所有用户都能练到，才是产品的正确选择**）。
3. 第 15 章术语变更（履历分析→传记资料分析）需在**旧题数据清洗时批量替换**，避免出现旧术语误导用户。
4. 人力科目学习顺序建议：**第二部分（56分，理论+计算，需理解，先啃）→ 第三部分（55分，实务+案例）→ 第一部分（29分，法条记忆，最后背）**。理由同经济基础：理解型先学、纯记忆型后学。


### 1.2 用户痛点

备考中级经济师的人群有一个共性：**时间极度稀缺，且失败成本高**。他们大多是在职人员，为了职称、落户、积分、晋升而考，每天能挤出的整块学习时间通常不超过 90 分钟。

| #  | 痛点          | 具体表现                       | 痛感强度  | 现有方案为何失效                                           |
| -- | ----------- | -------------------------- | ----- | -------------------------------------------------- |
| P1 | **学了就忘**    | "第三章看完，回头做第一章的题全错了"        | ★★★★★ | 视频课/教材是线性输入，没有复习调度；人的遗忘曲线在 24 小时内掉 70%，App 却从不安排复习 |
| P2 | **刷题无效**    | "刷了 3000 题，模考还是 70 分"      | ★★★★★ | 只做题不复盘，错题不重做；题目顺序固定导致"背答案"而非"会做题"                  |
| P3 | **不知道该学什么** | "还剩 80 天，六本书，从哪开始？"        | ★★★★☆ | 通用学习计划表不分人群；没有基于个人掌握度的动态重排                         |
| P4 | **机考陌生**    | "平时纸质做题挺好，一上机就慌，时间不够"      | ★★★★☆ | 绝大多数 App 的模考界面与真实机考完全不同，练了等于没练                     |
| P5 | **多选恐惧**    | "多选一错就是 0 分，不敢选又拿不到分"      | ★★★★☆ | 市面产品只给答案，不给"少选/激进"的策略建模与期望值测算                      |
| P6 | **没人答疑**    | "这题解析看不懂，问谁？"              | ★★★☆☆ | 录播课无交互；社群答疑响应慢；真人答疑贵                               |
| P7 | **资料杂乱**    | "网盘几十 G 资料、PDF、截图、笔记，怎么用？" | ★★★☆☆ | 没有工具把个人资料转成可练的题目                                   |
| P8 | **坚持不下来**   | "学了 3 天就断了，然后一直断"          | ★★★★☆ | 缺乏有效的进度反馈与中断挽回机制                                   |
| P9 | **二战焦虑**    | "去年基础过了实务挂了，今年只剩一科，怎么保？"   | ★★★☆☆ | 产品默认"两科一起学"，二战用户体验割裂                               |

### 1.3 不解决这个问题的代价

- **用户侧**：一次失败 = 一年职业机会成本（职称评定/积分落户窗口期），且成绩滚动周期只有 2 年，连续失败意味着从头再来。
- **产品侧**：备考 App 是**高意图、短周期、强结果导向**的品类，用户付费意愿高但容忍度极低——考不过就卸载且给差评。这意味着产品的唯一北极星必须是**真实通过率**，而不是 DAU 或时长（详见 §13）。
- **市场侧**：这是一个每年百万级考生的稳定市场，但目前头部产品停留在"视频+题库"的上一代形态，AI 原生 + 学习科学原生的产品存在明确的代际机会。

---

## 2. 目标（Goals）

### 2.1 用户目标

| #  | 目标           | 衡量方式                            | 目标值                          |
| -- | ------------ | ------------------------------- | ---------------------------- |
| G1 | 用尽可能少的时间通过考试 | **活跃学习用户**两科同时通过率（不再区分付费与否，v1.2 调整） | ≥ 68%（行业公开宣称值约 30-45%，需实测校准） |
| G2 | 消除"学了就忘"     | 学后 7 天知识点\** retention rate\*\* | ≥ 75%（对照组：纯刷题约 35-40%）       |
| G3 | 消除考场陌生感      | 模考平均分与真实考试成绩**偏差**              | ±6 分以内                       |
| G4 | 让计划可执行、可坚持   | 计划**完成率**（完成日 / 计划活跃日）          | ≥ 65%；连续学习 ≥ 21 天用户占比 ≥ 40%  |
| G5 | 让 AI 真的有用且可信 | AI 内容**争议率**（被用户点"有疑问"的题目占比）    | < 3%                         |
| G6 | **不因商业化伤害体验**（v1.2 新增） | 广告相关差评占比 / 因广告卸载率            | < 5% / < 1%                  |

### 2.2 业务目标

| #  | 目标                     | 目标值                                        |
| -- | ---------------------- | ------------------------------------------ |
| B1 | **广告变现效率**（v1.2 替换原"付费转化率"） | ARPDAU ≥ ¥0.11（日活人均日收入），详见 M9-F10        |
| B2 | **去广告购买率**             | 累计 3 个月内 ≥ 3%（免费 App 工具类参考值）              |
| B3 | 用户规模（广告模式的根本杠杆）      | 12 个月内 MAU ≥ 100 万（**免费降低门槛是核心增长动力**）      |
| B4 | 单科/全科通过用户的**口碑裂变**     | NPS ≥ 45，通过用户推荐率 ≥ 30%                     |
| B5 | 单位服务成本下降               | AI 承担答疑工作，人工教研介入率 < 15%；**AI 服务成本恒为 ¥0**（纯 BYOK，无额度成本项） |
| B6 | 用户生命周期延伸               | 考后用户留存率（转职称继续教育/初级/高级课程）≥ 15%              |

> **为什么广告模式下"用户规模"的权重远高于买断制**：广告收入的公式是 `MAU × 活跃率 × 人均广告次数 × eCPM`。买断制下我们可以只服务 10 万个高付费意愿用户；广告模式下，我们必须服务 100 万个用户才能获得同等甚至更高的收入。**这意味着产品决策的天平必须向"更低的门槛、更广的适配、更强的口碑"倾斜**——而"核心能力全免费"正是为此服务的设计。

### 2.3 北极星指标（North Star Metric）

> **NSM = 周活跃「有效学习用户」数**  
> 定义：一周内完成 ≥ 4 天、且**包含至少 20 题主动回忆练习**（非纯看课）的用户数。

**为什么不用 DAU/时长**：备考 App 的陷阱是"虚假繁荣"——用户刷视频 2 小时，感觉努力了，其实零提取练习。NSM 强制绑定"有效学习行为"，才能让团队所有决策对齐真实通过率。

---


## 3. 非目标（Non-Goals）

明确写下**不做什么**，防止工程过度实现与范围蔓延。

| #        | 非目标                                           | 为什么不做                                                                                                         |
| -------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| NG1      | **不做视频大课**                                    | 视频是被动输入，与本产品"主动回忆"核心主张冲突；且带宽/存储/教研成本极高。改为"3-5 分钟微课 + 图文精讲"，只在用户卡壳时按需推送。理由见 §5 测试效应。                           |
| NG2      | **不做直播课 / 真人答疑社区**                            | v1 阶段边际成本不可控，且与 AI 私教定位重叠。v2 再评估"AI 优先 + 人工兜底"的轻答疑。                                                           |
| NG3      | **不做"押题""保过""命中率"承诺**                         | 合规红线。涉及《广告法》第二十四条（教育/培训广告不得对通过率作保证性承诺）。改为"高频考点预测"，且必须带统计学口径说明与免责声明。                                           |
| NG4      | **不做社交/IM/论坛**                                | 社交是时间黑洞，与备考"节省时间"的核心价值相悖。仅做轻量的"学习搭子打卡"（匿名、无 IM），且默认关闭。                                                        |
| NG5      | **不做除《经济基础知识》《人力资源管理》外的其他 8 个专业，也不做初/高级经济师等** | 聚焦两科，把内容做深做透。但**数据模型必须保留多科目扩展能力**（见 §11），避免后续加专业时重构。                                                          |
| NG6      | **不做 Web 端**                                  | 本地优先架构下，Web 端无法与主端共享数据（无云端同步），跨端只能靠备份文件迁移，体验割裂。专注于把移动端做到极致。                                                   |
| NG7      | **不自建大模型，不搭建应用服务端**                           | 采用第三方 LLM API，客户端直连（见 §8.8）。**无应用服务器**是本地化架构的核心约束。                                                            |
| NG8      | **不做离线视频下载**                                  | 同 NG1（本来就不做视频课）。                                                                                              |
| **NG9**  | **🔒 不做账号系统、不做云端同步、不做多设备实时同步**                | 本地优先架构的**核心决策**。带来的好处：隐私（数据永不上传）、零服务成本、完全离线可用。代价：数据备份责任在用户侧 → 因此 **M10 备份/恢复升级为 P0 功能**。跨设备迁移通过「备份文件导出/导入」实现。 |
| **NG10** | **不把用户学习数据用于任何形式的上传或分析**                      | 隐私承诺。所有学习记录、笔记、上传资料仅存本机。**埋点也必须是"本地统计 + 用户主动上报"模式**（见 §13.4）。**广告 SDK 不得访问任何业务数据**（M9-F11）。 |
| **NG11** | **v1.2 新增：不做插屏广告、不在学习流程中投放任何广告**              | 见 M9-F1 三条铁律与 §5.3。**v1.3 修订**：开屏广告经评估后允许（占用等待时间而非学习时间，且受 M9-F3 位置⓪ 的 10 条硬约束），但**插屏广告与学习流程广告依然绝对禁止**。 |
| **NG12** | **v1.2 新增：不卖学习结果，不把核心能力锁在付费墙后**             | 见 M9-F1 T3。题库、模考、复盘、复习调度、计划引擎、过线预测**全部免费**。付费只能买"便利"（去广告），不能买"分数"。 |
| **NG13** | **v1.3 新增 / v1.5 定稿：不做 AI 额度生意，不经手任何 AI 请求**            | 见 M9-F7。**v1.3 原表述为"不做 AI 中间商，不经手请求"；v1.4 曾窄口径修订为"只做技术通道（保留无状态转发层）"；v1.5 撤销该修订，回归 v1.3 原始口径并强化。**<br>**红线（不可越）**：① **不提供任何形式的 AI 转发/代理/中转通道**——不做无状态转发层，也不做任何"代用户调用"的服务；② **不经手、不接收、不存储用户的 AI 请求与响应**（请求由设备直连服务商）；③ 不做 **AI 额度体系**（无"平台额度"这一概念）；④ 不把 AI 当**盈利业务**（不转售、不返利分成、不捆绑付费）；⑤ 不做用户级 AI 行为画像；⑥ 不为模型输出的准确性背书。 |
| **NG14** | **v1.4 新增：不做需要用户账号的 AI 会员/订阅制**                       | 与 NG9 配套。**不得**引入"登录才能用 AI""开通会员解锁 AI"这类设计——那会瞬间把无账号架构的优势全部作废。**v1.5 强化**：由于已无平台额度体系（NG13），本条的适用面进一步扩大——**AI 的任何能力都不得与付费、账号、等级挂钩**。 |
| **NG15** | **v1.5 新增：不做任何需要我方服务器在线的功能**                          | 这是 v1.5 的**架构总闸**，覆盖 AI 之外的未来功能提案。判定标准：**若某个功能在我方服务器下线后会失效，则不允许做**。<br>逐条对照：广告 SDK（✅ 第三方服务，非我方服务器，且失败不影响学习功能）；平台 IAP（✅ 无状态收据校验）；备份文件（✅ 用户自己存本地/网盘）；AI（✅ 设备直连服务商）。<br>**唯一例外**：应用商店分发与更新（由平台承担，不属于我方运维）。 |
| **NG16** | **v1.5 新增：不把 AI 当作产品的可靠承诺**                              | 因为无中转、无额度兜底，AI 对相当比例的用户**必然不可用**（不愿配置 Key）。因此：① 任何宣传物料、商店描述、App 内引导**均不得**把 AI 作为核心卖点或功能承诺；② 所有 AI 入口文案必须带"可选增强"定性；③ **AI 不可用不得阻断任何学习路径**（技术硬约束：AI 相关异常必须被兜住，绝不可冒泡到页面级错误）。 |

---


## 4. 用户画像（Personas）

> **v1.1 范围说明**：当前版本仅支持《经济基础知识》+《人力资源管理》。下表中报考其他专业（工商管理、金融、财政税收等）的画像，在本期产品中会通过 onboarding 的"专业选择"步骤被**引导至等待列表**（收集邮箱/推送预约，为后续版本蓄水）。画像本身保留，用于指导后续版本的专业扩展优先级。

|            | **张明 · 零基础在职型**      | **李婷 · 二战补弱型**      | **王姐 · 时间碎片型**        | **陈工 · 专业对口型**               | **刘同学 · 极限冲刺型**    |
| ---------- | -------------------- | ------------------- | --------------------- | ---------------------------- | ------------------ |
| 年龄/职业      | 32 岁 / 制造业主管         | 28 岁 / 行政           | 40 岁 / 全职宝妈           | 35 岁 / HR 经理                 | 26 岁 / 应届考公党       |
| 报考专业       | 工商管理                 | 人力资源管理              | 财政税收                  | 人力资源管理                       | 金融                 |
| 备考状态       | 剩 5 个月，零基础           | 去年基础过（92分），实务挂（79分） | 剩 4 个月，每天 3 段碎片时间     | 剩 3 个月，实务有底子                 | 剩 60 天             |
| 可用时间       | 工作日 60-90 分钟，周末 3 小时 | 工作日 45 分钟           | 每段 15-25 分钟，共 3-4 段   | 工作日 40 分钟                    | 每天 3-4 小时          |
| 核心诉求       | 有人告诉我每天该干嘛           | 只补弱的，别让我重学一遍        | 把一个知识点塞进 15 分钟        | 跳过我会的                        | 60 天极限提分，性价比优先     |
| 最大恐惧       | 学了后面忘了前面             | 今年再挂，成绩作废           | 坚持不下去                 | 浪费时间在已会内容上                   | 时间不够，抓错重点          |
| **产品必须做到** | 强计划驱动 + 复习自动调度       | **单科模式 + 去年成绩档案继承** | 任务原子化（≤15 分钟一包）+ 极简入口 | **前置自测免学**（掌握度 ≥80% 的章节自动跳过） | 冲刺计划 + 二八法则砍掉低权重章节 |
| 主要活跃功能     | 今日计划、章节学练、知识图谱       | 薄弱点诊断、专项训练          | 每日 15 分钟包、记忆卡片        | 摸底测评、跳学、错题                   | 冲刺计划、考点预测、模考       |

**反画像（Anti-Persona，明确不服务）**：囤资料不学的"资料收藏家"、只想要"押题密卷"走捷径的用户、以及非中级经济师考试人群——他们的需求会污染产品方向。

---

## 5. 设计原则：学习科学 → 功能映射

> 这是本产品的**方法论地基**。每一条原理都必须落到具体功能，否则就是装饰品。工程和设计评审时，任何需求都要能回答"它对应哪条原理？"


### 5.1 学习科学原理映射表

| 学习科学原理                             | 核心发现（证据）                                            | 本产品如何落地                                                                 |
| ---------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------------- |
| **主动回忆 Active Recall**             | 提取练习的长期保持效果是重复阅读的 2-3 倍（Roediger & Karpicke, 2006）  | **默认交互是"先答题，后看解析"**。禁止"边看答案边做题"模式。知识点卡片一律先出背面问题，用户自评后再翻正面               |
| **间隔重复 Spaced Repetition**         | 在遗忘临界点复习，记忆巩固效率最高（艾宾浩斯遗忘曲线）                         | 采用 **FSRS-6 算法**（比 SM-2 更精准的现代调度器）驱动"今日复习"队列。复习到期量是**硬约束**，不可跳过         |
| **测试效应 Testing Effect**            | 测试本身即学习事件，且测试比额外学习更能提升长期记忆                          | 每个知识点学完后**强制 3 题即时测**；章节完成后**强制章节测**，通过才可推进（不通过则触发"回炉"）                 |
| **交错练习 Interleaving**              | 混合不同主题的练习，长期保持率与迁移能力显著优于分块练习（Rohrer & Taylor, 2007） | 章节学完 24 小时后，该章节题目**自动混入**跨章节练习池。复习队列默认打乱章节顺序                            |
| **间隔效应 Lag Effect**                | 分布式练习优于集中练习                                         | 错题重做采用**延迟调度**（当日→次日→3日→7日→15日），而非"错一次立刻重做"                             |
| **认知负荷理论 Cognitive Load**          | 工作记忆容量有限（约 4±1 组块，Cowan 2001），外在认知负荷会挤占学习           | ① 单屏只呈现一道题；② 长题干默认折叠案例材料，可展开；③ 解析默认收起，点击才展开；④ 新知识点一次最多推 7 个（Miller 7±2） |
| **精细加工 Elaborative Interrogation** | 追问"为什么是这样"能显著提升理解深度                                 | AI 助教在用户答错时，不直接给答案，而是**苏格拉底式追问**："你认为 A 和 C 的区别在哪？"                     |
| **具体实例 Concrete Examples**         | 抽象概念配具体实例可大幅降低理解门槛                                  | 每个抽象考点 AI 自动生成**生活化类比**（如"货币政策工具→洗澡水阀门"）                                |
| **双重编码 Dual Coding**               | 语言系统与非语言系统并行加工，记忆效果叠加                               | 复杂考点自动生成**思维导图 / 对比表格 / 流程图**（AI 生成 + 教研审核）                             |
| **元认知校准 Metacognition**            | 学习者对"自己会不会"的判断准确性，直接影响学习效率                          | **答题前先做"确信度自评"**（很确定/有点懵/完全没思路）。系统据此识别"真会"与"蒙对"，并统计用户的**元认知校准偏差**       |
| **最近发展区 ZPD / 刻意练习**               | 在"略微超出当前能力"的区域练习效果最佳                                | 自适应难度引擎：目标正确率锚定 **85%**（研究显示此为最优学习区），正确率 >92% 自动升难度，<70% 自动降难度          |
| **检索失败后再学习 Retrieval Failure**     | 先尝试回忆（即使失败），再学习，效果优于直接学习                            | 知识点学习前先出"预测试题"，即使答错也显著提升后续学习效果                                          |


### 5.2 UI / 交互科学映射表

| 原理                               | 本产品如何落地                                                                          |
| -------------------------------- | -------------------------------------------------------------------------------- |
| **费茨定律 Fitts's Law**             | 主行动按钮（开始学习/下一题）置于**拇指热区**（屏幕下半部）；选项整行可点击（高度 ≥56px），而非只有小圆点可点                     |
| **希克定律 Hick's Law**              | 首页决策项 ≤ 3 个（今日主任务 / 继续上次 / 自由练习）；底部 Tab 固定 5 个，不做折叠菜单                            |
| **米勒定律 7±2**                     | 一次学习单元的新知识点 ≤ 7 个；底部导航 ≤ 5 项；单屏信息块 ≤ 4 个                                         |
| **格式塔邻近性/相似性**                   | 题目-选项-解析用间距与卡片分组，而非仅靠颜色；同类信息视觉权重一致                                               |
| **峰终定律 Peak-End Rule**           | 每次学习结束时强制展示**本轮成果卡**（答对 X 题 / 掌握 Y 个考点 / 连续 Z 天），用正向峰值收尾                         |
| **蔡格尼克效应 Zeigarnik Effect**      | 首页常驻"今日进度环"，未完成时有视觉张力；计划中断时推送温和提醒（非骚扰式）                                          |
| **渐进式披露 Progressive Disclosure** | 解析、提示、举一反三默认收起，按需展开。首屏只给最小决策集                                                    |
| **一致性（雅各布定律）**                   | 遵循 iOS HIG / Material You 标准交互；不发明新手势                                            |
| **容错性**                          | 所有破坏性操作（重置计划、清空错题本、交卷）二次确认；答题提供"撤销上一次选择"                                         |
| **无障碍**                          | 对错判定**绝不仅靠颜色**（红/绿对色盲不可辨），必须叠加图标 ✓/✗ 与文字；支持系统字号 200% 缩放不破版；支持 TalkBack/VoiceOver |

### 5.3 反暗黑模式原则（Anti-Dark-Pattern Commitment）

备考产品的伦理底线。以下做法**明确禁止**：

- ❌ 用"连续打卡中断清零"制造焦虑绑架（改为：中断后提供**补卡**机制，每周 1 次免费补卡）
- ❌ 用虚假稀缺（"仅剩 3 个名额"）逼单
- ❌ 诱导性弹窗打断学习流（学习过程中**零营销弹窗**，这是硬性约束）
- ❌ 隐藏取消订阅入口（订阅管理必须在"我的"一级页面可见）
- ✅ 允许：真实的成就反馈、真实的数据可视化、基于学习科学的温和提醒

**v1.2 广告模式追加的禁止项**（对应 M9-F1 三条铁律）：

- ❌ **学习流程中的任何广告**（答题、解析、复习队列、模考、AI 对话、资料解析、onboarding）——**零例外**
- ❌ **被动打扰型广告**：自动播放、进页面即加载、倒计时强制播放、**插屏广告**
- ⚠️ **开屏广告（v1.3 允许，但受 10 条硬约束）**：必须 ≤5 秒、2 秒后可跳过、跳过按钮 ≥44×44pt、**不做"跳过按钮藏得很小"的把戏**、点击跳过立即进入、不遮挡任何功能、冷启动才展示（热启动不展示）、离线直接跳过、**不得有"再确认一次"的二次拦截**
- ❌ **用广告惩罚不付费用户**（如"不看广告就不给复习"）——不看广告的唯一后果是"没有额外奖励"
- ❌ **模糊的奖励承诺**（"解锁神秘奖励"）——激励视频必须**明确告知时长与具体奖励内容**
- ❌ **购买后仍展示广告**（含"你能看到这条广告是因为……"之类的次级骚扰）
- ❌ **用焦虑话术推销去广告**（"别的考生都买了纯净版，你还在忍受广告？"）
- ❌ **广告的出现与消失不可预期**（v1.3 明确禁止"有→无→有"的突变，包括"考前停广告"这类看似善意的设计）——见 M9-F4
- ✅ 允许：开屏广告（受上述硬约束）、用户主动触发的激励视频、坦率说明商业模式的引导文案

---

## 6. 上岸计划引擎：最科学的备考路径

> 本章回答"最科学的学习上岸计划"具体是什么。这不是一张通用时间表，而是**一套基于分值权重、遗忘曲线、个人掌握度和剩余天数的动态算法**。

### 6.1 计划生成算法总览

```
输入：
  - 考试日期 D_exam、今日 D_today → 剩余天数 T = D_exam - D_today
  - 报考科目（基础 / 实务 / 两科）+ 所选专业
  - 每日可用时长（用户申报 + 系统实测校准）H_daily
  - 初始掌握度画像（来自 30 题摸底测评，未测评则按"零基础保守值"初始化）

输出：
  - 阶段划分与时间表
  - 每日任务包（复习到期量 + 新学量 + 错题歼灭量 + 模考量）
  - 每周里程碑与检查点
  - 动态重排规则
```


### 6.2 四阶段模型（以 T=180 天两科为例）

| 阶段          | 时间窗              | 目标         | 核心动作                              | 时间分配比例                   |
| ----------- | ---------------- | ---------- | --------------------------------- | ------------------------ |
| **P0 诊断期**  | D1-D3（3 天）       | 建立掌握度基线    | 30 题分模块摸底测评 + 学习目标设定 + 可用时间申报     | 测评 100%                  |
| **P1 基础构建** | D4-D77（约 10.5 周） | 全覆盖，建立知识框架 | 按权重顺序学模块 → 每节即时测 → 每章章节测 → 复习队列   | 新学 60% / 复习 30% / 测试 10% |
| **P2 强化突破** | D78-D132（约 8 周）  | 消灭薄弱点，真题精研 | 专项训练 + 近 5 年真题（按知识点刷，非按年份刷）+ 错题歼灭 | 复习 30% / 专项 40% / 真题 30% |
| **P3 冲刺模考** | D133-D172（约 6 周） | 全真演练，适应机考  | 每周 1 套全真模考（含连考模式）+ AI 复盘 + 高频考点回炉 | 模考 35% / 回炉 45% / 复习 20% |
| **P4 考前稳定** | D173-D180（8 天）   | 轻负载，稳状态    | 每日轻量复习 + 1 套最终模考 + 机考操作演练 + 作息调整  | 复习 70% / 轻量测 30%         |

**分档策略**——不是所有人都从 T-180 开始，系统按剩余天数自动切换计划模板：

| 剩余天数         | 计划模板    | 策略要点                                        |
| ------------ | ------- | ------------------------------------------- |
| T ≥ 150      | 完整版（上表） | 全覆盖 + 深度                                    |
| 90 ≤ T < 150 | 标准版     | 压缩 P1，模块全覆盖但深度降级（跳过低频考点）                    |
| 45 ≤ T < 90  | **冲刺版** | 砍掉低频低权重考点，只覆盖"覆盖 84 分所需的 65% 高频考点"；P1/P2 合并 |
| T < 45       | **极限版** | 只做"高频考点 + 真题 + 模考"，明确告知用户风险与取舍（诚实 > 虚假承诺）   |

### 6.3 模块学习顺序：为什么是这个顺序

**排序逻辑 = 分值权重（0.4）× 学习性价比（0.3）× 遗忘敏感度（0.3）**

| 顺序 | 模块        | 排序理由                                                                  |
| -- | --------- | --------------------------------------------------------------------- |
| 1  | **经济学基础** | 分值最高（29-30分），且是所有其他模块的理解底座。**先啃最难的骨头，因为此时精力最充沛、时间最充裕**。放在后面学会因时间不足而崩盘 |
| 2  | **货币与金融** | 分值 22-23 分，贴近生活、理解难度低。**早期快速提分建立信心**，对抗 P8 痛点（坚持不下来）                  |
| 3  | **财政**    | 23-25 分，但考点细碎、记忆量大。放在中期，配合"对比记忆 + 口诀"，且此时用户已建立学习习惯                    |
| 4  | **统计**    | 公式为主、套路固定，短期可突破                                                       |
| 5  | **会计**    | 需要一定理解成本                                                              |
| 6  | **法律**    | 纯记忆型，**遗忘速度最快**（艾宾浩斯：无意义音节遗忘最快）。放在临考前，使"记忆→考试"的间隔最短，遗忘损失最小            |

> **这个顺序本身就是一个 demo 卖点**：在 onboarding 结束时展示"AI 为你生成的 180 天作战地图"，并逐条解释排序理由。用户第一次感受到"这个 App 真的懂考试"。

**实务科目**：专业不同，权重模型不同，采用"教材章节 + 近 5 年真题频次统计"动态生成顺序，不写死。

### 6.4 每日任务包算法

```
每日总容量 C = H_daily（分钟） × 效率系数 η（默认 0.85，根据用户历史实际完成率校准）

任务包构成（按优先级严格分配）：
  1️⃣ 复习到期量 R（硬约束，不可跳过）
     R = FSRS 调度队列中 due_date <= today 的条目数
     单位耗时：记忆卡片 20 秒 / 练习题 35 秒
     ⚠️ 若 R 耗时 > C × 0.5，触发"复习超载"：
        → 按"掌握度低 + 分值权重高 + 临近遗忘临界"排序，超出部分顺延
        → 同时冻结当日新学任务（宁可不学新的，也不能欠复习的债）
        → 首页明确告知："今天复习量偏大，已为你暂停新学。复习债不还，学了也白学。"

  2️⃣ 新学任务 N（弹性）
     N = min(剩余容量 × 0.6, 单日新学上限)
     单日新学上限 = 7 个知识点（Miller 7±2）或 1.5 个章节，取小值

  3️⃣ 错题歼灭 W（弹性）
     W = min(剩余容量 × 0.4, 错题池到期条目)

  4️⃣ 模考 M（按计划节点触发，非每日）

任务包原子化：每个任务包标注预计耗时（如"约 12 分钟"），支持单包独立完成与打卡。
碎片时间用户（王姐画像）：系统自动将任务包拆分为 ≤15 分钟的原子单元。
```

### 6.5 「84 分手账」得分策略模型（核心差异化功能）

> 84 分 = 60% 正确率。这意味着**你不需要全会，你需要会的地方全对**。本模块把这句话变成可计算、可训练、可追踪的策略。

**A. 目标分解计算器**

用户输入目标分（默认建议 100 分，留出 16 分安全垫），系统反推所需表现：

```
目标分 G = 单选得分 + 多选得分 + 案例得分

《经济基础知识》（目标 100 分）示例：
  单选 70 题 × 正确率 a × 1 分
  多选 35 题 × 得分率 b × 2 分
  → 推荐配置：a = 85%（59.5 分），b = 58%（40.6 分）→ 合计 100.1 分
  → 系统给出 3 套可选配置：保守型 / 均衡型 / 激进型，用户自选
```

**B. 多选题策略模拟器（基于 R1 少选给分规则）**

这是产品的"啊哈时刻"功能。系统基于用户历史答题数据，测算两种策略的期望得分：

| 策略      | 行为            | 期望得分模型                 | 适用人群            |
| ------- | ------------- | ---------------------- | --------------- |
| **保守型** | 只选 100% 确定的选项 | Σ P(选中项全对) × 0.5 × 选中数 | 多选得分率 < 50% 的用户 |
| **均衡型** | 确定项必选 + 高概率项选 | 混合模型                   | 得分率 50-65%      |
| **激进型** | 选满所有倾向项       | Σ P(全对) × 2 分          | 得分率 > 65%       |

系统每周用用户真实数据重算一次，并给出建议："**数据显示你采用保守策略的期望得分比激进策略高 8.3 分，建议保持。**"

**C. 训练落地**：在练习中提供「策略模式」开关——开启后，多选题提交前强制提示"你有 2 个选项把握度低，确定要选吗？（选错整题 0 分）"。

### 6.6 计划动态调整规则

| 触发条件                 | 系统响应                                                       |
| -------------------- | ---------------------------------------------------------- |
| 连续 3 天未完成计划          | **不指责**。主动降档：每日容量下调 20%，重新生成计划，并推送："最近很忙？我把计划调松了，先保住复习不断档" |
| 连续 7 天超额完成 120%      | 主动加档，并提示"状态很好，可以提前进入下一阶段"                                  |
| 模考成绩连续 2 次下滑         | 触发"回炉"：自动将该模块标记为薄弱，插入专项训练                                  |
| 章节测正确率 < 60%         | 阻断推进，强制回看该章节精讲 + 重做错题                                      |
| 章节测正确率 ≥ 90% 且确信度自评高 | 允许"跳学"（陈工画像刚需），直接标记章节完成                                    |
| 距考试 < 15 天           | 自动切换为 P4 稳定期，停止新学，只做复习与模考                                  |
| 长期未登录（≥7 天）          | 回归时**不显示愧疚文案**，显示"我们重新算一下，还来得及"并立即给出压缩后的可行计划               |

---

## 7. 功能规格（Functional Requirements）

### 7.0 功能地图与信息架构

```
底部 Tab（5 个，固定，不折叠）
├── 📘 学习      → 今日计划 / 上岸地图 / 知识图谱 / 精讲
├── 📝 题库      → 章节练 / 专项练 / 真题 / 错题本 / 收藏 / 自建题库
├── ✨ AI        → AI 私教对话 / AI 出题 / AI 解析 / AI 诊断 / 考点预测
├── 🎯 模考      → 全真模考 / 模考历史 / AI 复盘报告
└── 👤 我的      → 数据看板 / 我的题库 / 奖励中心 / 购买状态 / 设置
```

> **v1.2 说明（v1.3 补充）**：「我的 → 订阅」已改为「**奖励中心**」——本产品无订阅，该入口承担两个职能：① **用户主动**领取看广告换额度的奖励（M9-F3 位置②）；② 查看购买状态与恢复购买（M9-F6）。**入口本身不含任何广告，只有用户点进去才看得到可选项。**
>
> ⚠️ **v1.5 修订**：本产品的 **AI 接入配置**（服务商选择 / API Key）**不在此处配置**，而在「设置 → AI 服务设置」（M9-F7）。**v1.5 起 AI 为纯 BYOK、无额度体系，因此奖励中心不再承担"看广告换 AI 次数"的职能**（该广告位已随中转方案一并撤销，见 M9-F3）。奖励中心现在只负责：购买状态查看与恢复购买。

**模块清单与优先级**

| 编号  | 模块               | 优先级 | 依赖         |
| --- | ---------------- | --- | ---------- |
| M1  | 上岸计划引擎           | P0  | M2, M3     |
| M2  | 知识图谱与精讲          | P0  | —          |
| M3  | 智能刷题引擎           | P0  | M2         |
| M4  | 错题本与薄弱点诊断        | P0  | M3         |
| M5  | 全真机考模考           | P0  | M3         |
| M6  | AI 私教矩阵          | P0  | M2, M3, M7 |
| M7  | 自建题库（上传 + AI 生成） | P0  | M6         |
| M8  | 数据看板与过线预测        | P1  | M1-M5      |
| M9  | 商业化（广告 + 去广告）    | P0  | —          |
| M10 | 数据存储、备份与设置       | P0  | —          |
| M11 | 学习搭子打卡（轻量）       | P2  | —          |

---


### M1 上岸计划引擎

#### 功能描述

根据考生剩余天数、每日可用时间、报考科目、初始掌握度，生成并动态维护一份可执行的备考计划。计划的最小单位是可独立完成、可打卡的**任务包**。

#### 用户故事

| #        | 用户故事                                                   | 优先级 |
| -------- | ------------------------------------------------------ | --- |
| US-M1-01 | 作为零基础在职考生，我希望在注册后 3 分钟内得到一份为我定制的 180 天计划，以便我不用自己纠结从哪开始 | P0  |
| US-M1-02 | 作为二战考生，我希望只生成"专业知识和实务"单科计划，以便不浪费时间在已通过的基础科目上           | P0  |
| US-M1-03 | 作为碎片时间用户，我希望任务被拆成 ≤15 分钟的小包，以便我在孩子午睡时也能完成一个            | P0  |
| US-M1-04 | 作为进度落后的用户，我希望计划能自动重排而不是让我从头再来，以便我在有限剩余时间内最大化得分         | P0  |
| US-M1-05 | 作为用户，我希望看到计划中每个阶段的目标和完成标准，以便我知道自己走到哪了                  | P1  |
| US-M1-06 | 作为中断学习的用户，我希望回归时得到一份"还来得及"的新计划而不是愧疚感，以便我能重新开始          | P0  |

#### 详细需求

**M1-F1 计划初始化向导（Onboarding）**

1. 引导流程共 **5 步，总耗时 ≤ 90 秒**，每屏一个问题（希克定律）：
   - S1：报考科目（两科 / 仅基础 / 仅实务）+ 专业选择（10 选 1，带"不确定？测一测"入口）
   - S2：考试日期选择（默认 2026-11-07，支持自定义）
   - S3：每日可用时长（滑块：15/30/45/60/90/120+ 分钟；支持"工作日/周末不同"）
   - S4：学习时段偏好（早起型/通勤型/午休型/睡前型）→ 决定推送时间
   - S5：摸底测评（30 题，约 12 分钟，**可跳过**，跳过则按零基础保守初始化并标注"未测评，计划精度较低"）
2. 生成完成后展&#x793A;**「我的作战地图」**：阶段甘特图 + 模块学习顺序 + 排序理由 + 预计考试日达标概率。支持分享为图片（裂变入口）。

**M1-F2 每日任务包**

1. 首页顶部为「今日」卡片，包含：日期、进度环（完成/总数）、剩余预计时长。
2. 任务包按类型分组展示：**复习（优先级最高，视觉置顶且有警示色）→ 新学 → 错题 → 测试**。
3. 每个任务包展示：类型图标、标题（如"复习：财政政策工具"）、题量、预计耗时、状态。
4. 任务包可独立完成与打卡；完成时有**微动效 + 进度环填充**（峰终定律）。
5. 任务包支持"换一批"（复习包除外，复习是硬约束）。

**M1-F3 计划动态重排**

1. 每日 04:00 服务端重算次日任务包。
2. 本地检测：当用户完成任务量偏离计划 ±30% 时，实时提示是否调整计划（用户确认后重排）。
3. 触发规则见 §6.6。

**M1-F4 阶段检查点**

1. 每阶段结束时生成「阶段报告」：完成率、掌握度变化、模考成绩趋势、下一阶段重点。
2. 若阶段检查点未达标（如模块覆盖率 < 80%），系统提供两个选项：**延长本阶段（顺延后续）** 或 **接受风险继续推进**（明确告知风险）。

#### 验收标准（部分示例）

- [ ] Given 用户选择"仅实务"且去年基础合格，When 完成 onboarding，Then 计划中不含任何《经济基础知识》任务，且首页显示"距考试 X 天 · 单科冲刺"
- [ ] Given 复习到期量耗时超过当日容量 50%，When 生成任务包，Then 新学任务数为 0，且首页显示复习超载提示
- [ ] Given 用户连续 3 天完成率 < 50%，When 第 4 天生成计划，Then 每日容量自动下调 20%，且不出现指责性文案
- [ ] Given 用户在飞行模式，When 打开首页，Then 已缓存的今日任务包可正常查看与答题，答题结果本地暂存

---


### M2 知识图谱与精讲

#### 功能描述

将考纲结构化为**三级知识树**（模块 → 章 → 节/知识点），每个知识点关联精讲内容、题目、掌握度状态。知识树是全部学习行为的组织骨架与数据索引。

#### 用户故事

| #        | 用户故事                                     | 优先级 |
| -------- | ---------------------------------------- | --- |
| US-M2-01 | 作为考生，我希望看到一张覆盖全部考点的知识地图，以便我知道总共要学多少、还剩多少 | P0  |
| US-M2-02 | 作为考生，我希望知识树用颜色标出我的掌握程度，以便我一眼看出哪里是弱项      | P0  |
| US-M2-03 | 作为专业对口考生，我希望对已掌握的章节做自测后直接跳过，以便不浪费时间      | P0  |
| US-M2-04 | 作为考生，我希望精讲内容短小精悍且配实例，以便我在通勤时读完一个知识点      | P0  |
| US-M2-05 | 作为考生，我希望复杂考点有思维导图/对比表，以便我理解易混淆概念         | P1  |

#### 详细需求

**M2-F1 三级知识树**

1. 结构：`科目 → 模块（6个/实务按章节）→ 章 → 知识点`。每个知识点挂载：精讲内容、关联题目、掌握度、近 5 年考查频次、分值权重。
2. 展示形态：可折叠树形列表，**默认展开到"章"层级**，点击展开知识点。
3. 每个节点显示：掌握度色块（5 档：未学/薄弱/一般/良好/掌握，对应灰/红/橙/浅绿/深绿，**同时配图标**避免色盲问题）、题量、分值权重、真题频次。
4. 顶部提供「热力图」切换视图：按模块聚合的掌握度热力矩阵。

**M2-F2 知识点精讲**

1. 单页只讲**一个知识点**，阅读时长 ≤ 3 分钟（约 500-800 字）。
2. 结构固定为五段式（渐进披露，默认展开前三段）：
   ```
   ① 一句话定义（必读）
   ② 核心内容（图文/表格/导图）
   ③ 💡 生活化类比（AI 生成 + 教研审核）
   ④ ⚠️ 易错陷阱（高频失分点）
   ⑤ 🔗 关联考点（点击进入对比学习）
   ```
3. 精讲页底部固定「学完了，测一测（3 题）」按钮（测试效应，强制）。未完成 3 题不允许标记"已学"。
4. 支持**语音朗读**（通勤/闭眼场景，倍速可调）。

**M2-F3 免学跳关（Skip-if-Mastered）**

1. 章节详情页提供「我会了，直接测」入口。
2. 触发 10 题快速测评；正确率 ≥ 90% 且确信度自评"很确定"占比 ≥ 80% → 标记章节为"已掌握"，计划中移除。
3. 正确率 60-90% → 只对错误知识点生成精学任务（**不重学整章**）。
4. 正确率 < 60% → 正常学习，并提示"这个章节对你还有 X 分提升空间"。

**M2-F4 考点权重与频次标注**

1. 每个知识点展示：近 5 年真题考查次数、预估分值、重要度星级（★-★★★★★）。
2. 冲刺期（P3/P4）提供「高频考点清单」筛选器：按"近 5 年考查 ≥ 3 次"筛选，生成考前必刷清单。

---


### M3 智能刷题引擎

#### 功能描述

题库是核心资产。刷题引擎负责"练什么、怎么练、什么时候再练"，由 FSRS-6 调度器 + 自适应难度引擎 + 交错策略共同驱动。

#### 用户故事

| #        | 用户故事                                         | 优先级      |
| -------- | -------------------------------------------- | -------- |
| US-M3-01 | 作为考生，我希望每次打开就能看到"今天该复习的题"，以便不用自己决定练什么        | P0       |
| US-M3-02 | 作为考生，我希望题目难度跟着我的水平走，以便既不无聊也不挫败               | P0       |
| US-M3-03 | 作为考生，我希望提交后立即看到逐项解析，以便马上理解为什么错               | P0       |
| US-M3-04 | 作为考生，我希望在答题前先标注把握度，以便系统知道我是真会还是蒙的            | P0       |
| US-M3-05 | 作为考生，我希望练习时题目被打乱，以便避免背答案                     | P0       |
| US-M3-06 | 作为考生，我希望解析看不懂时可以追问 AI，以便当场搞懂                 | P0       |
| US-M3-07 | 作为有多个来源资料的用户，我希望把自己的 PDF/图片/笔记变成可练的题，以便资料不浪费 | P0（见 M7） |

#### 详细需求

**M3-F1 练习模式矩阵**

| 模式          | 组卷逻辑                                    | 场景       |
| ----------- | --------------------------------------- | -------- |
| **今日复习**    | FSRS 到期队列 + 跨章节打乱                       | 每日必做，硬约束 |
| **章节练习**    | 指定章节，顺序或乱序                              | 学完即练     |
| **专项训练**    | 按知识点/题型/难度/易错标签筛选                       | 补弱       |
| **真题演练**    | 近 5 年真题，**支持按知识点横向刷**（如"刷完所有年份的财政政策真题"） | P2 阶段主力  |
| **错题重做**    | 错题池 + 延迟调度                              | 日常       |
| **高频必刷**    | 近 5 年考查 ≥3 次的考点题目                       | 冲刺期      |
| **AI 智能组卷** | 对话式："给我来 10 道我薄弱的多选题"                   | 见 M6     |
| **挑战模式**    | 限时答题 PK（不联网对战，为自我挑战）                    | P2       |

**M3-F2 答题交互（核心流程，务必严格遵守）**

```
答题页面布局（单屏一题，认知负荷控制）：
┌─────────────────────────────┐
│ ← 3/20        ⏱ 00:42    ⋯ │  ← 顶部：进度/计时/菜单
├─────────────────────────────┤
│ [单选题] 财政政策 · ★★★★    │  ← 题目标签
│                             │
│ 下列属于紧缩性财政政策的是（ │
│ ）。                         │  ← 题干（18px，行高1.7）
│                             │
│ ○ A. 降低税率               │  ← 选项整行可点，高≥56px
│ ○ B. 增加政府支出           │
│ ○ C. 提高存款准备金率       │
│ ○ D. 减少政府购买           │
│                             │
├─────────────────────────────┤
│ 作答前：你有多大把握？       │  ← 确信度自评（先评后答）
│ [很确定] [有点懵] [没思路]  │
├─────────────────────────────┤
│    [ 提交 ]                 │  ← 拇指区主按钮
└─────────────────────────────┘
```

1. **确信度自评前置**：提交前必须/建议先选自评（可关闭，但默认开启）。数据用于：区分"真会"与"蒙对"、计算元认知校准偏差。
2. **单选**：点击整行选中，再点其他行切换。提交后锁定。
3. **多选**：选项前为**方框**（☑，与案例题一致），单选为**圆圈**（⚪）——**与真实机考视觉规范保持一致**。
4. **提交后即时反馈**：正确/错误用 **图标 + 颜色 + 轻微震动**三重反馈；选项行显示 ✓/✗（不依赖颜色）。
5. **解析区**（渐进披露）：默认展示"答案 + 一句话点拨"，点击「查看完整解析」展开逐项解析。
6. **解析页底部操作栏**（固定）：`收藏` `记笔记` `AI 追问` `举一反三（生成2道同类题）` `加入错题本`。
7. **计时**：练习模式默认开启单题计时，超时（基础 51 秒/实务 54 秒）时计时器变黄提醒（不强制跳题）。
8. **禁止行为**：练习模式下**不允许边看答案边做**——必须提交后才展示答案（主动回忆，NG1 原则）。

**M3-F3 FSRS-6 调度器**

1. 采用 FSRS-6（Free Spaced Repetition Scheduler）算法，维护每个**知识点/题目**的记忆状态：难度 D、稳定性 S、可提取性 R。
2. 用户答题后，系统给出**四级评分**（不是"记住/忘记"两档）：`重来 / 困难 / 良好 / 简单`。四级比两级能提供更精细的调度信号。
3. 初始调度：新学知识点默认 1 天后首次复习，之后按算法扩展。
4. 复习到期判定：可提取性 R < 0.9 时进入今日复习队列。
5. \** overdue 保护\*\*：中断 7 天回归时，不对积压复习做惩罚性重置，而是按"最紧急优先"分批释放（避免用户一回来看到 300 道题直接放弃）。

**M3-F4 自适应难度引擎**

1. 难度维度：知识深度（记忆/理解/应用/综合）× 题目复杂度（单考点/多考点/计算/案例）。
2. 目标正确率锚定 **85%**（最近发展区最优）。滚动窗口 = 最近 20 题。
3. 调节规则：正确率 > 92% 连续 2 次 → 难度 +1；正确率 < 70% 连续 2 次 → 难度 -1 且插入同类基础题。
4. 情绪保护：连续答错 3 题时，自动降低难度并插入一道用户已掌握的简单题（防止挫败流失）。

**M3-F5 题目数据结构（关键，决定所有上层能力）**

每道题必须包含的字段（详见 §11 数据模型）：

- 题型（单选/多选/案例）、题干、选项、正确答案、难度、分值权重
- **知识点绑定**（可多绑，支持跨章节题）
- 逐项解析（每个选项独立解析，不只是一句"故选D"）
- **来源分级**（S/A/B/C，见 §8.6 可信度体系）
- 真题年份、考查频次、易错标签
- AI 生成元数据（生成模型、置信度、校验状态）

---

### M4 错题本与薄弱点诊断

#### 用户故事

| #        | 用户故事                                | 优先级 |
| -------- | ----------------------------------- | --- |
| US-M4-01 | 作为考生，我希望错题自动归集并按考点分类，以便我知道自己到底哪里不会  | P0  |
| US-M4-02 | 作为考生，我希望错题按遗忘曲线安排重做，以便真正记住而不是背答案    | P0  |
| US-M4-03 | 作为考生，我希望看到错题的"错误原因分析"，以便区分是没记住还是没理解 | P0  |
| US-M4-04 | 作为考生，我希望掌握后能把错题移出错题本，以便看到自己的进步      | P0  |

#### 详细需求

**M4-F1 错题归集**

1. 自动入池：练习答错、模考答错自动入池。**蒙对的题（确信度"没思路"但答对）也入池**，标记为"侥幸答对"。
2. 错题按**知识点聚合**（不是按时间堆），展示：该知识点下错题数、最近错误时间、掌握度。
3. 筛选：按科目/模块/题型/错误原因/入池时间。

**M4-F2 错误归因（AI 辅助，见 M6）**  
每题答错后，系统引导归因（一键选择，≤3 秒完成）：

- `概念没记住` → 推送记忆卡片 + 加入间隔重复
- `概念混淆了` → 推送对比表 + 生成辨析题
- `题意没看清` → 推送审题训练（关键词高亮练习）
- `计算失误` → 推送同类计算题
- `完全没思路` → 推送知识点精讲 + 降难度题

归因结果进入个人学情画像，用于 AI 诊断与计划调整。

**M4-F3 错题重做调度**  
延迟调度：当日（订正）→ 次日 → 3 日 → 7 日 → 15 日 → 30 日。任一环节答对则推进到下一间隔，答错则退回上一级。连续 3 次答对 → 自动移出，标记为"已攻克"，并给予正向反馈。

**M4-F4 攻克进度可视化**  
错题本顶部展示：`累计错题 X 道 · 已攻克 Y 道（Z%）`，配进度条。这是用户最能感知的"我在变强"信号。

---


### M5 全真机考模考（核心差异化）

> **这是本产品与市面上所有经济师 App 拉开差距的地方。** 1:1 还原中国人事考试网机考系统，练的就是考场。

#### 用户故事

| #        | 用户故事                                       | 优先级 |
| -------- | ------------------------------------------ | --- |
| US-M5-01 | 作为考生，我希望模考界面和真实机考一模一样，以便上考场不陌生             | P0  |
| US-M5-02 | 作为考生，我希望模考还原"按题型分段锁定"的规则，以便我训练一次性准确率       | P0  |
| US-M5-03 | 作为两科连考的考生，我希望体验连考节奏（中间 40 分钟休息），以便提前适应体力消耗 | P1  |
| US-M5-04 | 作为考生，我希望考后有详细复盘，包括时间分配分析，以便我知道时间浪费在哪       | P0  |
| US-M5-05 | 作为考生，我希望模考成绩能预测真实考试分数，以便我知道还差多少            | P0  |

#### 详细需求

**M5-F1 界面 1:1 还原（严格遵循 R5）**

```
模考作答界面（横屏/竖屏可切，默认竖屏）
┌──────────────────────────────────────┐
│ 张明 | 2026年中级经济考试 | 经济基础  │  ← 标题栏
│ 机位号 A-12        剩余 68:32   [交卷]│
├──────┬───────────────────────────────┤
│ 题号 │                               │
│ ┌──┐ │ 一、单项选择题（共70题）       │
│ │1△│ │                               │
│ │2 │ │ 3. 下列关于...的说法，正确的是 │
│ │3◉│ │                               │
│ │4 │ │ ○ A. ...                      │
│ │5 │ │ ○ B. ...                      │
│ │..│ │ ○ C. ...                      │
│ │70│ │ ○ D. ...                      │
│ └──┘ │                               │
│      │                               │
│ 白=未答│                              │
│ 蓝=已答│  [强调显示] [标记] [计算器]  │  ← 底部作答栏
│ 橙=当前│                              │
│ △=标记 │            [上一题] [下一题] │
└──────┴───────────────────────────────┘
```

严格还原项：

1. **题号栏**：白底=未作答 / 蓝底=已作答 / 橙底=当前 / 三角形=已标记。位置、尺寸、配色与官方一致。
2. **标题栏**：考生姓名、考试名称、科目、机位号、倒计时、交卷按钮。
3. **分栏切换**：案例题默认左右分栏，可切换上下分栏（与官方一致）。
4. **强调显示**：选中文字 → 点"强调显示" → 黄底高亮，**切换试题后自动取消**（与官方行为一致）。
5. **文字缩放**：支持 100%/110%/125%/150%。
6. **内置计算器**：科学型，UI 与 Windows 计算器一致，位置在屏幕左下角可唤起。
7. **标记功能**：点击标记当前题，题号显示三角符号。

**M5-F2 题型分段锁定（对应 R6，最关键）**

| 模式           | 规则                                                          | 说明            |
| ------------ | ----------------------------------------------------------- | ------------- |
| **严格模式（默认）** | 按题型分段。进入下一题型后，**不可返回上一题型修改**。案例题：当前案例内小题可回退，**进入下一案例后不可返回** | 最严约束，推荐所有用户使用 |
| **宽松模式**     | 本科目结束前可自由跳转任意题目修改                                           | 供已确认考场规则的用户使用 |

1. 进入模考前，系统展示规则说明与模式选择，**默认严格模式**并推荐。
2. 严格模式下，当用户完成当前题型最后一道并点击"下一题"时，弹出**强确认弹窗**：
   > ⚠️ 即将进入【多项选择题】，共 35 题。  
   > 进入后将无法返回修改单项选择题的答案。  
   > 当前单选题：已答 68/70，标记 2 题。  
   > `[返回检查]` `[确认进入]`
3. 案例题同理：进入下一案例时确认。
4. **设计理由**：即使真实考场允许回看，练严格模式也只赚不亏——它强制训练"一次性准确率"和"标记-回看"的节奏管理能力。

**M5-F3 时间压力训练**

1. 全程倒计时（与官方一致的右上角位置）。
2. 单题超时提醒：超过平均时限（基础 51s / 实务 54s）时题号栏该题边框变黄，**不强制跳转**（真实考场也不强制，但要做时间感知训练）。
3. 时间节点提醒：剩余 30/15/5 分钟时系统内提示（非弹窗，避免打断）。
4. **两科连考模式**（对应 R3）：一次性连续完成基础（90分钟）+ 间隔 40 分钟休息（App 内倒计时，可锁屏）+ 实务（90分钟）。还原真实体力与节奏消耗。

**M5-F4 交卷流程**

1. 点击交卷 → 展示检查清单：
   - 未作答题数（可点击跳转，宽松模式下）
   - 已标记题数
   - 各题型答题情况
2. 二次确认后交卷，交卷后**不可修改**（与官方一致）。
3. 交卷后即时出分（真实考试需等 60 天，这是产品的正向体验优势）。

**M5-F5 AI 模考复盘报告**

考后自动生成的报告，包含 6 个维度：

| 维度        | 内容                                                        |
| --------- | --------------------------------------------------------- |
| ① 总分与过线判断 | 得分、合格线（84）、差值、**过线概率预测**                                  |
| ② 题型得分分析  | 单选/多选/案例各得分率，与目标的差距                                       |
| ③ 模块得分分析  | 六大模块得分率雷达图，标出拖后腿模块                                        |
| ④ 时间分配分析  | 各题型实际用时 vs 建议用时；耗时最长的 5 道题；**平均单题用时**                     |
| ⑤ 遗憾分分析   | ⭐ 核心亮点：**"本可多得的分数"** = 蒙对后改错的分 + 少选可得的分 + 时间不够没做的分 + 粗心失分 |
| ⑥ 下一步建议   | AI 生成：优先补哪 3 个模块，推荐专项任务包                                  |

> **「遗憾分分析」是本模块最有说服力的功能**。用户考了 78 分，系统告诉他"其中有 11 分是你本来能拿到的"，这比任何鼓励都更能驱动继续学习。

**M5-F6 模考卷来源与节奏**

1. 卷池：教研组卷卷（60%）+ 近 5 年真题卷（30%）+ AI 生成卷（10%，明确标注）。
2. P3 阶段：建议每周 1 套（App 主动安排进计划）。P4 阶段：考前 3 天 1 套最终模考。
3. 真题卷有**使用保护机制**：真题珍贵，同一真题卷建议最多用 2 次，第二次使用时提示"这是真题二刷，注意避免背答案"。

---


### M7 自建题库（上传 + AI 生成）⭐ 用户重点需求

> 用户明确要求："可以自己上传题库和 AI 生成"。这是本产品区别于封闭题库产品的关键能力。
>
> **🔒 v1.1 本地化加强**：切换到纯本地存储后，本模块反而成为最大受益者——
>
> - **隐私**：用户上传的教材、讲义、内部资料只存本机，**永不上传服务器**。对于上传公司内部培训资料、付费讲义的用户，这是决定性的信任门槛。
> - **无存储成本**：不占用云端存储，容量限制可大幅放宽。
> - **原文保留**：上传的 PDF/图片原件本地留存，可随时回看原文对照（云端方案下为省存储往往只留结构化题目）。
> - **离线可用**：自建题库完全离线可练。

#### 用户故事

| #        | 用户故事                                                | 优先级 |
| -------- | --------------------------------------------------- | --- |
| US-M7-01 | 作为考生，我希望上传 PDF/Word/图片格式的资料，系统自动识别成题目，以便把网盘资料变成可练的题 | P0  |
| US-M7-02 | 作为考生，我希望拍照上传纸质习题，OCR 后自动转成电子题，以便练习纸质材料              | P0  |
| US-M7-03 | 作为考生，我希望指定一段教材内容让 AI 出题，以便针对刚学的章节加深练习               | P0  |
| US-M7-04 | 作为考生，我希望 AI 生成的题目有解析且标注这是 AI 生成的，以便我判断可信度           | P0  |
| US-M7-05 | 作为考生，我希望自己上传的题库能像官方题库一样进入复习调度，以便统一管理                | P0  |
| US-M7-06 | 作为考生，我希望发现题目错误时能一键举报并得到反馈，以便题库质量可控                  | P0  |
| US-M7-07 | 作为考生，我希望把自己的题库导出备份，以便换设备不丢                          | P1  |

#### 详细需求

**M7-F1 上传入口与格式支持**

| 输入方式 | 支持格式                         | 说明               |
| ---- | ---------------------------- | ---------------- |
| 文件上传 | PDF、Word(.docx)、TXT、Markdown | ≤ 50MB / ≤ 100 页 |
| 图片上传 | JPG/PNG（支持多图，最多 20 张）        | OCR 识别           |
| 拍照   | 相机直拍（自动边缘检测 + 增强）            | 纸质习题场景           |
| 粘贴文本 | 剪贴板纯文本                       | 最快路径             |
| 网页链接 | 支持解析公开网页正文                   | P1               |

**M7-F2 解析流水线**

```
① 上传 → ② 格式解析（PDF文本层/OCR） → ③ 版面还原 → ④ 题目切分
   → ⑤ 结构化抽取（题干/选项/答案） → ⑥ 知识点映射 → ⑦ 质量校验
   → ⑧ 生成预览 → ⑨ 用户确认编辑 → ⑩ 入库
```

1. **步骤 ②**：PDF 优先提取文本层，无文本层则走 OCR（中文识别准确率要求 ≥ 98%）。
2. **步骤 ④ 题目切分**：基于规则（题号正则：1. / 1、/ 第1题 / (1)）+ 语义模型兜底，切分单题。
3. **步骤 ⑤ 结构化抽取**：抽取题干、选项（A-E）、答案、解析（若有）。
   - 若原文本**无答案**：AI 自动作答生成参考答案，并标记 `答案来源=AI推断`，提示用户核对。
4. **步骤 ⑥ 知识点映射**：通过 Embedding 相似度 + 关键词匹配，把题目映射到知识树的知识点。置信度 < 0.7 时标记"未分类"，允许手动指定。
5. **步骤 ⑦ 质量校验**（详见 §8.6）：去重、完整性检查、答案合理性校验。

**M7-F3 生成预览与人工确认（关键交互）**

> **绝不静默入库。** AI 处理的结果必须让用户看得见、改得了。

1. 解析完成后展示**预览列表**，每道题以卡片展示：题干、选项、答案、解析、识别置信度、知识点标签。
2. **置信度可视化**：
   - 高（≥0.9）：绿色 ✓ "识别良好"
   - 中（0.7-0.9）：黄色 ⚠ "请核对"
   - 低（<0.7）：红色 ⚠ "建议检查或删除"
3. 批量操作：全选/反选、按置信度筛选、批量删除、批量修改知识点标签。
4. 单题编辑：点击进入编辑页，可修改题干、选项、答案、解析、知识点、难度。
5. 底部「确认导入（N 题）」按钮。导入后进入"我的题库"。

**M7-F4 我的题库管理**

1. 我的题库 = 用户自建题的集合，支持创建**题集（Folder）**，如"2026教材新增内容""老师押题讲义""错题截图集"。
2. 题集操作：重命名、删除、合并、导出（PDF/Excel/Anki 格式）、分享（生成分享码，P2）。
3. 题集可直接发起练习，支持所有练习模式（章节练不适用，因可能未映射知识点）。
4. **纳入复习调度**：自建题答错后同样进入错题池与 FSRS 调度。
5. **容量限制（v1.2 调整）**：数据存本地，无云端成本。**免费用户 ≤ 3000 题**（看激励视频可临时扩容，单次 24 小时内 +2000 题）；**已购买去广告用户 ≤ 100000 题**（实际上限取决于设备剩余存储）。上传的原始文件（PDF/图片）同样保存在 App 私有目录，可在「我的 → 存储管理」查看占用并选择性清理（**清理原件不影响已入库的题目结构化数据**）。
6. **v1.2 说明**：容量限制的设计意图是**防止滥用而非制造门槛**。3000 题足够覆盖绝大多数个人自建需求（用户整理的真题+讲义通常在这个量级）。真正需要 10 万题的是"把整本教材+多年真题全丢进来"的重度用户，他们要么看广告扩容，要么买断——**两条路都是通的，没有死胡同**。

**M7-F5 AI 出题（从内容生成题目）**

用户上传一段教材/笔记内容，或指定某个知识点，AI 生成题目。

1. 入口：知识点详情页「AI 出题」、题集页「AI 生成」、AI 私教对话中直接说"给我出 5 道题"。
2. 参数配置（收纳在折叠面板，默认展开精简版）：
   - 题量（1-20）
   - 题型（单选/多选/案例，可混合）
   - 难度（简单/中等/困难/自适应）
   - **认知层次**（布鲁姆分类，默认混合）：记忆 / 理解 / 应用 / 分析
   - 出题风格：常规 / 辨析型（易混概念）/ 计算型 / 场景应用型
3. 生成后进入**与 M7-F3 相同的预览-编辑-入库流程**。
4. 每道 AI 生成题必须携带元数据并在 UI 上可见：
   ```
   ✨ AI 生成 · 待验证 | 置信度 0.87 | 生成模型 xxx | 2026-09-03
   ```
5. **真题模考卷中禁止混入 AI 生成题**（防止污染成绩预测准确性）。

**M7-F6 争议与纠错闭环**

1. 每题提供「有疑问」入口（题目详情页右上角 ⋯ 菜单）。
2. 提交后进入教研复核队列，状态可查：待处理 / 已确认有误（已修正/已下架）/ 已确认无误（附说明）。
3. **响应 SLA**：S 级（真题/官方）24 小时，A 级 48 小时，B 级（AI生成）72 小时。
4. 用户提供的正确纠错给予积分奖励（**可兑换免广告天数**，v1.2 调整——本产品已无"会员时长"概念；**v1.5 起不再兑换 AI 额度**，因为已无额度体系）。

---

### M8 数据看板与过线预测

#### 详细需求

**M8-F1 学习数据看板**

1. 总览卡片：累计学习天数、连续天数、总题量、总正确率、距考试天数。
2. 图表（简洁，不超过 4 个）：
   - 学习热力图（GitHub 风格，全年打卡）
   - 掌握度趋势折线（按周）
   - 模考成绩趋势（含 84 分参考线）
   - 模块掌握度雷达图
3. **元认知校准图**（特色）：横轴=确信度，纵轴=实际正确率，展示用户"自我判断是否准确"。这是很有教育价值的可视化。

**M8-F2 过线概率预测**

1. 模型输入：模考成绩（加权，近期权重更高）、知识点掌握度（按分值权重加权）、剩余天数、历史学习效率、计划完成率。
2. 输出：`过线概率 XX%` + 预测分数区间（如"预计 88-97 分"）+ 关键提升杠杆（"把财政模块掌握度从 45% 提到 70%，过线概率将提升 12 个百分点"）。
3. **必须附免责声明**：预测基于模型估算，不构成对考试结果的承诺。
4. 更新频率：每次模考后重算，每周例行重算。

---


### M9 商业化与变现（v1.3 开屏广告 + 渐进释放重构）⭐

> **v1.3 核心决策：以开屏广告为稳定变现主力，主动收缩高干扰位；移除"考前 7 天停广告"，改为「由少到多」的广告渐进释放。**
>
> v1.2 的设计哲学是"把广告压到最低、把信任放到最大"，因此拒绝了开屏广告、并提出"考前 7 天全部关闭"。v1.3 对这两个判断做了修正——**理由是商业可持续性与用户体验的一致性，而不是短期收入**：
>
> **① 为什么要开屏**：激励视频收入高度依赖"用户愿不愿意主动点"。而备考用户的实际行为是——**打开 App 大多是直奔今日任务，很少点奖励入口**。这导致 v1.2 的收入模型过于依赖一个低频行为，波动大、不可预测。**开屏广告是唯一稳定、可预测的变现来源**（每次启动必曝光），有了它，我们才**有能力主动砍掉那些"卡顿用户主流程"的广告位**。这不是"加一个广告"，而是**用稳定的收入换来对高干扰位的削减权**。
>
> **② 为什么取消"考前 7 天停广告"**：这条设计有一个现实缺陷——**它让广告的出现变得不可预测**。用户前 6 个月适应了"有广告"，考前一周广告突然全部消失，考完又全部回来，会产生"这 App 怎么一会儿有一会儿没有"的困惑。更糟的是：**如果用户在考前一周才发现"原来可以没有广告"，他对前 6 个月广告的容忍度会瞬间崩塌**——这正是"被欺骗感"的来源。
>
> **正确的做法是反过来**：**广告从第一天就可以出现（开屏），但位置由少到多、逐步释放**，让用户对"这个 App 有哪些广告"形成**稳定、完整、可预期**的认知。宁可一开始就坦诚，也不要后期突袭。
>
> 但免费不等于无代价。**广告是备考产品的伦理风险点**：用户把最稀缺的资源（注意力）交给我们，任何一次在不该出现的时刻弹出的横幅，都是在偷走他本应用来记一个考点的 15 秒。因此本章不只是"放哪些广告位"，而是一整套**克制经济学**：把干扰压到最低、把信任收益放到最大、把去广告做成一份"体面的选择"而不是"被逼的赎金"。

---

#### M9-F0 收入结构总览（一句话看懂怎么赚钱）

```
┌─────────────────────────────────────────────────────────────┐
│  收入结构（三条腿，全部依赖平台内购，无自有服务端）            │
├─────────────────────────────────────────────────────────────┤
│  ① 开屏广告（主力·稳定）每次启动必曝光，收入可预测             │
│                        约占广告收入 40-45%                    │
│  ② 激励视频（辅助·高 eCPM）用户主动触发，零心流打断            │
│                        约占广告收入 35-40%                    │
│  ③ 去广告（次）        一次性买断 ¥68 / 本考季                 │
│                        终身版 ¥128（跨考季，可选）             │
├─────────────────────────────────────────────────────────────┤
│  ⚠️ 已削减：昨日结算页加练位、内容页原生卡片（见 M9-F3）        │
│     削减的理由：这两个位置会卡顿主流程，收入/体验比不划算       │
├─────────────────────────────────────────────────────────────┤
│  原则：开屏收入 = 稳定底盘，用来"买下"削减高干扰位的空间        │
│        激励视频 = 高价值增量，用户主动换取实际好处             │
│   AI 费用  = 纯 BYOK，用户自负 → 我们 ¥0（v1.5 撤销中转）      │
│              ② BYOK：用户自付，我们不经手                    │
└─────────────────────────────────────────────────────────────┘
```

**为什么是"开屏 + 激励视频"这个组合，而不是纯激励视频**（本节最重要的一条，后续所有设计都由此推导）：

v1.2 选择"纯激励视频"，理由是"零心流打断"。这个判断在体验上是对的，但在**商业上有一个致命缺陷：收入完全依赖用户的主观意愿**。

真实用户行为是这样的：**大多数备考用户打开 App 后直奔"今日任务"，根本不会点进奖励中心，也不会主动点"看视频换额度"（该广告位 v1.5 已撤销）。** 结果是激励视频的实际人均观看次数远低于预期，收入波动大、无法用于支撑固定成本（教研、内容更新、客服）。

**开屏广告解决的是"确定性"问题**：

- 每次启动必曝光 → 收入 **可预测、可规划**
- 用户对开屏广告有**成熟的认知**（几乎所有国内 App 都有），心理预期低、抵触感弱于"学习中途弹出的横幅"
- 时长可控（3-5 秒，**可跳过**），不占用学习时间

**但开屏的代价必须用削减其他广告位来偿付**——否则就变成了"广告堆砌"，而那是我们明确要避免的。因此 v1.3 **主动砍掉了两个位置**（详见 M9-F3 末段），把开屏带来的收入增量，用于换取用户主流程的顺滑。

> **一句话原则**：**用一个高确定性、低认知负荷的位置（开屏），换掉多个高干扰、低效率的位置（结算页加练、内容页原生卡片）。总量不一定增加，但体验一定变好、收入一定变稳。**

---

#### M9-F1 三条铁律（不可协商的商业硬约束）

以下三条为**产品级硬约束，优先级高于任何收入目标**。任何广告位设计、任何商业化实验，都必须先通过这三条的自检。

| 铁律                        | 含义                                                                  | 自检问句                                             |
| ------------------------- | ------------------------------------------------------------------- | ------------------------------------------------ |
| **T1 · 学习心流禁投区**          | 用户处于「正在获取价值」的流程中时，**任何形式的广告（含去广告引导）都不得出现**                           | "此刻用户是**在用**产品，还是在**等**产品？"——若在**用**，一律禁投        |
| **T2 · 主动触发优先，被动曝光受限** ⚠️v1.3 修订 | **默认只做主动触发**。唯一的被动曝光例外是**开屏广告**，且必须满足：可跳过、≤5 秒、不在学习流程内、不遮挡任何功能入口 | "用户如果什么都不点，会不会看到广告？"——**除开屏外**若会，违法               |
| **T3 · 不买进度，不买正确率**        | 激励机制只兑换**便利**（跳过、外观、导出次数），**绝不兑换学习结果**（分数、掌握度、复习量、模考次数、错题攻克）。**v1.5 起"额度"已从可兑换项中移除** | "这个奖励能不能让用户以更少的努力通过考试？"——若能，落回 §5.3 反暗黑模式红线，违法 |

> ⚠️ **v1.3 关于 T2 的修订说明（重要）**：v1.2 的 T2 是"只做主动触发"，开屏广告与之直接冲突，因此必须**明确修订而非偷偷绕过**。
>
> 修订后的 T2 保留了它的**核心保护对象**——"学习过程中的被动打扰"依然是绝对禁止的；它放开的只是一个**边界清晰的例外**：开屏广告。
>
> **为什么开屏可以例外**：用户在点击图标到进入首页之间的 3-5 秒，是**"等待产品启动"的时间，不是学习时间**。此时用户的心理预期本就是"App 要加载一会儿"（所有 App 都这样），广告不占用任何学习时间、不打断任何学习动作。这与"答题答到一半弹出广告"有本质区别——**前者占用等待时间，后者占用学习时间。**

**T1 禁投区白名单（发生即视为严重缺陷）**：

```
❌ 答题过程中（题干展示 → 提交 → 看解析，全流程）
❌ 今日复习队列（FSRS 到期题，硬约束队列）
❌ 模考全流程（进入考场 → 交卷 → 出分，含 40 分钟连考中场休息）
❌ 学习计划生成 / 计划重排过程中
❌ AI 对话过程中（含流式输出期间）
❌ 上传资料的解析处理过程中
❌ 首次启动 onboarding 全流程（含 S5 摸底测评）
❌ 前端埋点上报 / 备份导出 / 数据恢复过程中
❌ 除开屏外的任何"用户没点就出现"的位置
```

**允许出现广告的位置（v1.3 精简为 4 个）**：

```
✅ 【开屏】每次启动曝光（可跳过，≤5s）— 唯一被动曝光位，受 M9-F3 位置⓪ 全部约束
✅ 【激励视频】任务包完成后的「成果卡」— 用户主动点击
✅ 【激励视频】「我的」页奖励中心 — 用户主动进入
❌ 【激励视频】获取额外 AI 额度 — **v1.5 已撤销**（无额度体系，此广告位取消）
```

> **v1.3 删除了两个位置**（详见 M9-F3 末段）：原位置②「今日结算页加练」、原位置⑤「内容页底部原生卡片」。**删除理由：这两个位置卡顿或干扰了用户的主流程，而开屏带来的稳定收入已足以偿付这次削减。**

---

#### M9-F2 免费版权益矩阵（广告支持的免费版到底给多少）

**核心决策：把备考的「完整核心能力」全部免费，广告只换便利，不换能力。**

这是本模式最关键的产品判断。市面上失败的免费备考 App，几乎都犯同一个错：**把核心能力（模考、题库）锁在广告或付费后面**，导致用户走不到"感受到价值"的那一刻就已经卸载了。

本产品的做法恰恰相反：

> **用户完整做完一套全真模考、看完全部 6 维度 AI 复盘报告、刷完全部题库、用上 FSRS 智能复习——全程不花一分钱，不被一条广告打断。**
>
> 广告只出现在「还想再多要一点」的地方，以及「打开 App 时那几秒等待」。

| 能力                | 免费版（广告支持）      | 说明                       |
| ----------------- | -------------- | ------------------------ |
| 两科全量题库 + 精讲        | ✅ **完整**      | 不设题量上限、不设章节解锁            |
| 全真机考模考（含连考模式）     | ✅ **完整**      | 核心差异化功能，必须免费体验           |
| AI 复盘报告（6 维度）     | ✅ **完整**      | 含「遗憾分分析」——最有说服力的功能，不锁     |
| FSRS-6 智能复习队列     | ✅ **完整**      | 学习科学的落地，锁了就不是这个产品        |
| 错题本 / 薄弱点诊断        | ✅ **完整**      | —                        |
| 数据看板（基础 4 图）      | ✅ 完整          | 热力图 / 掌握度趋势 / 模考趋势 / 雷达图  |
| 自建题库（上传 + OCR）     | ✅ 完整          | ≤ 3000 题                 |
| 计划引擎（四阶段 + 动态重排）  | ✅ 完整          | —                        |
| **过线概率预测**        | ✅ 完整          | 不锁（这是产品信任的锚点）            |
| **AI 功能** 🔄v1.5  | **纯 BYOK**——填自己的 Key，**无限制**；未配置则不可用（**可选增强，不影响任何学习功能**） | 见 M9-F7 —— **v1.5 无平台额度，故无"看视频续期"** |
| **自建题库容量**        | 3000 题         | 看激励视频可临时扩容（单次 24h 内 +2000） |
| **备份导出频次**        | 每周 1 次        | 手动本地备份不限制；云盘分享看视频解锁      |
| **数据看板进阶维度**      | 3 个需解锁        | 能力矩阵、学习效率曲线、同期考生对照       |
| **记忆助手（口诀/导图）**   | 每日 3 次        | 看激励视频 +2 次               |
| 广告                | 有（受 T1/T2/T3 约束） | —                        |

> ⚠️ **特别注意**：**「过线概率预测」不锁、不卖、不做付费增强**。这是本产品的信任锚——一旦用户发现"概率预测要付费才能看准"，整套学习科学的可信度就塌了。宁可少赚，不可自毁地基。

**"完整能力全免费"这件事本身，就是最强的获客与口碑资产**。App Store 评论区里"这才是真的免费"比任何投放都有效。

---

#### M9-F3 广告位设计（v1.3 精简为 4 个位置 → **v1.5 最终为 3 个**，逐个交代）

##### 位置 ⓪ 开屏广告（v1.3 新增·变现主力，占广告收入 40-45%）

**这是 v1.3 最重要的一次改动，也是唯一允许的被动曝光位。因此它的约束必须比其他位置严格十倍。**

```
┌─────────────────────────────────────┐
│                                     │
│                                     │
│         [ 广告素材区域 ]              │
│         品牌图 / 短视频              │
│                                     │
│                                     │
│  ┌──────────────┐                   │
│  │  跳过 3s  ⊙  │ ← 右上角，随机位  │
│  └──────────────┘   置（防误触训练） │
│                                     │
│  ─────────────────────────────────  │
│  📘 经师 AI       广告               │ ← 明确标注「广告」
└─────────────────────────────────────┘
```

**硬性约束（任何一条不满足即视为缺陷）**：

| # | 约束                       | 说明                                                |
| - | ------------------------ | ------------------------------------------------- |
| 1 | **总时长 ≤ 5 秒**            | 含跳过按钮出现前的等待时间。**严禁"跳过按钮在 3 秒后才出现 + 广告还有 30 秒"这种套娃设计** |
| 2 | **跳过按钮 2 秒后必须出现**        | 且**位置固定、可预期**（建议右上角，符合国内用户习惯）。不做"跳过按钮藏得很小"的把戏  |
| 3 | **跳过按钮可点击区域 ≥ 44×44pt** | 满足无障碍最小触控标准（§5.2 费茨定律）。**故意做小按钮属于反暗黑模式，禁止**       |
| 4 | **点击跳过 → 立即进入 App**     | 不得有"再确认一次"、"再等 1 秒"的二次拦截                           |
| 5 | **不遮挡任何功能**              | 开屏期间不得出现"开通会员"悬浮球、不得索要任何权限                        |
| 6 | **冷启动路径优先**              | 广告必须在**首屏渲染完成后**才展示。用户看到的是"App 已就绪 + 广告"，而不是"白屏等广告" |
| 7 | **中断即放弃**                | 用户在开屏期间切后台/锁屏 → 返回时**不再展示广告**，直接进 App            |
| 8 | **离线不展示**                | 无网络时**直接跳过开屏**，进 App。**绝不出现"加载失败"页面**（M9-F11）     |
| 9 | **频率上限**                 | 每次**冷启动**展示；**热启动（从后台切回）不展示**。单日上限 **12 次**（超过则本次跳过） |
| 10 | **渐进释放**                | 新用户首日**不展示**（见 M9-F4 渐进释放表），从第 2 天起逐步启用            |

**为什么开屏是"用户可以接受"的**（这条论证决定了它是否合规于我们的伦理标准）：

1. **占用的是等待时间，不是学习时间**。用户点击图标后的这几秒，本来就在等 App 加载。广告没有"偷走"任何学习时间。
2. **用户对此有成熟认知**。国内几乎所有 App 都有开屏广告，这已经是用户默认预期的一部分。**没有预期落空的伤害**（对比之下，"答题中途弹广告"才是预期外的伤害）。
3. **用户握有完全的控制权**：5 秒上限 + 可跳过 + 可买断移除。**它不是强制的，是可以选择退出的。**
4. **它换来了削减其他广告位的能力**——这是它最大的正当性来源。

> ⚠️ **但必须承认代价**：开屏广告会在**每一次冷启动**时制造几秒的等待感。因此 M9-F4 的**渐进释放**机制变得至关重要——**用户对广告的接受度是一个需要被"培养"而非"强加"的过程。**

##### 位置 ① 任务包完成后的「成果卡解锁」（激励视频主力位，占广告收入 30-35%）

```
┌─────────────────────────────────────┐
│  🎉 任务包完成！                      │
│                                     │
│  ✅ 本章掌握度 45% → 58%             │
│  ✅ 答对 16/20 · 攻克 3 个易错点       │
│  ⏱️ 用时 14 分钟 · 比预计快 3 分钟      │
├─────────────────────────────────────┤
│  🎬 看 15 秒，解锁「AI 深度洞察」       │
│     · 你在"财政政策工具"上的错因分析    │
│     · 3 道针对你薄弱点的同类题          │
│                          [ 解锁 → ]  │
├─────────────────────────────────────┤
│  [ 继续下一个任务 ]        [ 结束 ]   │  ← 主按钮始终在，不被遮挡
└─────────────────────────────────────┘
```

**设计要点**：
- **时机**：任务的**目标已达成**之后（峰终定律的正向峰值点）。用户的情绪是"我完成了"，而不是"我被打断了"。
- **交换契约明确**：15 秒 ↔ 一份**具体、可描述**的价值（不是"解锁神秘奖励"——模糊的承诺会被读成欺骗）。
- **主行动不被遮挡**：「继续下一个任务」与「结束」始终可见可点。**不点广告完全不影响任何事**，这是 T2 的落地。
- **频次上限**：每个任务包后最多 1 次，每日上限 6 次（见 M9-F4）。
- **不重复**：同一天内，同一用户第 3 次之后，卡片改为仅展示成果，不再出现解锁入口（避免变成"每天都来烦我"）。

##### 位置 ② 「我的」页 · 奖励中心（激励视频，占广告收入 15-20%）

一个用户**完全主动进入**的页面，本质是"任务墙"：

| 可做的事                  | 奖励            |
| --------------------- | ------------- |
| ~~看激励视频~~             | ~~+3 次 AI 调用额度~~ **v1.5 已删除**（无额度体系） |
| 完成今日全部任务包             | +1 天免广告体验      |
| 连续打卡 7 天              | +1 天免广告体验      |
| 邀请同学（分享战绩图）           | +30 天免广告体验     |
| 帮助改进产品（开启匿名上报，§13.4）  | +1 天免广告体验      |
| 举报题库错误并被采纳（M7-F6）     | +7 天免广告体验      |

> **v1.3 调整 / v1.5 确认**：原「帮助改进产品 → 永久 +1 次/日 AI 额度」改为「+1 天免广告体验」。原因：v1.3 已取消平台承担的 AI 额度生意（M9-F7），"永久额度"这一激励形式不再成立，改为免广告天数更自洽。**v1.5 撤销中转后，本产品彻底无 AI 额度概念，该调整得到进一步确认。**

- 用户不来这里，就永远看不到任何东西。
- 奖励均为**便利性**，无一涉及学习结果（T3）。**v1.5 起不再包含 AI 额度类奖励。**

##### ~~位置 ③ 获取额外 AI 体验额度~~（**v1.5 已撤销**）

> ⛔ **此广告位随中转方案一并删除。**
> 原因：v1.5 改为纯 BYOK，**平台不存在任何额度**，因此"额度耗尽 → 看视频续期"这一场景不复存在。
> **广告位数量由 4 个减为 3 个**：开屏 / 成果卡 / 奖励中心。
> 这一删除**同时消除了一个商业风险**：v1.4 中"用户看视频换额度"是净亏的（收入 ¥0.04 < 成本 ¥0.10），现在这笔亏损不存在了。
> `[ 看 30 秒换取 +5 次 ]`  ← 用户主动点击
> `[ 配置我自己的 API Key（无限使用）]`  ← 主推路径，视觉权重更高
> `[ 今天先不用了 ]`

- ⚠️ **主推路径是 BYOK，不是看广告**。因为 BYOK 对我们零成本、对用户无限额——这是双赢，理应优先推荐。
- 看广告只是"暂时不想折腾 Key"用户的过渡方案，**不是我们希望长期发生的行为**。

##### ⚠️ v1.3 主动削减的两个位置（重要说明）

v1.2 设计了 5 个位置，v1.3 **主动删除了其中 2 个**：

| 删除的位置 | v1.2 定位 | 删除理由 |
| ----- | ------- | ---- |
| **原位置② 今日结算页「再来一组加练」** | 占 25% | ⚠️ **它卡顿了用户的"收尾仪式"**。用户完成今日全部任务是一个**心理上的完成时刻**，此刻最该给他的是"今天辛苦了，明天见"的轻松收尾。在这个位置塞一个"再看 30 秒"的选项，会把一个干净的正向峰值**污染成"还有事没做完"的未完成感**——这违反了蔡格尼克效应的正向用法 |
| **原位置⑤ 内容页底部原生卡片** | 占 5% | ⚠️ **性价比最低，且最容易引发反感**。原生卡片 eCPM 本就低（¥6-15），而"翻列表翻到底部突然出现一张广告"是**用户最熟悉、也最厌恶的广告形式**。用 5% 的收入换取一类明确的负面情绪，不划算 |

**这次削减的决策逻辑，值得写进产品原则**：

> **收入结构应该"少数位置高价值"，而不是"多个位置低价值"。**
>
> 前者：用户对广告的**总感知次数少**，但对每次曝光的**接受度较高**，总收入稳定。
> 后者：用户到处都遇到广告，**每一次都加深"这 App 广告真多"的印象**，最终导致卸载——而每一次曝光赚到的钱，远低于流失一个用户的损失。
>
> **5 个低价值位置，不如 2 个高价值位置 + 1 个稳定的开屏。**

---

#### M9-F4 频次控制与「渐进释放」机制（v1.3 重写核心）

**总原则：广告位置的暴露，应该像"引入新功能"一样被管理——由少到多、可预期、永不突然出现。**

##### ① 频次上限

| 规则                     | 数值                            | 理由                     |
| ---------------------- | ----------------------------- | ---------------------- |
| **开屏每日上限**             | 12 次/日（超出则跳过本次）               | 冷启动才展示，热启动不算。覆盖不了"每天开 30 次"的极端用户 |
| **每日激励视频总上限**          | 6 次/日                         | 超过这个数，用户会把"看广告"当成负担而非选择 |
| **主动触发广告的单次最短间隔**      | 90 秒                          | 防止误触连点、防止"刚看完又弹一个"     |
| **同一位置每日展示上限**         | 3 次（位置①/②）                  | —                      |
| **疲劳熔断**               | 连续 3 天某位置点击率为 0 → 该位置降频 50%   | 自动识别"不感兴趣"用户，主动退让      |
| **模考日保护**              | 用户当日完成过模考 → 当日激励视频上限降为 3 次    | 模考消耗大，当天应给予优质体验        |

> 🔧 **v1.5 实现期说明（约束叠加时的处理）**：
> 上表六条规则**同时生效**，任一不满足即不放行。当多条规则对同一位置给出不同上限时，**取最严格者**。
>
> 典型例子：奖励中心（位置②）同时受「激励视频 6 次/日」与「同一位置 3 次/日」约束 —— 后者更严，因此**该位置实际单日最多 3 次**，6 次的额度用不满。
>
> ⚠️ 这是**刻意设计而非缺陷**：两个上限并存时取严，是唯一不会伤害用户的选项。开发/测试时若发现"激励视频额度没用完"，属预期行为，**不得据此放宽同位置上限**。
>
> 实现落点：`frequencyCap.ts` 的 `canShowByFrequency()`；该交错关系已由单测 `frequencyCap.test.ts` 显式断言锁定。

##### ② 「渐进释放」机制（v1.3 新增，替代 v1.2 的"考前 7 天停广告"）

> **设计意图**：让用户对"这个 App 有哪些广告"形成一个**稳定、完整、可预期**的认知过程。
>
> **核心区别**：v1.2 是"先没有，后突然有"（考前 7 天停广告 = 用户后期才发现"原来可以没有"→ 产生被欺骗感）。v1.3 是"**从一开始就有，由少到多慢慢来**"（用户全程知道有广告，只是位置逐步释放 → 无预期落差）。

**释放时间表**（以用户首次启动为 D0）：

| 阶段            | 时间窗       | 开屏            | 位置① 成果卡 | 位置② 奖励中心 | 说明                              |
| ------------- | --------- | ------------- | ------- | --------- | ------------------------------- |
| **阶段 0 · 纯净期** | D0        | ❌ 不展示         | ❌ 隐藏     | ❌ 隐藏      | **首日完全无广告**，让用户完整建立"这是个好产品"的第一印象 |
| **阶段 1 · 单点引入** | D1-D2     | ✅ **开启**      | ❌ 隐藏     | ❌ 隐藏      | **只引入开屏**。用户只感知到"打开时有个广告"，**内容区依然纯净** |
| **阶段 2 · 成果位开放** | D3-D5     | ✅             | ✅ **开启**  | ❌ 隐藏      | 引入成果卡——这是最容易被接受的位置（用户已完成任务）。**同时开放位置②奖励中心**（用户主动进入，零打扰） |
| **阶段 3 · 完全体** | D6+       | ✅             | ✅         | ✅         | 全部位置开放。**⚠️ v1.5：阶段 3 不再引入新位置**（原位置③ AI 额度位已随中转方案一并删除），此阶段仅为「渐进过程已走完」的语义标记 |

**关键设计约束**：

1. **阶段 0 的"纯净首日"必须保留**（D0 全天无任何广告，含开屏）。
   - 理由：首日的目标是**让用户完成第一个任务包、感受到学习科学的价值**。任何一次曝光都可能在这个最脆弱的时刻造成流失。**这一天的广告收入微不足道，但它决定用户会不会留下。**
   - ⚠️ 注意：这与 v1.2 的"48 小时零广告"不同。v1.2 是"48 小时完全无广告"，v1.3 缩短为 **24 小时**——因为要保证"渐进"是**看得见的过程**，太长会让用户误以为"这 App 没广告"，反而在揭露时造成落差。

2. **开屏在阶段 1 就引入，而不是放到最后**。
   - 理由：开屏是"最容易被理解"的广告形式（用户认知成熟）。**先用最容易接受的位置建立"这个 App 有广告"的心理预期**，后续引入内容区位置时才不会突兀。
   - 如果反过来（先上内容区广告、最后才上开屏），用户体验是"广告越来越多且越来越烦人"，感受完全不同。

3. **一次只引入一个新位置**（同一阶段内最多引入 1 个新位置）。
   - 理由：用户对"新增了一个广告位"的敏感度，远高于"广告位一直都在"。**批量引入 = 一次感知冲击；逐个引入 = 无感过渡。**

4. **释放节奏基于"活跃天数"而非自然日**。
   - 定义：D_n = 用户有实际学习行为的第 n 天（当日完成 ≥ 1 个任务包）。
   - 理由：如果一个用户下载后放置了 30 天才开始用，他应该从"阶段 0"开始，而不是一上来就面对完全体广告。**这保证每个用户都经历完整的、教科书式的渐进步骤。**

5. **⚠️ 自然日兜底必须带「最近活跃」前置条件**（v1.5 实现期补充，修正一处真实漏洞）。
   - 背景：兜底机制（活跃不足但自然日达 2 倍阈值即推进）本意是补偿**间歇性活跃**用户（如只周末学习的在职考生），避免他们因活跃天数攒得慢而被长期锁死在低级阶段。
   - ⚠️ **漏洞**：实现早期，兜底**无条件生效**，会导致：
     > 用户 9/1 下载、只用了 1 天就放置，9/20 再打开。
     > 活跃天数 = 1（本该停在阶段 1），
     > 但自然日 = 19 天 ≥ 6×2 = 12 天 → **直接跳到阶段 3「完全体」**。
     >
     > 这与本条约束 4 的核心意图**直接冲突** —— 本条明确要求"放置 30 天后开始用，应从阶段 0 开始"。
   - ✅ **修正**：兜底生效的**必要条件**是「距上次活跃 ≤ 3 天」。从而区分两类用户：
     - 周末学习者：一直在活跃 → 兜底正常生效 ✅
     - 长期放置后回归的用户：距上次活跃远超 3 天 → 兜底失效，按实际活跃度重新计算 ✅
   - 附带结论：**活跃天数 = 0 的用户永远停在阶段 0**（兜底也不生效）。这与"纯净首日不可妥协"一致 —— 一次都没学过，就不该被推着进入广告阶段。
   - 代码落点：`releaseEngine.ts` 的 `NATURAL_FALLBACK_MAX_GAP_DAYS` / `daysSinceLastActive()`。

6. **不得回退，也不得加速**。
   - ❌ 禁止"前一天有、后一天没有"（会引发"为什么昨天有今天没有"的困惑，与 v1.2 考前停广告是同一个错误）。
   - ❌ 禁止因为"数据不好看"而把时间表压缩（如 D1 就全部上线）。

**与"考前保护"的关系（v1.3 明确取消）**：

> ❌ **v1.2 的「考前 7 天全部关闭广告」已取消。**
>
> **取消理由（这是 v1.3 的一个重要判断）**：
>
> 这条设计看起来很美好（"冲刺期不打扰用户"），但它有一个**严重的连带伤害**：它让广告的存在变得**不可预测**。
>
> 设想一个真实场景：一个用户在 9 月开始用这个 App，用了两个月，已经习惯了"打开有开屏、完成任务有成果卡"的节奏。到了 11 月初，广告突然全部消失。他会怎么想？
>
> - 最好的情况："哦，可能是考前照顾我们"——但**他依然会意识到"原来广告是可控的"**。
> - 最坏的情况："广告没了？不习惯……" 然后考完试，**广告全部回来**，他的感受是"**这东西果然是冲着钱来的，考完就变脸了**"。
>
> **核心问题**：任何"广告会突然变化"的设计，都会打破用户对产品的**稳定性预期**。而稳定性预期一旦破裂，用户会开始**审视每一次广告曝光**——这才是"被欺骗感"的真正来源。
>
> **v1.3 的替代方案**：不再做"考前特殊照顾"，而是用**整体的克制**来保证体验：
> - 位置从 5 个减到 4 个（M9-F3）
> - 学习流程依然零广告（T1 铁律不变）
> - 开屏严格 ≤5 秒且可跳过
> - **考前 7 天仍然有广告，但只保留开屏**（一种温和的处理方式：冲刺期减少内容区干扰，但保持"有广告"的一致性认知）
>
> ⚠️ 注意最后一条：**考前 7 天只保留开屏**是允许的，因为它**不产生"有/无"的突变**（开屏本就在阶段 1 之后就常驻），只是内容区广告位静默。这是"减少"而非"消失"，用户感知上更接近"今天没碰到"，而不是"广告被关掉了"。

##### ③ 关于「免广告体验」的"时间货币"设计

奖励中心里的「+1 天免广告体验」是本模式的一个巧思——它把广告从"必须忍受的东西"变成"可以通过正向行为获得豁免的东西"。用户连续打卡 7 天 → 获得 1 天纯净体验 → 体会到无广告的爽感 → 更有动力买断去广告。**这是一条自然的转化路径，且全程无推销。**

> ⚠️ **重要约束**：免广告体验期间，**开屏广告也必须一并移除**（否则用户会觉得"我明明换了免广告，怎么还有"）。**「免广告」必须是名副其实的全量去广告**，不能有例外——这是信任问题。

---

#### M9-F5 变现主体推荐：¥68 一次性去广告（本考季）

##### 定价

| 档位            | 价格       | 权益                                    | 有效期        |
| ------------- | -------- | ------------------------------------- | ---------- |
| **去广告 · 本考季** | **¥68**  | 移除**全部**广告位（含开屏）+ 奖励中心全部解锁            | 至 2026-12-31（本考季结束） |
| **去广告 · 终身**  | **¥128** | 同上，且跨考季永久有效（二战/换专业无需再买）               | 永久         |
| 单独看广告换奖励      | ¥0       | 免费用户路径，**永久保留**                       | —          |

> **v1.3 说明 / v1.5 确认**：v1.2 的买断权益中包含「AI 每日额度 5 → 30 次」。**v1.3 已删除该项**——因为平台不再自行提供 AI 额度。**v1.5 撤销中转后，本产品彻底无 AI 额度体系，该判断得到最终确认。** 买断权益现在**纯粹是"去广告"**，权益边界清晰：¥68 买的是无广告体验，**不包含也不影响 AI**（AI 只取决于用户是否配置了自己的 Key）。

##### 为什么是「一次性买断」而不是「订阅」

这是与 v1.1 一脉相承、且在广告模式下更加确定的选择：

1. **无账号体系下，订阅的续费管理需要服务端**，直接违背 NG9（不搭应用服务器）。而一次性购买的平台收据校验是**无状态**的——只需校验"这张收据是否存在且未被退款"，不需要任何服务器记忆。
2. **备考是"一次性、有明确终点"的需求**。用户的心智是"我花一次钱，把这个考试搞定"，而不是"我要长期订阅一个备考 App"。订阅制在备考品类会引起强烈的抗拒感（"考完还得去退订"）。
3. **广告模式下，我们不需要订阅来保证 LTV**。免费用户本身就在贡献广告收入，去广告是"锦上添花"而非"生存必需"。这让我们可以用最没有攻击性的方式定价。

##### 权益的边界（重要）

| 买断包含            | 买断**不**包含                  | 为什么                     |
| --------------- | -------------------------- | ---------------------- |
| 移除全部广告          | **AI 调用费用**                | 真实边际成本，必须自付（见下方说明）     |
| AI 额度提升 | ~~无限 AI 额度~~               | **v1.5 已删除该权益项**。AI 本就不限次（BYOK 由用户自己付费），无需也不可能由我们来"提升" |
| 自建题库容量 3000 → 10 万题 | 云存储                      | 本产品无云存储（M7 存本地）        |
| 奖励中心全部解锁        | 模考/题库等**已全部免费**的功能，无额外内容可卖 | 核心能力本就免费，不存在"买断解锁内容" |

> ⚠️ **AI 成本必须单独说明**：LLM 调用是真实的外部成本（每次几分钱到几毛钱）。如果把"无限 AI"打包进 ¥68 的买断里，**必然亏损**（重度用户一个月能烧掉几百次调用）。因此处理方式是：**买断不捆绑任何 AI**。用户自备 Key 走 BYOK，**"无限 AI"由用户自己的服务商账号提供，与我们无关**——这是唯一成本可控的做法。
>
> **诚实是这里的唯一正解**：与其承诺"无限 AI"然后在暗中限流（用户发现后信任崩塌），不如一开始就说清"**AI 需要你自己配一个 Key，配了就不限次，费用直接付给服务商**"。

##### 反暗黑模式合规（对应 §5.3）

| 禁止行为                          | 本设计如何避免                     |
| ----------------------------- | --------------------------- |
| 学习过程中弹窗逼单                     | T1 铁律，学习流程零广告零引导（硬约束）        |
| 虚假稀缺（"仅剩 3 个名额"）              | 不使用任何稀缺性话术                  |
| 隐形默认勾选                        | 无订阅，不存在续费陷阱                 |
| 隐藏取消入口                        | 无订阅可取消；购买状态可在「我的」一级页面查看与恢复  |
| 诱导性文案（"不买就过不了"）               | 全部文案法务过审；禁止将购买与通过率关联（NG3）   |
| **用广告惩罚不付费用户**（如"不看广告就不给复习"） | ❌ 明确禁止。不看广告的唯一后果是"没有额外奖励"    |

---

#### M9-F6 🔒 无账号下的购买、恢复与风控（本模式的技术难点）

> **这是整个广告模式中最容易翻车的地方。** 无账号 = 没有"用户身份"可用于绑定购买记录。一旦处理不当，用户的体验是**"我花了 68 块钱，换手机就没了"**——这不只是差评，是可能引发消费者投诉的严重问题。

##### 核心原则：**把"购买身份"完整托管给平台，我们只做本地缓存**

```
     用户点击「去广告 · 本考季 ¥68」
              │
              ▼
   ┌──────────────────────────┐
   │ 平台 IAP（不可绕过）        │   ← 唯一真实的"身份"来源
   │ · iOS: StoreKit 2        │      平台持有 Apple ID / 华为账号等
   │ · 华为/小米/OPPO/vivo     │      = 用户实际的身份凭证
   └──────────┬───────────────┘
              │ 返回交易凭据（JWS 签名收据 / 平台订单号）
              ▼
   ┌──────────────────────────┐
   │ 本地校验（离线可验）        │   ← ① 本地验签（StoreKit 2 JWS 可离线验签）
   │ · 签名有效性              │      ② 记录 transactionId + originalTransactionId
   │ · productId 匹配          │      ③ 防重放：transactionId 去重
   │ · 有效期计算              │
   └──────────┬───────────────┘
              │ 通过
              ▼
   ┌──────────────────────────┐
   │ 写入本地 entitlement      │   ← 无账号架构下唯一的"会员状态"存储地
   │ · 加密存储（见下）         │
   │ · 同时写入系统钥匙串（备份） │
   └──────────┬───────────────┘
              │
              ▼
        全 App 广告位关闭
```

##### 1. 购买流程（三条硬要求）

| 要求               | 实现                                                                                |
| ---------------- | --------------------------------------------------------------------------------- |
| **平台 IAP 为唯一支付通道** | 不接入任何第三方支付（微信/支付宝直连）。**这是应用商店的强制要求**，绕过会导致下架。iOS 必须走 StoreKit，国内安卓渠道走各商店的 IAP。 |
| **购买前不要求任何注册**     | 点击购买 → 直接拉起平台支付 → 完成。**全程不出现"请先注册/登录"**（否则违背 NG9，且平台也不允许）                      |
| **购买后立即生效 + 明示**  | 购买成功后弹出确认：「✅ 已开启纯净模式 · 有效期至 2026-12-31 · 换设备可用「恢复购买」找回」——**主动教会用户恢复购买**，而不是等他出问题 |

##### 2. 「恢复购买」：本模式的生死入口 🔒

> 这是**法律与平台双重强制要求**（App Store 审核指南 3.1.1 / 各安卓商店同类条款），也是无账号架构下用户换机时的**唯一救命稻草**。

**入口必须放在一级页面**（不是设置页深处）：

```
「我的」页（一级 Tab）
├── 👤 账号与购买状态
│     ├── 当前状态：✅ 已购买「去广告 · 本考季」（至 2026-12-31）
│     │              或  ○ 未购买（免费版，广告支持）
│     ├── 🔄 [恢复购买]  ← 醒目按钮，永远可点，无论当前是否已解锁
│     ├── 📄 [购买记录]  （显示本机检测到的交易凭据摘要）
│     └── ❓ [为什么需要"恢复购买"？]  ← 帮助说明
```

**"恢复购买"的完整逻辑**：

```
用户在新设备点击「恢复购买」
        │
        ▼
  请求平台账号下的历史交易（需联网）
        │
   ┌────┴────┬──────────────┐
   ▼         ▼              ▼
 有有效交易   无交易        网络不可用
   │         │              │
   ▼         ▼              ▼
 校验签名   明确告知：     提示"请连接网络后重试"
 → 写入本地  "未在该账号下   并给出替代路径：
 → 提示成功   找到购买记录。   "如果你有历史备份文件，
             可能原因：       也可以从备份恢复购买状态"
             ① 换了 Apple ID  （见下）
             ② 未完成支付
             ③ 购买的是另一版本"
             → 提供 [联系客服]
```

**三种失败场景的兜底设计**（这是最体现产品成熟度的地方）：

| 场景                    | 用户看到什么                                                    | 兜底方案                                      |
| --------------------- | --------------------------------------------------------- | ----------------------------------------- |
| 换了 Apple ID / 平台账号    | 明确说明原因（不笼统报错），引导用户回忆当年购买时用的账号                             | 提供「凭支付凭证找回」人工通道（见下）                       |
| 恢复成功但本地已解锁（重复购买风险）    | **购买按钮自动变为「已解锁」不可点**，防止重复购买                                | 购买前先 `restore` 一次，检测到已有有效交易则直接解锁而非再次扣款     |
| 平台不可用（如国内安卓某商店停服）     | 提前在 v1.1 就设计**"购买状态随备份文件迁移"** 作为兜底（见下）                      | —                                         |

##### 3. 🔑 无账号架构独有的兜底：**购买状态随备份文件迁移**

这是本产品的一个**独创性设计**，直接解决"平台账号丢失/渠道替换导致购买丢失"的最坏情况：

1. 用户导出备份文件（`.jjbak`，M10-F3）时，**可选**勾选「包含购买凭据」。
2. 勾选后，备份文件中会写入一条**加密的购买凭据记录**：
   - 交易 ID（`transactionId` / `originalTransactionId`）
   - 购买时间、商品 ID、有效期
   - **平台原始收据的哈希**（不存储完整收据，避免泄露）
3. 新设备「从备份恢复」时，检测到凭据 → 尝试联网向平台**二次校验该交易 ID 是否真实有效** → 校验通过则恢复解锁状态。
4. ⚠️ **安全边界**：**这条凭据不能单独用于解锁**，必须通过平台二次校验。原因：
   - 备份文件可能被分享/泄露，若可单独解锁 = 破解者的免费通行证
   - 平台二次校验是无状态的（只查交易 ID 有效性），不违背 NG9
5. **校验失败时**（网络不可用 / 交易已被退款）→ 降级为未解锁，但**保留"待校验"状态**，下次联网时自动重试，并告知用户"购买状态待确认，联网后将自动恢复"。

##### 4. 离线宽限期（防止断网误锁）

| 状态                        | 处理                                                       |
| ------------------------- | -------------------------------------------------------- |
| 已购买 + 有网络                 | 正常使用，每次启动时静默校验（不打扰用户）                                     |
| 已购买 + **连续 7 天以上未联网**      | 仍保持解锁（**不做强制锁**）。本地记录 `lastVerifiedAt`，超过 30 天未校验时，在「我的」页显示温和提示：「已为你保持解锁状态，联网后将自动校验，请放心使用」 |
| 已购买 + 平台明确返回"交易已退款"       | 撤销解锁，但**给出明确说明和申诉入口**，不用静默锁                                        |

> **设计哲学**：宁可放过少量盗版，也绝不误伤一个付费用户。**误锁付费用户的代价（一星差评 + 退款 + 口碑损伤）远高于盗版损失。** 这与 R-12 的"接受一定破解率，把它当作获客"一致。

##### 5. 防篡改与风控（务实，不偏执）

| 措施                | 说明                                       |
| ----------------- | ---------------------------------------- |
| 本地 entitlement 加密 | 使用平台安全存储（iOS Keychain / Android Keystore）保存，非明文 |
| 双写冗余              | 加密 DB + 钥匙串双写；任一损坏可用另一个恢复                |
| 收据签名校验            | StoreKit 2 的 JWS 可在**本地离线验签**，无需服务器        |
| 定期抽查复核            | 每次启动 + 每次联网时静默复核，发现异常（如系统时间被大幅回拨）→ 记录但不打断用户 |
| 劫持操作系统的极端情况        | 不投入资源对抗（成本无底洞）。**接受一定比例的破解，把它当作"免费获客"**（R-12） |
| 风控红线              | ❌ 绝不因怀疑盗版而静默降级用户体验；❌ 绝不上传设备指纹做"黑名单"      |

##### 6. 合规要点（法务必读）

| 要求                                 | 落地                                                  |
| ---------------------------------- | --------------------------------------------------- |
| **平台内购政策**                       | 虚拟商品必须走平台 IAP。**严禁引导用户到 App 外支付**（App Store 审核指南 3.1.1） |
| **价格透明**                           | 购买页明确展示：价格、有效期、包含内容、**不包含内容**（AI 费用另计）              |
| **退款指引**                           | 明确告知用户可在平台申请退款（iOS：reportaproblem.apple.com），并提供客服入口 |
| **恢复购买（强制）**                       | 一级页面常驻入口（见上）                                        |
| **未成年人保护**                         | 支付需平台账号验证；不去诱导性设计吸引未成年人付费                           |
| **《广告法》第二十四条**                     | 广告内容不得与"通过率""保过"关联（NG3）                              |
| **广告标识（《互联网广告管理办法》）**              | 所有广告内容**显著标注"广告"**；激励视频需明确告知时长与奖励内容                  |
| **个人信息保护法 - 广告标识符**                | ⚠️ 若接入广告联盟需用 IDFA/OAID，**必须弹窗征求同意**（iOS ATT）。同意率预计仅 25-40%，需按此估算收入 |
| **不投放个性化广告（可选增强）**                 | 若完全不使用设备广告标识符，可做"非个性化广告"（eCPM 更低但零隐私争议）——**与产品隐私卖点一致，值得考虑** |

> 🎯 **一个重要的战略建议**：本产品的核心卖点之一是"你的数据只存在你的手机里"。如果接入广告联盟后大量使用设备标识符做定向投放，**这个卖点会被削弱**。建议优先选择**支持非个性化投放的广告平台**，接受 eCPM 低 20-30% 的代价，换取品牌一致性。这是**长期的正确选择**。

---

#### M9-F7 🔄 AI 服务的定位：纯 BYOK · 可选增强（v1.5 定稿）

> **本节的定位（不要跳读）**：v1.5 撤销了 v1.4 的"中转双路径"，AI 回归**纯 BYOK**。
> 请特别理解两件事：
> 1. **为什么撤销** —— 不是中转不可行，而是它与本产品的架构基石（NG9 无服务端）根本冲突，对个人开发者是不可承受的长期运维负担。
> 2. **撤销后怎么补** —— 用三个**零服务端成本**的方案（静态示例 + 图文教程 + 诚实的可选定性）接管中转原本承担的"让用户看见 AI 价值"的职责。

##### 一、最终形态：单一 BYOK 路径

**核心定义**：用户填入自己的 API Key，App **直连**服务商（OpenAI 兼容协议）。**请求不经过我们的任何服务器。**

| 项 | 说明 |
| --- | --- |
| **接入方式** | 用户在设置页选择服务商 + 填入自己的 API Key，Key 加密存储于系统钥匙串（iOS Keychain / Android Keystore） |
| **调用链路** | App **直连** LLM 服务商。**我们不中转、不代理、不接收、不存储** |
| **成本归属** | 用户直接向服务商付费。**我们零成本、零风险、零责任** |
| **额度** | **无限制**（用户自己付钱，我们没有任何理由限制）。**平台不存在"额度"这一概念** |
| **隐私优势** | 请求直达厂商，我们不经手、不留存、看不到内容——**隐私叙事的最强形态**（v1.4 曾因中转失去这个卖点，v1.5 重新拿回） |
| **可用性前提** | **必须已配置 Key**。未配置时 AI 不可用——**这是设计取舍，不是缺陷**（见第三节） |

##### 二、AI 的产品定位：可选增强，而非核心功能

> 🔧 **v1.5 最重要的产品判断。** 它决定了所有 AI 相关的文案、交互与验收标准。

| 维度 | v1.4 口径 | **v1.5 口径（须严格执行）** |
| --- | --- | --- |
| AI 的地位 | 三大核心之一（学习科学 + 机考 + AI 私教） | **核心之外的增强项**。产品核心 = 刷题 + 错题本 + 记忆曲线 + 全真模考 |
| 未配置 Key | 视为转化漏斗断点，必须用中转兜住 | **视为正常状态**。不设"触达率"目标，不做诱导性设计 |
| 必须明示 | — | **「不配置不影响任何学习功能」**（代码常量 `AI_IS_OPTIONAL_NOTICE`，**不可删除**） |
| 宣传口径 | 可作核心卖点 | **不得作为核心卖点**（NG16）。商店描述、引导页均须带"可选增强"定性 |

**为什么必须这么诚实**：我们不再提供免费额度了。如果不把"AI 是可选的"说清楚，用户会把它理解为"这个 App 的 AI 要收费/要我用别的平台" —— **那是误解，而误解会变成差评**。主动说清楚，反而降低预期落差。

##### 三、零服务端成本的补偿设计（v1.5 新增核心内容）

中转在 v1.4 里承担的唯一职责是：**让用户看见"配了 Key 能换来什么"，从而产生配置动力**。
v1.5 用三个**不需要任何服务器**的方案完成同一件事：

###### 方案 ①　静态 AI 示例解析（`AiSamplePage`）

内置几组**真实考点**的样例问答，打包进 App，**零网络请求、不需要 Key、人人可看**。

| 示例组 | 解决的痛点 | 样例内容 |
| --- | --- | --- |
| **把教材黑话翻译成大白话** | 术语看不懂就卡住，是在职备考最常见卡点 | 「用大白话解释挤出效应」「需求价格弹性到底在说什么」 |
| **编口诀硬记知识点** | 纯背诵条目靠反复读记不住 | 「编个口诀记住反倾销措施的三种类型」 |
| **错因诊断：我到底错在哪** | 比"答案是 C"更有用的是指出哪个知识点没打通 | 「这题我选了 D，为什么错」（统计性歧视 vs 工资歧视） |

**实现要求**：
- 数据放在 `src/domain/ai/samples.ts`，纯常量，**不发起任何网络请求**。
- 页面顶部**必须**标注「这是本地预置的示例，不是 AI 现场生成」—— 避免用户误认为 AI 已可用，也避免我们为样例内容的准确性背书。
- 页面底部给出转化入口（"想让它回答你自己的问题？去配置"）+ **再次明示可选性**。

###### 方案 ②　Key 配置图文教程（`AiKeyTutorialPage`）

BYOK 的真实门槛不是技术，是**认知** —— 很多在职考生从没听过 API Key，看到输入框就退出了。

| 内容块 | 作用 |
| --- | --- |
| **「API Key 是什么」开篇** | 用"钥匙"类比说清楚：它证明调用是你发起的，服务商凭它计费。**强调我们不参与** |
| **四步流程** | 注册账号 → 找到 API Key 页 → 创建并复制（提示只显示一次）→ 回 App 粘贴测试 |
| **按服务商给的申请入口** | 每家可展开，含场景标签与「去申请 Key ↗」直达控制台按钮 |
| **常见报错对照表** | Key 无效 / 网络失败 / 频率限制 / 余额不足 / 换设备失效 / 不配是否影响备考 |
| **安全说明** | Key 只存本机、不经我们；**没有账号系统所以无从获取你的 Key** |

**纪律**：
- 全部纯静态，**零网络请求**。
- **不承诺任何服务商的赠金/免费额度** —— 政策随时会变，说死了要背锅。
- 文案里不出现"免费"二字来误导（BYOK 的真实成本由用户付给服务商，必须说清）。

###### 方案 ③　三处明示「不影响学习」

| 位置 | 文案 |
| --- | --- |
| **AI 设置页顶部** | 「AI 是可选增强功能。不配置 Key 也能完整使用刷题、错题本、记忆曲线、模考等全部学习功能。」 |
| **AI 对话页状态条** | 未配置时显示「未配置 · AI 为可选功能」，而非报错态 |
| **示例页底部** | 再次重申可选性 |

> 💡 **这三处文案属于产品诚实性的一部分，验收时必须核对，不得因"简洁"而删除。**

###### 方案 ④　（可选）服务商邀请返利

部分 AI 服务商有邀请返利机制。**是否启用见 §16 Q21 第③条**（v1.5 建议：**先按"无返利"上线**，合规确认后再接入）。

**若启用，必须遵守四条合规要求**：
1. 跳转前**明确告知**这是推广链接（可用小字标注「推广」），不得隐瞒。
2. **不得因返利而推荐更贵或更差的服务商** —— 排序必须基于客观评分卡。
3. 返利收入**不改变用户价格**，用户在任何渠道开户价格相同。
4. 需在隐私政策中披露该合作关系。

##### 四、服务商列表：用户看到什么

**列表展示给用户的（必须去技术化）**：

| 服务商 | 展示标签 | 场景建议（写给用户看） |
| --- | --- | --- |
| **DeepSeek** | ⚡ 快 · 💰 便宜 | 「日常出题、解析错题，性价比最高」（**默认首选**） |
| **Kimi** | 📚 长文强 | 「需要读大段教材原文、做长材料分析时选它」 |
| **通义千问** | 🏛 法规准 | 「问法条、问政策原文类的题目，准确度更稳」 |
| **智谱 GLM** | 🧠 推理稳 | 「需要多步推导的题目（如计算题思路）表现好」 |
| **OpenAI** | 🌐 通用 | 「已有账号的用户可直接接入」 |

> ⚠️ **排序规则（内部原则，不可受商务影响）**：默认位次由**客观评分卡**决定 —— 中文能力 40% / 单次成本 30% / 稳定性与响应速度 30%。**返利比例不得进入排序权重。** 排序函数（`rankProviders`）中不得出现任何商业参数 —— 这条要求在代码层面固化，而不只是文档约定。

##### 五、配置向导（把"搭桥"做到位）

```
设置 → AI 服务设置（单卡片布局）
├── ⓘ 「AI 是可选增强功能」明示块（含「先看看 AI 能帮我做什么 ›」入口）
│
├── 📊 状态条：已配置 · DeepSeek  /  尚未配置
│
├── 🎯 第一步 · 选服务商
│    ┌─────────────────────────────────┐
│    │ ○ DeepSeek  ○ 通义千问           │
│    │ ○ Kimi      ○ 智谱 GLM          │
│    │ ○ OpenAI                        │
│    │ （点击即自动填好接口地址与推荐模型）│
│    └─────────────────────────────────┘
│
├── 🔑 第二步 · 填 API Key
│    ├── [ 不知道怎么申请 Key？看图文教程 › ]   ← 跳 AiKeyTutorialPage
│    ├── [ 打开 DeepSeek 的 Key 管理页 ↗ ]      ← 直达控制台
│    ├── API Key  [____________________] [显示/隐藏]
│    ├── 模型     [ deepseek-chat       ]
│    └── [ 测试连接 ]  [ 保存并启用 ]
│
├── 💰 关于费用
│    │  本 App        → 不收费
│    │  AI 调用       → 由你直接向服务商支付
│    └  备考场景调用量很小，多数服务商有新用户赠送额度
│
└── 🔒 免责声明
     「AI 功能由你选择的第三方服务商提供，请求由你的设备直接发送给
       该服务商，我们不接收、不存储你的任何对话内容。其输出内容
       不代表本产品的立场，我们不对其准确性负责。」（法务必审）
```

##### 六、未配置 Key 时的降级路径（不得出现死胡同）

| 场景 | 处理方式 |
| --- | --- |
| **用户点开 AI 页但未配置** | **不是错误态**。状态条显示「未配置 · AI 为可选功能」；消息区首条为欢迎语 + **示例页入口卡片**（"AI 到底能帮我做什么？"）；发送时轻提示「AI 是可选功能，配置 Key 后即可使用」并引导到设置页 |
| **AI 请求失败** | 按 `AiError` 分类给可读文案（未配置 / 鉴权失败 / 限流 / 网络 / 服务商异常）。**任何 AI 异常都不得冒泡为页面级错误**（NG16 技术硬约束） |
| **离线** | AI 不可用（需联网），但**其余全部功能照常**（本地优先架构的既有承诺） |
| **关于"额度用完"** | **此场景在 v1.5 中不存在** —— 无平台额度。用户自己的服务商账号若余额不足，属该服务商侧的提示，我们在错误文案中如实转达 |

##### 七、这个调整的取舍（诚实评估）

> **放弃的东西**：
> - **新手零门槛体验** —— 未配置用户确实用不了 AI（v1.4 承诺的"100% 新用户都能体验"作废）。
> - **广告换额度的收入线** —— 没有额度体系，这条线关闭。
>
> **换来的东西**：
> - **零运维** —— 没有服务要盯，没有凌晨三点的告警。
> - **零法务风险** —— R-20（备案义务）随中转一起撤销。
> - **零持续成本** —— AI 成本永久为 ¥0，不受用户规模影响。
> - **隐私叙事完整** —— "我们的服务器不在 AI 链路上"重新成为**无条件的真话**。
>
> **净判断**：**对一个个人开发者，这笔交换是划算的。** v1.4 的错误在于用"有团队"的成本模型（¥1.0/用户的小额获客成本）去论证一个"个人开发"的决策，却忽略了这个方案真实占用的不是钱，而是**长期的心力**。
>
> **至于"AI 没人用怎么办"** —— 接受它。**核心价值是刷题、错题、记忆曲线、模考**，AI 是锦上添花。用三个零成本方案把价值展示清楚，愿意配的人自然会配；不愿意配的人，本来也不是 AI 的目标用户。**追求"让所有人都用上 AI"本身就是 v1.4 的判断错误。**

##### 八、UI 要求（汇总给设计）

1. 设置页「AI 服务设置」为**单卡片布局**（不再是 v1.4 的双卡片）：
   「可选增强」明示块 → 状态条 → 第一步选服务商 → 第二步填 Key（含教程与直达控制台入口）→ 费用说明 → 免责声明。
2. **不得出现任何"剩余次数/额度"UI** —— 我们没有额度，假装知道用户还剩多少次是错的。
3. AI 对话页状态条**常驻显示**：已配置则显示服务商名（小字，如「DeepSeek」），未配置则显示「未配置 · AI 为可选功能」。
4. 未配置时，对话页显示**示例页入口卡片**（引导种草），而非报错或空白。
5. 教程页与示例页均为**独立路由**，可被外部链接直达（分享场景）。

#### M9-F8 「上岸保障」的广告模式适配

v1.1 的「上岸保障」（全额退款）**是基于付费的前提**。广告模式下，绝大多数用户没付过钱，**退款承诺失去意义**。改为：

> **「二战支持计划」**（替代 v1.1 的"上岸保障"）
>
> 对**已购买「去广告」**的用户：若 2026 年度两科均未达到合格线（84 分），凭**官方成绩截图**申请，**全额退还去广告费用（¥68）**，且**赠送下一年度的去广告权限**（跨考季支持）。
>
> 判定条件保持极简：**两科成绩均 < 84 分**，凭截图即可，不验证学习过程——条件越简单，用户对承诺的信任度越高，纠纷越少。

- 对免费用户：无退款承诺（本就未付费），但提供「二战档案继承」功能（M1，免费）——**这是免费用户真正需要的东西**。
- 需法务审核文案措辞（避免"保过"表述）。
- 运营成本：需人工处理，预估占比 < 3%（因为只有付费购买者才适用）。

---

#### M9-F9 商业化原则（v1.3 更新）

1. **学习过程中零广告零营销**（T1 铁律，§5.3 硬约束）。这不是"尽量少"，是**零**。唯一的被动曝光是开屏（占用等待时间，不占用学习时间）。
2. **不卖学习结果**（T3）、**不做除开屏外的被动打扰**（T2 修订版）。
3. **不卖"押题密卷"**（合规红线，NG3）。
4. **核心能力全免费**：题库、模考、复盘、复习调度、计划引擎、过线预测——不设任何付费门槛。
5. **位置少而精，而非多而碎**：宁可 4 个高价值位置，不要 8 个低价值位置。**削减广告位的优先级高于增加广告位。**
6. **广告的暴露必须可预期**：位置由少到多渐进释放（M9-F4），**永不出现"有→无→有"的突变**。
7. **不做 AI 额度生意**：AI 采用**纯 BYOK**——用户直连服务商，我们零成本、零中转、零额度。**红线：不经手任何请求、不留存任何对话内容、不做用户级画像、不为模型输出背书**（NG13）。
8. **隐私承诺作为核心卖点**在商店页与新手引导中突出：**"你的学习数据，只存在你的手机里。"** 广告投放优先选择非个性化方案，保持品牌一致性。
9. **用户随时可以体面退出**：不看广告的唯一后果是"没有额外奖励"，其余一切照旧；¥68 一次性买断即可永久移除全部广告。

---

#### M9-F10 收入结构与成本模型（财务参考，需实际数据校准）

> ⚠️ 以下为**建模参考值**，上线后必须用真实数据替换。所有 eCPM 均为保守估算区间。

**假设**（备考品类，中文市场，开屏 + 激励视频双引擎）：

| 参数             | 保守值        | 说明                          |
| -------------- | ---------- | --------------------------- |
| **开屏广告 eCPM**  | **¥15-30** | 开屏单价通常低于激励视频，但曝光量大且可预测      |
| 激励视频 eCPM      | ¥25-45     | 教育类目偏低，按保守取 ¥30             |
| 单次开屏收益         | ¥0.015-0.03 | 按 eCPM ¥22 / 1000 次        |
| 单次激励视频收益       | ¥0.03-0.06 | 按 eCPM ¥30 / 1000 次 / 15秒  |
| 人均日开屏曝光（活跃用户）  | 4.2 次      | 受 M9-F4 上限 12 次约束，实测取均值     |
| 人均日激励视频（活跃用户）  | 1.4 次      | 受频次上限约束，保守估计（v1.2 估 2.1 次偏乐观） |
| 去广告购买率         | 1.5-3%     | 保守取 2%（行业工具类免费 App 参考）      |

**月度收入模型（假设 10 万 MAU）**：

| 收入项          | 计算                                 | 月收入              |
| ------------ | ---------------------------------- | ---------------- |
| **开屏广告**     | 10万 × 45% 活跃 × 30天 × 4.2次 × ¥0.022 | **≈ ¥124,700**   |
| 激励视频         | 10万 × 45% 活跃 × 30天 × 1.4次 × ¥0.04  | ≈ ¥75,600        |
| 去广告（一次性）     | 10万 × 2% × ¥68（摊到 3 个月）            | ≈ ¥45,300/月      |
| **合计**       | —                                  | **≈ ¥245,600/月** |

**对应成本**：

| 成本项             | 估算                            | 说明                          |
| --------------- | ----------------------------- | --------------------------- |
| **AI 服务成本** | **¥0（永久）**                  | **v1.5 撤销中转后归零**，且不随用户规模变化。BYOK 下调用成本由用户直接向服务商支付 |
| AI 服务成本（BYOK 路径） | **¥0**                        | 用户自付，我们不经手                  |
| 缓存与降本后实际成本 | 上式的 **60-70%**                | 响应缓存 + 上下文裁剪显著降低成本          |
| 广告 SDK / 服务     | 按收入分成 0%（直连广告平台）             | —（若接联盟会有分成）                   |
| 崩溃监控等 SaaS      | ≈ ¥2,000/月                    | —                           |

**AI 成本（v1.5：永久归零，且不随规模变化）**：

| 月新增用户 | AI 成本（本产品） | 说明 |
| -------- | -------------- | --- |
| 1 万      | **¥0**         | AI 调用费用由用户直接向服务商支付，**不经过我们** |
| 10 万     | **¥0**         | 同上（成本不随用户规模线性放大——这是纯 BYOK 的核心优势） |
| 100 万    | **¥0**         | 同上 |

> ✅ **v1.4 → v1.5 的财务权衡（诚实交代）**：
>
> | 维度 | v1.4（双路径） | **v1.5（纯 BYOK）** | 判断 |
> | --- | --- | --- | --- |
> | AI 成本 | ≈ ¥1.0/新用户 | **¥0（永久）** | ✅ **成本归零，且永不受规模影响** |
> | AI 功能实际使用率 | 接近 100% 新用户 | **仅愿意配置 Key 的用户**（比例未知，不设目标） | ⚠️ **这是明确的取舍：放弃触达率，换零成本与零运维** |
> | AI 相关广告收入 | 有（额度耗尽 → 看视频续期） | **无**（无额度体系，该广告位撤销） | ⚠️ 收入略降，但**同步消除了"看视频换额度净亏"的问题** |
> | 合规复杂度 | 中（转发层 + 分路径披露 + 不留存证明） | **低**（我们不在链路上） | ✅ **R-20 撤销** |
> | 运维负担 | 需 7×24 保障转发层可用 | **零运维** | ✅ **对个人开发者是决定性优势** |
>
> **结论**：v1.4 用 ¥1.0/新用户买"每个用户都体验过 AI"，账算得没错，**但它默认了"有人值守"这个前提**。v1.5 承认这个前提对个人开发者不成立 —— 我们放弃触达率，换回**零成本 + 零运维 + 零法务风险**。
>
> **"AI 没人用怎么办"的答案**：接受它。用三个零成本方案（静态示例 / 图文教程 / 诚实明示）把价值展示清楚，**愿意配的人自然会配**。核心价值始终是刷题、错题、记忆曲线与模考 —— AI 是锦上添花，不是救命稻草。

**关键财务指标（需持续监控）**：

| 指标                    | 目标         | 红线             |
| --------------------- | ---------- | -------------- |
| ARPDAU（日活跃用户平均收入）     | ≥ ¥0.15    | < ¥0.08 需复盘    |
| **开屏广告收入占比**          | 40-45%     | > 60% 说明结构过于依赖单一位置 |
| **BYOK 配置率**（活跃用户中）🔁v1.5 | **监控即可，不设目标** | **不作为 KPI**——AI 是可选增强，配置率低不等于产品有问题 |
| ~~中转额度消耗率~~（v1.4） | ⛔ **已删除** | 无额度体系，该指标失去意义 |
| ~~AI 中转成本占比~~（v1.4） | ⛔ **已删除** | AI 成本恒为 ¥0，无需监控 |
| 去广告购买率（累计 3 个月内）      | ≥ 3%       | —              |
| 因广告导致的卸载率             | < 1%       | > 3% 触发全盘复盘    |
| 广告相关差评占比（商店评论）        | < 5%       | > 10% 立即降频甚至下线 |
| **开屏跳过率**             | 监控即可（无红线）  | 若 > 95% 说明素材质量差 |

---

#### M9-F11 🔒 离线与弱网下的广告行为（本地架构的必答题）

> 本产品的核心使用场景包含**地铁通勤、飞机、电梯、老小区弱网**——这些恰恰是"每天碎片时间学习"的主战场。而广告 SDK 是**唯一依赖网络的非核心组件**。
>
> **铁律：广告加载失败，绝不能让任何一个学习功能受影响。**

| 场景                       | 广告行为                                                   | 用户看到什么                            |
| ------------------------ | ------------------------------------------------------ | --------------------------------- |
| **完全离线**（飞行模式 / 无信号）     | **开屏直接跳过**，进 App；内容区广告位整体不渲染                            | 页面正常，没有任何"加载失败"的痕迹。用户根本不知道这里本该有广告 |
| **弱网**（加载超时 > 1.5s，开屏标准更严） | 开屏**直接跳过**；内容区静默放弃，不重试、不提示                            | 进 App 更快，用户甚至会觉得"这 App 挺快"        |
| **用户主动点击解锁，但加载失败**       | 明确告知："网络不稳定，**本次奖励直接发放给你**"——**失败即赠送，不惩罚用户** | 坦荡的处理方式，反而加信任分                    |
| **已购买去广告 + 离线**         | 广告位与解锁入口**完全不存在**                                      | 纯粹的 App                            |
| **广告加载中用户点了主按钮**（继续学习）  | **立即放弃广告加载**，优先响应主按钮                                   | 用户的意图永远优先于我们的收入                   |
| **后台恢复前台**               | **不展示开屏**（热启动不展示），不重新加载已放弃的广告位                        | —                                 |

> ⚠️ **开屏的超时标准必须比其他位置更严**（1.5 秒 vs 5 秒）。
>
> **理由**：开屏位于冷启动的关键路径上。如果广告加载要等 5 秒，用户看到的是"点了图标之后白屏 5 秒，然后才是广告"——**这是最糟糕的组合：既等了广告，又等了加载**。
>
> **正确行为**：请求广告的同时并行完成 App 初始化。**若 1.5 秒内广告未就绪 → 直接进 App，不展示开屏**。宁可少一次曝光，也不能让用户觉得"这 App 启动慢"。

> 💡 **"失败即赠送"这条值得强调**：当用户主动点了"看视频领奖励"、但广告因为网络原因加载不出来时，如果我们什么都不给，用户的第一反应是"白点了，这 App 真烂"。而如果我们直接赠送奖励，用户的反应是"这 App 挺讲道理"。**同一次技术故障，可以是一次扣分，也可以是一次加分。** 这点损失远小于口碑收益。

**必备的广告 SDK 兜底机制**：

1. **广告 SDK 崩溃隔离**：广告组件必须运行在独立的错误边界内，**SDK 崩溃不得导致 App 崩溃**（这是最常见的事故来源）。
2. **超时熔断**：单次广告加载超时 5 秒 → 放弃；同一会话内连续 3 次加载失败 → 本会话内禁用广告请求，不再尝试。
3. **不影响启动速度**：广告 SDK **延迟初始化**（用户首次到达可展示广告位时才初始化），**严禁在 App 启动时同步初始化**——冷启动 2s 的性能指标优先级高于广告预热。
4. **不阻塞渲染**：任何广告位都不参与首屏关键渲染路径。

**广告平台选型建议**（需研发 + 法务共同评估）：

| 考量维度     | 建议                                                    |
| -------- | ----------------------------------------------------- |
| **隐私一致性** | ⚠️ 优先选择**支持非个性化投放**的平台。本产品的核心卖点是"数据只在你手机里"，若为定向广告采集 IDFA/OAID，**卖点会被击穿**。接受 eCPM 低 20-30% 的代价 |
| 国内合规     | 必须选择具备《互联网广告管理办法》合规资质的平台；广告内容需可审核、可屏蔽特定类目          |
| 类目屏蔽     | **必须屏蔽**：教育培训竞品、金融理财、医美、游戏充值、贷款类——这些会直接损害备考用户的信任感   |
| 中介 vs 直连  | 优先直连（穿山甲/优量汇等），走中介网络会有多层分成与不可控的内容风险                  |
| 填充率      | 留存 30 天以上再评估；初期填充率可能仅 60-70%，需按此估算收入                  |

**数据外流边界更新（补充 §8.8.3）**：

| 数据                    | 流向     | 告知方式                         |
| --------------------- | ------ | ---------------------------- |
| 广告 SDK 采集的设备/网络信息（非个性化模式下为最小集） | 广告平台   | 隐私政策明示 + 首次启动时的广告说明（见下）      |
| **⚠️ 绝不上送**：学习数据、题目内容、答题记录 | —      | 广告 SDK 不得访问 App 任何业务数据（技术隔离强制） |

> ✅ **v1.5 说明（此段在 v1.4 曾被修订，现已恢复）**：v1.3 曾把"我们的服务器完全不在 AI 请求链路上"当作核心隐私卖点。v1.4 因引入中转路径，这个卖点一度在默认路径下不再成立。**v1.5 撤销中转后，该卖点重新成为无条件的真话**：
>
> - **v1.5 起（纯 BYOK，唯一路径）**：**所有** AI 对话都由你的设备直连服务商，**我们不接收、不转发、不存储**。隐私叙事完整无折损，且**不再需要任何"分路径披露"的复杂话术**（v1.4 的两个话术版本一并作废，降低了虚假宣传风险）。
>
> **这不意味着隐私优势需要重新论证**，而是**它现在是无条件的**：
>
> > 「你的学习数据只存在这部手机里。
> > 广告商拿不到你的任何学习信息。
> > 想获得最强隐私？**配置你自己的 AI Key** —— 你的对话直接发送给你自己的服务商，我们完全不经手。
> > 使用默认通道时，我们只做转发，**不留存、不分析你的任何对话内容**。」
>
> 💡 **这个修订反而带来一个正面的产品效果**：**隐私成为 BYOK 的价值主张**，而不只是一段说明文字。用户有了"为了隐私去配置 Key"的具体理由——**这把隐私从合规负担变成了 BYOK 的转化动力。**

**首次启动的广告告知（合规必需，且必须写成"人话"）**：

> 「这个 App 是免费的，**我们会展示少量广告来支付服务器和开发成本**。
>
> 广告只出现在你**完成学习任务之后**，**永远不会打断你的学习过程**。我们不会把你的学习数据分享给广告商。
>
> 如果你喜欢纯净的体验，可以花 ¥68 永久关闭全部广告。」
>
> `[ 我知道了 ]`

**这段文案本身就是一次品牌建设**——把商业模式坦诚地讲清楚，比藏着掖着更能赢得理解。备考用户是成年人，他们理解"免费的东西需要有人付钱"。

---

#### M9-F12 与其他模块的联动检查清单（研发落地用）

| 关联模块                    | 需要改动的内容                                          | 优先级 |
| ----------------------- | ------------------------------------------------ | --- |
| **M7-F4 自建题库容量**        | 「已激活用户 ≤10 万题」→ 改为「免费 3000 题；看激励视频临时扩容；买断用户 10 万题」 | P0  |
| **M7-F6 争议纠错奖励**        | 「积分兑换会员时长」→ 改为「积分兑换免广告天数」（无订阅概念）                  | P1  |
| **§7.0 功能地图**           | 底部 Tab「我的 → 订阅」→ 改为「**奖励中心**」（主入口）+ 购买状态入口         | P0  |
| **§10.2 组件规范**         | 「订阅等关键操作使用模态确认」→ 改为「购买去广告、恢复购买等关键操作使用模态确认」；**新增开屏广告组件规范** | P0  |
| **§5.3 反暗黑模式**          | 需同步修订：v1.2 写的是「❌ 不做开屏广告」→ **v1.3 改为允许开屏，但附带 10 条硬约束**（M9-F3 位置⓪），并保留「❌ 学习流程中的任何广告」 | P0  |
| **§12 非功能需求**           | 新增**开屏广告性能要求**：冷启动 ≤2s 的指标**优先于**广告加载；广告未就绪直接跳过                | P0  |
| **§13.2 指标体系**          | 「退订率」→ 替换为「广告相关卸载率 / 广告差评占比」；新增开屏曝光与跳过率指标          | P0  |
| **§13.3 数据口径**          | 新增：广告收入从**广告平台后台**获取（L2 层级，非埋点）                   | P1  |
| **§15 路线图**             | M9 描述由「广告 SDK 接入 + 去广告 IAP + 奖励中心」→ 更新为「**开屏 SDK + 激励视频 SDK + 渐进释放引擎 + 去广告 IAP + 奖励中心**」 | P0  |
| **§2.2 业务目标 B1**        | 「付费转化率 ≥12%」→ 改为「广告 ARPDAU + 去广告购买率」双目标；**ARPDAU 目标由 ¥0.11 上调至 ¥0.15** | P0  |
| **§2.1 用户目标 G1**        | 「付费用户两科通过率」→ 改为「**活跃学习用户**两科通过率」（不再区分付费）          | P0  |
| **M10-F6 设置与隐私**        | 新增：广告偏好设置、去广告购买状态、恢复购买入口；**新增 AI 服务设置（v1.5 单卡片：服务商选择 + Key 输入 + 图文教程入口 + 示例查看入口）** | P0  |
| **§8.8 AI 联网边界** ✅      | **v1.5 回归单一路径**：① 数据流向只有一种——**设备直连服务商**；② §8.8.3 数据外流表**删除分路径列**，恢复单列表述；③ §8.8.4 降级路径修正为"未配置 → 引导配置（含示例页种草），**不得出现死胡同**"；④ §8.8.5 删除全部中转专项要求 | P0  |
| **§8.7 成本红线**           | **v1.5 重新归零**：AI 成本 ¥0（永久），**删除全部止损闸与成本监控指标**（不再需要） | P0  |
| **§8.6.5 合规与免责**       | **v1.5 恢复单版本免责**：仅保留 BYOK 免责（"AI 由你选择的第三方服务商提供，请求由你的设备直接发送，我们不接收、不存储"）。**删除中转免责版本** | P0  |
| **§12 非功能需求**          | **v1.5 删除**中转转发层的全部非功能要求（无该组件）。新增一条**客户端硬约束**：**AI 相关异常必须被兜住，绝不可冒泡为页面级错误**（NG16） | P0  |
| **§3 非目标 NG13**          | 回归并强化"**不做 AI 额度生意、不经手任何 AI 请求**"；**NG14** 保留并扩围；**新增 NG15**（不做任何需要我方服务器在线的功能，架构总闸）与 **NG16**（不把 AI 当作产品的可靠承诺） | P0  |
| **§11 数据模型**            | **v1.5 简化**：`AIProvider` 为**内置常量**（非实体、不入库）；**删除 `AIQuota` 实体**（无额度概念）；仅保留 `AIConfig`（本地 Key 配置，存系统钥匙串，**不入业务库**) | P0  |
| **§16 待决问题 Q9**         | **v1.5 再次决断：纯 BYOK**（撤销 v1.4 的中转）。**本项已彻底关闭，不再重新评估**（除非产品从"个人开发"变为"有专职运维的团队产品"） | P1  |
| **§16 待决问题 Q21**        | ⛔ **v1.5 大幅降级**：随着 R-20 撤销（我们不在 AI 链路上），备案义务判定**不再是阻塞项**。仅保留一项低优先级事项：**服务商邀请返利的合规披露**（可先按"无返利"上线） | P2  |

---

> **📌 本节小结（给评审的一句话）**
>
> v1.3 的核心不是在"加广告"和"减广告"之间做取舍，而是**重构收入结构的形状**：
>
> **① 用确定性换空间**——开屏提供稳定的收入底盘，使我们**有能力主动砍掉**那些卡顿主流程的位置（结算页加练、内容页原生卡片）。**位置从 5 个减到 4 个，用户接触广告的"场景数"变少了。**
>
> **② 用一致性换信任**——取消"考前 7 天停广告"，改为**由少到多的渐进释放**。**永不出现"有→无→有"的突变**。用户对广告的认知全程稳定，不存在"被欺骗"的窗口。
>
> **③ 用低门槛换使用率（v1.4 修订）**——AI 从"必须自己配 Key 才能用"改为"**选个服务商点一下就能用**"。v1.3 的纯 BYOK 虽然成本归零，但也**等于把核心差异化功能事实上废弃了**。v1.4 用 ¥1.0/新用户的获客投入，**把 AI 真正交到每一个用户手里**，并用"额度耗尽 → 引导 BYOK / 看视频续期"完成漏斗接力。
>
> 三件事合起来的效果：**收入更稳、体验更可预期、核心功能真正可用**。
>
> ⚠️ **v1.4 唯一需要盯紧的指标**：**AI 中转成本占比**（红线 20%）。这是从 v1.3 的"零风险"回到"有成本"后必须守住的闸门。
### M10 数据存储、备份与设置 🔒（v1.1 重写，升级为 P0）

> **本地化后，本模块从"设置页"升级为产品的生命线。**  
> v1.0 里数据丢了服务端还有一份；v1.1 里**数据丢了就是真的没了**。用户做过的 8000 道题、攒了 3 个月的错题本、自己上传整理的题库——全在本地。备份不是锦上添花，是**产品责任**。

#### M10-F1 无账号，开箱即用

1. 无注册、无登录。首次启动完成 onboarding（§9.1）后直接进入首页。
2. 设备即身份。首次启动时生成 `device_id`（本地 UUID），仅用于本地数据标识与备份文件校验。
3. **换设备**通过「备份导出 → 导入」完成，不做自动同步（NG9）。

#### M10-F2 数据资产分级（决定备份策略）

| 级别       | 内容                                                       | 是否备份      | 说明                   |
| -------- | -------------------------------------------------------- | --------- | -------------------- |
| **核心数据** |                                                          |           |                      |
| （必须备份）   | 答题记录、FSRS 记忆状态、错题本、笔记、收藏、学习计划与完成记录、自建题库（结构化题目）、模考历史、设置偏好 | ✅ **必须**  | 体积约 5-50MB，丢失即"从头再来" |
| **大文件**  |                                                          |           |                      |
| （可选备份）   | 用户上传的 PDF/图片原件、AI 生成的导图图片                                | ⚠️ **可选** | 体积可达数百 MB，导出时让用户勾选   |
| **缓存**   |                                                          |           |                      |
| （不备份）    | 精讲内容缓存、AI 响应缓存、图片缓存                                      | ❌ 不备份     | 可重新生成/下载             |

#### M10-F3 三重备份机制（P0）

**① 本地自动快照（防误操作）**

- 触发：每日首次启动 App 时自动创建；单次答题超过 50 题时追加一次。
- 保留：滚动保留最近 **7 份** 每日快照 + **4 份** 每周快照。
- 位置：App 私有目录，用户不可见但可通过「备份与恢复」查看。
- 用途：用户误删错题本、误重置计划时，可**回滚到任意快照点**。
- 交互：「备份与恢复 → 时间机器」，按时间轴列出快照，显示"该时点：已答 1243 题 / 错题 87 / 计划进度 34%"，一键回滚。

**② 手动导出备份文件（防设备丢失）**

- 导出为单个文件：`经师AI备份_20260903_1243题.jjbak`
- 内容选择：核心数据（默认全选，必选）+ 大文件（默认不选，可勾选）
- 导出通道：调用系统分享面板 → 微信/邮件/网盘/本地存储/电脑传输
- 可选密码加密（AES-256）。**开启时必须强提示**："密码一旦遗忘将无法恢复，请务必记牢"。建议默认关闭加密（本地存储本身已具备隐私性）。
- **导出提醒机制**（关键）：
  - 用户累计答题 ≥ 500 题且从未导出过 → 温和提醒一次（非弹窗，首页卡片提示条，可关闭）
  - 距上次导出 ≥ 30 天 → 每月提醒一次
  - 大版本更新前 → 强制提醒（"更新前建议先备份"）

**③ 系统级云备份（被动兜底）**

- iOS：核心数据库文件**允许**被 iCloud 备份（默认开启），实现用户无感知的兜底恢复；大文件目录标记为 `excludedFromBackup`（避免撑爆用户 iCloud 空间）。
- Android：接入系统「应用数据备份」（Android Backup Service），同样只备份核心数据。
- 说明：这是**兜底**而非主方案（用户重装系统时可用），产品内仍需主动引导用户导出备份文件。

#### M10-F4 换机迁移

1. **主路径**：旧机导出备份文件 → 传输到新机 → 新机「从备份恢复」。
2. 传输方式建议（UI 引导，按推荐度排序）：
   - 微信/QQ 发给自己（最普及）
   - 系统分享到网盘（百度网盘/阿里云盘/iCloud Drive）
   - 数据线连电脑
3. **P1 增强**：**局域网直传**（旧机生成二维码，新机扫码，同一 WiFi 下点对点传输）——体验最好，无第三方依赖，建议 v1.1 排期。
4. 恢复流程：选择文件 → 校验完整性 → 展示备份摘要（"该备份包含 1243 题记录，创建于 2026-09-03，来自 iPhone 13"）→ 确认恢复 → 重启生效。
5. **冲突处理**：恢复是**覆盖式**的（不是合并）。需明确警告"恢复将覆盖当前设备上的所有学习数据"，避免用户误操作。

#### M10-F5 存储管理

- 「我的 → 存储管理」展示：数据库占用、附件占用、缓存占用，分别提供清理入口。
- **清理附件不影响已入库的结构化题目**（重要，避免用户误删后题目也丢了）。
- 缓存超 1GB 时提示清理。

#### M10-F6 设置与隐私

1. 设置项：推送时间与免打扰时段、字号、深色模式（自动/浅色/深色）、AI 服务设置（§M9-F7）、**广告偏好设置（v1.2 新增）**、**购买状态与恢复购买（v1.2 新增）**、存储管理、备份与恢复、隐私政策、关于。
2. **广告偏好设置（v1.2 新增）**：
   - 「个性化广告」开关（**默认关闭**，符合产品隐私定位）——注意：此项受平台 OAID/ATT 授权状态约束，用户拒绝系统级授权时此开关不可用
   - 「广告类目偏好」：允许用户排除特定类目（如金融、医美）
   - 「关闭广告」入口 → 跳转购买页（**不是隐藏入口，是坦荡的引导**）
   - 说明文案：**"我们不使用你的学习数据投放广告。广告来自公开的类目定向，与你的学习内容无关。"**
3. **购买状态与恢复购买（v1.2 新增，与 M9-F6 联动）**：
   - 「我的」**一级页面**常驻「账号与购买状态」入口（不是藏在设置里 —— 平台与法律双强制）
   - 显示：当前状态、有效期、`[恢复购买]` 按钮、购买记录摘要
   - 「为什么需要恢复购买？」帮助说明（用大白话讲清楚换设备场景）
4. **隐私合规（本地架构下大幅简化，成为卖点）**：
   - 无注册即无个人信息收集，用户协议与隐私政策大幅简化。
   - **首次启动明示**："本应用不收集任何个人信息，你的全部学习数据仅保存在本机。"
   - 数据删除：提供「清除全部数据」入口（恢复出厂状态）——**无需**账号注销流程。
   - 数据导出：备份文件即为数据导出，满足《个人信息保护法》的可携带权要求。
   - AI 数据边界（v1.5 单一路径）："题目文本与你输入的内容会**直接发送给你配置的 AI 服务商**，我们不经过、不留存"。**v1.5 无中转路径，因此不需要分路径说明**。这是本地架构下唯一的数据外流点，**必须显著告知**。
   - **广告数据边界（v1.2 新增）**：隐私政策中**单列「广告与第三方 SDK」章节**，明确说明：① 接入了哪些广告 SDK；② 它们采集什么（设备型号、网络状态、粗略地域——非个性化模式下不含标识符）；③ **明确承诺：你的学习数据、答题记录、上传资料、笔记，任何广告 SDK 都无法访问**（技术隔离，非承诺性表述）；④ 提供拒绝个性化的开关。

---

### M11 学习搭子打卡（P2，轻量）

匿名搭子匹配（不提供 IM）：随机匹配 3 位进度相近的考生，互相可见"今日是否完成"。**默认关闭**，用户主动开启。设计理由：社会促进效应（Social Facilitation）能提升坚持率，但 IM 会成为时间黑洞（NG4）。

---

## 8. AI 功能详细设计 ⭐

> **本章是产品的技术核心与最大风险点。**  
> AI 在备考场景的价值极高（24h 答疑、无限出题、个性化诊断），但风险同样极高——**AI 编造一个错误的税率，用户记到考场上就是丢分**。因此本章用一半篇幅讲能力，另一半讲治理。

### 8.1 AI 能力矩阵总览

| 编号     | AI 能力   | 一句话说明               | 优先级 | 技术路径                  |
| ------ | ------- | ------------------- | --- | --------------------- |
| **A1** | AI 出题引擎 | 从任意资料/知识点生成高质量题目    | P0  | RAG + 结构化生成 + 校验流水线   |
| **A2** | AI 解析引擎 | 逐项解析 + 陷阱分析 + 举一反三  | P0  | RAG + 多步推理            |
| **A3** | AI 学习教练 | 诊断薄弱点、动态调整计划、过线预测   | P0  | 数据模型 + 规则引擎 + LLM 解释层 |
| **A4** | AI 私教对话 | 24h 答疑、苏格拉底式追问、讲给我听 | P0  | RAG + 会话记忆 + 学情上下文注入  |
| **A5** | AI 记忆助手 | 生成口诀、联想记忆、记忆卡片、思维导图 | P1  | LLM + 模板              |
| **A6** | AI 考点预测 | 高频考点分析 + 考前必看清单     | P1  | 真题频次统计 + 大纲变动 diff    |
| **A7** | AI 审题训练 | 高亮题干关键词陷阱，训练审题习惯    | P2  | 序列标注 / LLM            |


### 8.2 A1 · AI 出题引擎

#### 8.2.1 输入与参数

| 输入类型  | 示例                             |
| ----- | ------------------------------ |
| 原始资料  | 上传的 PDF/图片/文本（教材章节、讲义、笔记、真题截图） |
| 指定知识点 | "给我出 5 道'财政政策工具'的题"            |
| 指定易混组 | "出几道能区分'财政政策'和'货币政策'的题"        |
| 错题反推  | "针对我最近错的 10 道题，出 5 道同类题"       |

| 参数   | 取值                         |
| ---- | -------------------------- |
| 题量   | 1-20                       |
| 题型   | 单选 / 多选 / 案例分析             |
| 认知层次 | 记忆 / 理解 / 应用 / 分析（布鲁姆分类）   |
| 出题风格 | 常规 / **辨析型** / 计算型 / 场景应用型 |
| 难度   | 简单 / 中等 / 困难 / 自适应         |

#### 8.2.2 生成流程（五步法）

```
Step 1 考点定位
  → 输入内容/知识点，检索知识树定位到具体知识点 ID
  → 获取该知识点的：考纲表述、教材原文、近5年真题、易错点库

Step 2 语料构建（RAG）
  → 检索 Top-20 相关教材片段（Chunk 512 tokens，overlap 64）
  → 检索该知识点历史真题 5-10 道（作为风格参考，非抄袭）
  → 检索易错点库（该考点的常见误解）

Step 3 结构化生成
  → Prompt 强制输出 JSON Schema：
    {
      stem, options[4-5], answer[], difficulty, bloom_level,
      explanation: { per_option: [...], key_point, trap_words },
      knowledge_points[], confidence, source_refs[]
    }
  → 温度参数 0.3（低随机性，提高事实准确性）
  → 干扰项设计约束（关键！）：
      ✗ 禁止"以上都对/以上都不对"（真实考试几乎不出现）
      ✗ 禁止长度明显异常的选项（最长的往往不像答案）
      ✓ 干扰项必须来自"真实易错点库"或"邻近概念"，确保有迷惑性

Step 4 自动校验（见 §8.6 校验流水线）
  → 事实校验 → 答案一致性校验 → 干扰项质量校验 → 去重

Step 5 呈现与入库
  → 预览卡片（含置信度与来源标注）→ 用户确认 → 入库
```

#### 8.2.3 出题质量红线（硬性约束）

| 红线                             | 处理方式                                          |
| ------------------------------ | --------------------------------------------- |
| 涉及**具体数字**的题（税率、准备金率、年限、金额、比例） | ⚠️ **强制走法规知识库校验**。无法从权威库验证的数字题**一律不生成**（宁缺毋错） |
| 涉及**法条原文**的题                   | 必须能引用到具体法规条目，否则不生成                            |
| 涉及时政/当年新增政策                    | 标注"时效性内容，请以最新教材为准"                            |
| 与已有题目 Embedding 相似度 > 0.92     | 判定重复，丢弃                                       |
| 干扰项可一眼排除（如表述荒谬）                | 判定低质，重新生成                                     |


### 8.3 A2 · AI 解析引擎

#### 解析输出结构（固定四段式）

```
┌─────────────────────────────────────┐
│ 正确答案：D                          │
├─────────────────────────────────────┤
│ 🎯 一句话考点                        │
│ 紧缩性财政政策 = 增收节支            │
├─────────────────────────────────────┤
│ 📖 逐项解析                          │
│ ✓ A 错误：降低税率属于扩张性政策...   │
│ ✓ B 错误：增加政府支出属于扩张性...   │
│ ✓ C 错误：提高存款准备金率属于货币... │
│    （这是最常见的混淆点，注意区分）   │
│ ✓ D 正确：减少政府购买直接压缩总需求  │
├─────────────────────────────────────┤
│ ⚠️ 题干陷阱词                        │
│ "紧缩性" ← 关键词，看到先圈出来      │
├─────────────────────────────────────┤
│ 🔍 举一反三                          │
│ [生成2道同类题]  [对比：财政vs货币]  │
├─────────────────────────────────────┤
│ 📚 来源：教材 P127 · 2026版 · 第三章 │
│ [追问 AI] [记笔记] [收藏]            │
└─────────────────────────────────────┘
```

#### 关键设计

1. **逐项解析是硬要求**：不允许出现"故选 D"这种无信息量解析。每个选项（含干扰项）都必须解释对错原因。
2. **来源溯源**：每道解析必须能追溯到教材章节或考纲条目。无出处的解析标记为"AI 推断"，降低置信度展示。
3. **陷阱词高亮**：题干中的限定词（"不属于""错误的""主要""直接"）自动高亮——这是经济师考试最高频的失分原因（审题不清）。
4. **追问入口**：解析看不懂直接进 AI 私教对话，**自动带入题目上下文**（不用用户重新描述题目）。

### 8.4 A3 · AI 学习教练（诊断与规划）

#### 输入数据

- 全量答题记录（题目、正误、用时、确信度、错误归因）
- 知识点掌握度矩阵（三级）
- 计划完成率、学习节律（每日活跃时段、平均容量）
- 模考成绩序列

#### 输出能力

**① 掌握度画像与热力图**

```
《经济基础知识》掌握度                    分值权重
  经济学基础    ████████░░ 78%   良好      29-30分
  财政          ████░░░░░░ 42%   薄弱 ⚠️    23-25分
  货币与金融    ██████░░░░ 63%   一般      22-23分
  统计          ███████░░░ 71%   一般      20-22分
  会计          ███░░░░░░░ 31%   薄弱 ⚠️    20-22分
  法律          █████████░ 85%   掌握      20-22分

💡 AI 诊断：
你的会计模块掌握度 31%，却占 20-22 分权重，
是当前最大的失分项。建议本周插入 3 次会计专项训练，
预计可提升 8-12 分。
[立即加入计划]
```

**② 遗忘预警**  
基于 FSRS 状态预测："你的『货币政策工具』考点将在 **3 天后**进入遗忘临界点（可提取性将降至 65%），已为你安排在 9 月 6 日复习。"

**③ 动态计划调整**  
用户说"今天只有 20 分钟"→ AI 立即重排：保留复习硬约束，砍掉新学，压缩错题量，并说明理由。

**④ 过线概率预测与杠杆分析**（见 M8-F2）

**⑤ 周报**：每周一推送「上周复盘 + 本周重点」，含数据 + AI 文字点评（不超过 150 字）。


### 8.5 A4 · AI 私教对话

#### 核心设计：上下文自动注入

用户从任何一道题/知识点进入对话时，系统自动注入上下文，用户不需要重新描述。

```
用户从题目详情页点「追问 AI」进入对话
→ 系统自动注入：题目全文、用户答案、正确答案、解析、该用户此题的历史记录
→ 输入框预填引导："关于这道题，你想问什么？"
→ 快捷问题 chips：[为什么A不对？] [换个说法讲讲] [相关考点有哪些？]
```

#### 苏格拉底式追问（Socratic Tutoring）

**不是直接给答案，而是引导。** 当用户答错时：

```
❌ 错误示范（填鸭）：
"答案是 D，因为减少政府购买属于紧缩性财政政策。"

✅ 正确示范（苏格拉底）：
"你选了 B（增加政府支出）。
先问你一个问题：政府增加支出，市场上的钱是变多了还是变少了？"
用户："变多了。"
"对。那题目问的是'紧缩性'，也就是要让市场上的钱___？"
用户："变少。"
"没错！那 B 显然走反了。再看看 D 选项？"
```

**切换规则**：用户提供"直接告诉我答案"或连续追问 3 轮仍未理解时，**立即切换为直接讲解模式**（避免用户产生挫败感）。

#### 对话能力清单

| 能力   | 示例                                           |
| ---- | -------------------------------------------- |
| 概念讲解 | "用大白话解释什么是挤出效应"                              |
| 对比辨析 | "财政政策和货币政策到底怎么区分？"                           |
| 计算辅导 | "这道现值计算的题怎么算？"（分步讲解，不直接给数）                   |
| 记忆技巧 | "帮我编个口诀记住反倾销措施"                              |
| 组卷   | "给我出 10 道我薄弱的多选题"                            |
| 学习建议 | "还剩 40 天，我该怎么安排？"                            |
| 情绪支持 | "学了三遍还是记不住，是不是我不行？"→ 共情 + 具体方法建议（**不做空洞鼓励**） |

#### 交互规范

1. **流式输出**（打字机效果），首字延迟 < 1.5 秒（感知性能关键）。
2. 回答中涉及知识点时，**自动渲染为可点击的知识卡片**（点击跳转精讲）。
3. 回答末尾提供 2-3 个**推荐追问 chips**（降低追问门槛）。
4. 长回答结构化：小标题 + 短段落 + 列表，禁止大段纯文字（移动端阅读）。
5. **引用标注**：涉及教材内容时标注出处（如"教材 P127"），可点击查看原文。


### 8.6 ⚠️ AI 准确性治理体系（产品生命线）

> **这是本章最重要的一节。** 备考产品的 AI 出错成本极高——不是"体验差一点"，而是用户记错知识点去考试丢分。必须建立工业化治理。

#### 8.6.1 内容四级可信度体系

所有题目/解析在 UI 上**必须**显示来源标签，用户有权知道内容从哪来。

| 等级      | 含义               | 生成方式           | UI 标识       | 校验要求           | 使用场景             |
| ------- | ---------------- | -------------- | ----------- | -------------- | ---------------- |
| **S 级** | 官方真题 / 教材原文      | 官方来源录入         | 🔵 蓝色「官方」   | 教研双人录入复核       | 真题演练、模考卷         |
| **A 级** | 教研编写             | 人工教研团队         | 🟢 绿色「已审」   | 教研审核 + 交叉校验    | 章节练、模考卷          |
| **B 级** | AI 生成 · 已校验      | AI 生成 + 自动校验通过 | 🟡 黄色「AI生成」 | 通过 §8.6.2 全部校验 | 练习、专项            |
| **C 级** | 用户自建 / AI 生成·未校验 | 用户上传或 AI 生成未过检 | ⚪ 灰色「自建/待验」 | 无              | **仅个人练习，禁止进入模考** |

**硬性规则**：

- 模考卷中 **S 级 + A 级合计 ≥ 90%**，B 级 ≤ 10%，**C 级 0%**（保证成绩预测有效性）
- 过线概率预测**只基于 S/A 级题目的表现**计算
- AI 生成题在练习中的占比默认 ≤ 30%，用户可手动调高（调高时提示风险）

#### 8.6.2 AI 生成内容校验流水线（B 级准入）

生成内容要拿到 B 级标签，必须通过以下 **5 道关卡**，任一失败则降级为 C 或直接丢弃：

```
          AI 生成题目
               │
    ┌──────────▼──────────┐
    │ ① 事实一致性校验      │  RAG 检索教材，提取题目中的事实性断言
    │                      │  每个断言必须有教材/法规出处，无出处 → 丢弃
    │                      │  数字类断言（税率/年限/比例）→ 强制法规库比对
    └──────────┬──────────┘
    ┌──────────▼──────────┐
    │ ② 答案自一致性校验    │  用 3 个不同模型/prompt 独立作答
    │                      │  一致率 < 100% → 打回人工复核队列
    └──────────┬──────────┘
    ┌──────────▼──────────┐
    │ ③ 干扰项质量校验      │  干扰项必须来自易错点库/邻近概念
    │                      │  检测：长度异常、表述荒谬、"以上都对"→ 重新生成
    └──────────┬──────────┘
    ┌──────────▼──────────┐
    │ ④ 去重校验            │  Embedding 相似度 > 0.92 与存量题重复 → 丢弃
    └──────────┬──────────┘
    ┌──────────▼──────────┐
    │ ⑤ 结构完整性校验      │  题干/选项/答案/解析/知识点绑定 齐全 → 通过
    └──────────┬──────────┘
               ▼
          B 级「AI生成」入库
```

**人工抽检**：B 级内容每日随机抽检 5%，抽检不通过率 > 3% 时触发全量复核并暂停该批次。

#### 8.6.3 用户侧纠错闭环

```
用户点「有疑问」 → 进入复核队列 → 教研判定
   ├─ 确认有误 → 修正或下架 → 通知用户 + 积分奖励
   │            → ⚠️ 触发关联排查：同一批次/同一知识点的 AI 生成题全部降为 C 级
   └─ 确认无误 → 附教研说明回复用户 + 该题升为 A 级（人工已背书）
```

#### 8.6.4 Prompt 层防幻觉约束

系统 Prompt 中必须包含：

1. **知识边界声明**："你只能依据提供的教材片段作答。片段中未包含的信息，必须回答'根据提供的资料无法确定'，禁止使用你的内部知识补充。"
2. **年份锚定**："当前考期为 2026 年度，教材版本为 2026 版。涉及税率、法规、政策数字时，如资料中未明确标注适用年份，必须提示可能存在版本差异。"
3. **禁止编造条款**："禁止生成任何具体的法条编号、条款数字，除非资料中明确给出。"
4. **低置信度自陈**："若你对答案的把握低于 90%，在输出中标注 low_confidence 字段。"

#### 8.6.5 合规与免责

1. **不做押题承诺**（NG3）：A6 功能命名为「高频考点预测」，UI 中明确说明"基于近 5 年真题频次统计与考纲变动分析，为考点重要度排序，不构成对考试内容的预测或承诺"。
2. **AI 内容标识**：所有 AI 生成内容在 UI 上显著标注（B/C 级标签）。
3. **过线预测免责**：预测功能明确标注"基于模型的概率估算，不构成对考试结果的承诺"。
4. **数据合规**：用户上传的资料默认私有，不用于模型训练（除非用户明确授权）；用户上传内容需符合版权规范，UI 提示"请确保你有权上传该资料"。
5. **《生成式人工智能服务管理暂行办法》合规**：履行内容审核义务、标识义务（AI 生成内容显著标识）。
6. **⚠️ AI 服务提供方的免责边界（v1.4 新增，法务必审）**——**必须按接入路径分别表述，不得混用**：

| 路径 | 免责声明（设置页 + 首次使用弹窗） |
| --- | --- |
| **A. BYOK** | 「AI 功能由**你选择的第三方服务商**提供。其输出内容不代表本产品的立场，**我们不对其准确性负责**。请以教材与官方文件为准。」 |
| ~~**B. 中转**~~ | ⛔ **v1.5 已删除**。无中转路径，**免责声明只需保留 BYOK 一个版本**——这也消除了 v1.4 的"用错话术即构成虚假宣传"风险。 | |

> ✅ **v1.5 更新（本节风险已大幅下降）**：由于撤销中转，**我们完全不在 AI 请求链路上**，因此不构成《生成式人工智能服务管理暂行办法》意义上的"服务提供者"，**备案义务判定不再是阻塞项**（原 R-20 撤销）。
>
> **但仍需保留的合规动作**：
> 1. 设置页明确告知"AI 服务由你选择的第三方提供"，并提供免责声明（法务必审）。
> 2. **不得对模型输出作准确性背书**，所有 AI 输出均带"请以教材与官方文件为准"的提示。
> 3. **若启用服务商邀请返利**：跳转前标注「推广」，披露合作关系，且不得因返利影响服务商排序（详见 §M9-F7）。**建议先按"无返利"上线。**

### 8.7 AI 技术选型建议

| 层    | 选型                            | 理由                                 |
| ---- | ----------------------------- | ---------------------------------- |
| 大模型  | 国内主流商用 LLM API（多模型路由）         | 中文理解好、合规可控、成本可控。**必须多模型冗余**，避免单点依赖 |
| 向量库  | Milvus / pgvector             | 教材/题库 Embedding 检索                 |
| 调度算法 | FSRS-6（开源）                    | 比 SM-2 更精准的现代间隔重复算法                |
| OCR  | 成熟 OCR 服务（中文识别率 ≥98%）         | 拍照上传刚需                             |
| 成本策略 | 分级路由：简单解析用小模型，复杂推理用大模型；高频问题缓存 | 控制边际成本                             |

**成本红线**（v1.5：**AI 成本永久归零，无需红线**）：

| 路径 | 我们的成本 | 说明 |
| --- | --- | --- |
| **AI 服务（唯一路径：BYOK 直连）** | **¥0（永久）** | 用户自付、设备直连、我们不经手。**成本不随用户规模变化** |
| ~~降本三招~~ | ⛔ **v1.5 已删除** | 无中转即无服务端成本，无需降本手段（上下文裁剪仍可作为**用户体验**优化保留，但不再是成本手段） |
| ~~成本红线~~ | ⛔ **v1.5 已删除** | AI 成本恒为 ¥0，无需止损闸 |

> ✅ **v1.5 说明**：v1.3 表述为"AI 成本归零、不做中转、不做配额"；v1.4 曾引入中转产生 ¥1.0/新用户获客成本；**v1.5 撤销中转，AI 成本永久归零，且不随用户规模放大**——这是纯 BYOK 的核心财务优势。

---


### 8.8 AI 联网边界设计（v1.1 本地架构新增）🔒

> 本地优先架构下，AI 是**唯一**的网络依赖。必须把边界划清楚：什么离线能用、什么必须联网、什么数据会离开设备。

#### 8.8.1 能力-网络依赖矩阵

| AI 能力               | 离线可用       | 联网依赖说明                                |
| ------------------- | ---------- | ------------------------------------- |
| **A2 解析查看**（已缓存的）   | ✅          | 首次生成需联网，之后本地缓存，离线可反复查看                |
| **A3 学习教练 · 掌握度诊断** | ✅ **完全离线** | 纯本地数据计算，不涉及 LLM                       |
| **A3 学习教练 · 计划重排**  | ✅ **完全离线** | 规则引擎 + 本地数据，无需 LLM。AI 文案部分可离线时降级为模板文案 |
| **A8 过线概率预测**       | ✅ **完全离线** | 本地统计模型                                |
| **A4 私教对话**         | ❌ 需联网      | 唯一强依赖                                 |
| **A1 AI 出题**        | ❌ 需联网      | 生成后可离线练习                              |
| **A5 记忆助手（口诀/导图）**  | ❌ 需联网（生成）  | 生成结果本地持久化，之后离线可看                      |
| **A6 考点预测**         | ✅ **基本离线** | 基于本地真题频次统计；考纲变动 diff 内容随版本更新内置        |

**设计原则**：**能用本地规则/统计解决的，绝不依赖 LLM**。这既保证离线可用性，也大幅降低成本。上表中 4 项能力（诊断、计划重排、概率预测、考点预测）全部离线实现——这意味着**用户在飞机上、地铁里，App 的核心智能仍然完整可用**，这是相对云端竞品的显著体验优势。

#### 8.8.2 RAG 语料：放本地还是云端？

**决策：RAG 检索全本地，LLM 生成走云端。**

```
用户提问 / 出题请求
      │
      ▼
【本地】检索（不上网）
  ├─ 自研精讲库（内置，随 App 分发）
  ├─ 考纲知识树（内置）
  ├─ 用户上传资料（本地解析的 Chunk）
  └─ 检索方式：BM25 关键词检索 + 可选本地向量检索
      │
      ▼ 仅把「检索到的相关片段 + 用户问题」发出去
【云端】LLM 生成
      │
      ▼ 结果回来后本地持久化缓存
【本地】存储与展示
```

**关键约束：内置语料只能是自研内容，不得内置教材原文**（版权风险，Q3）。

- ✅ 可内置：我们自研的精讲文字、考点摘要、对比表、公式手册、真题（自行整理的题干与解析）、考纲条目
- ❌ 不可内置：中国人事出版社教材原文扫描/全文

**用户上传的教材成为其个人 RAG 语料**——这部分完全在本地，且用户对自己拥有的资料做 RAG 属于合理使用范畴，无版权问题。**这反而是本地架构的一大优势**：云端产品不敢让用户上传整本教材做 RAG（存储与版权双重成本），而我们可以。

**本地检索技术选型建议**：

1. **首选 BM25（FTS5）**：SQLite 内置全文检索，零额外体积，中文需配置分词（可用 `jieba` 预分词入库）。对于考点明确、术语规范的考试场景，BM25 准确率足够。
2. **可选增强**：本地 ONNX 小 embedding 模型（如 multilingual-MiniLM，约 25-90MB）做向量检索，用 `sqlite-vec` 存储。建议作为**可选下载包**（设置里让用户选择是否下载"增强检索模型"），不强制增大主包体积。
3. **混合检索**：BM25 + 向量加权融合，效果最好，但 v1.1 可先只上 BM25。

#### 8.8.3 数据外流边界（必须显著告知用户）

**本地架构下，只有以下数据会离开设备**：

| 数据                       | 流向               | 告知方式                              |
| ------------------------ | ---------------- | --------------------------------- |
| **AI 对话中用户输入的文字** | 用户配置的 LLM 服务商（**不经我们**） | ⚠️ 首次使用 AI 功能时的弹窗说明（一次性，可随时在设置查看） |
| AI 生成解析/出题时携带的题目文本与上下文片段 | 同上（**同样不经我们**）    | 同上                                |
| 崩溃日志（如用户同意）              | 崩溃监控服务           | 首次启动时询问，**默认关闭**                  |
| 平台内购收据校验                 | App Store / 应用商店 | 无需额外告知（平台标准流程）                    |
| 应用更新检查                   | 应用商店             | —                                 |
| **广告 SDK 采集的设备/网络最小集**   | 广告平台（M9-F11）     | 隐私政策「广告与第三方 SDK」章节 + 首次启动广告说明     |

**绝不上送**：答题记录、错题、笔记、学习计划、掌握度、上传的原始资料文件、**AI 对话内容**。

> ✅ **v1.5 澄清（v1.4 的澄清已作废）**：
>
> v1.3 曾声明"我们的服务器完全不在 AI 请求链路上"。**v1.4 因引入中转路径，一度需要修正这个表述**（当时必须区分 BYOK 与中转两套话术）。
>
> **v1.5 撤销中转后，这句话重新成为无条件的真话，且不再需要任何分路径说明**：
>
> > **「你的对话由 App 直接发送给你配置的 AI 服务商，我们不经过、看不到、不留存。」**
>
> **这一简化带来三个好处**：① 话术单一，不存在"用错话术"的虚假宣传风险；② 用户不需要理解"我怎么知道走的是哪条路"；③ 隐私承诺的可验证性最强——**我们连服务器都没有，不存在"说了不留存但偷偷存了"的可能**。

**建议的产品文案**（可放在设置页作为卖点）：

> 「你的学习数据只存在这部手机里。
>
> AI 对话的内容**直接发送给你自己配置的服务商**——我们不经过、看不到、不留存。
>
> 广告商拿不到你的任何学习信息。」

#### 8.8.4 离线降级体验

| 场景                  | 降级方案                                                |
| ------------------- | --------------------------------------------------- |
| 无网络时使用 AI 对话        | 提示"AI 私教需要联网"，并**提供离线替代**：展示该知识点的精讲内容 + 相关题目，不出现死胡同 |
| AI 出题中途断网           | 已生成的部分保留，提示"网络中断，已生成 N 题"，支持重试续生成                   |
| 无网络时点击"AI 解析"       | 若该题目有预生成的本地解析则直接展示；否则展示教研组编写的静态解析（A 级内容，本就有）        |
| BYOK 用户 Key 失效/余额不足 | 明确提示原因（非笼统的"请求失败"），并提供"更换 Key"入口；**附常见原因自查清单**（余额不足 / Key 被禁用 / 触达服务商限流阈值 / 服务商故障）——**这些均属用户与服务商之间的事务，我们只做如实转达** |
| **未配置 Key 的用户**（v1.5） | **这不是错误态，是正常状态**。状态条显示「未配置 · AI 为可选功能」（灰点，非警告色）；对话页显示欢迎语 + **「AI 到底能帮我做什么？」示例入口卡片**；用户尝试发送时轻提示并引导到设置页。**全程不出现"待激活""功能不可用"这类负面表述** |
| ~~中转额度已用完~~ | ⛔ **v1.5 已删除**（无额度体系） |
| **AI 请求失败**（v1.5） | 按 `AiError` 分类给可读文案（未配置 / Key 无效 / 限流 / 网络 / 服务商异常），并附"常见原因自查清单"。**任何 AI 异常都不得冒泡为页面级错误**（NG16 技术硬约束） |

#### 8.8.5 客户端 AI 实现要点（研发关注）

**客户端 AI 实现要点（v1.5：单一路径，要求精简）**：

1. **Key 安全存储（仅 BYOK）**：iOS Keychain / Android Keystore，禁止写入 UserDefaults/SharedPreferences/日志。
2. **禁止日志脱敏遗漏**：网络请求日志中必须过滤 `Authorization` 头。
3. **多厂商适配**：抽象统一的 `LLMClient` 接口，支持多家厂商（OpenAI 兼容协议为主），便于用户自选与后续切换。
4. **超时与重试**：AI 请求超时 30 秒，失败提供一次手动重试，不做自动无限重试（省成本）。
5. **流式输出**：对话与长解析使用 SSE 流式，首字延迟目标 < 1.5s。
6. **并发控制**：AI 出题批量任务串行化或限并发 ≤ 3，避免触发厂商限流。

**v1.5 新增/调整的要求（替代原中转专项要求）**：

7. **上下文裁剪在客户端完成**：请求上行前，由 App 裁剪到"当前题目 + 关联知识点"，**不把整本书或历史全量对话传上去**。v1.4 曾把它当作降本手段，**v1.5 保留它但动机改为纯粹的用户侧优化**——减少 token 消耗（用户自己付钱，这是替他省钱）与隐私暴露面。
8. **响应缓存策略**：以「题目 ID + 提问类型」为 key 缓存 AI 解析结果（**本地缓存**），重复提问直接命中。同样是用户侧优化（省钱 + 更快）。
9. **AI 异常隔离（硬约束）**：所有 AI 调用必须被 `try/catch` 包裹并转换为可读文案，**绝不允许冒泡为页面级错误或白屏**。未配置、网络失败、服务商异常均不阻断任何其他功能（NG16）。
10. **不采集 AI 对话内容用于任何分析** —— 与 NG10 一致。**v1.5 无转发层，这条在技术上天然成立**（我们根本没有接触点）。

---

## 9. 关键用户旅程

### 9.1 新手首次启动（目标：3 分钟内产出第一份计划）

```
启动 App
  → 隐私政策确认（合规必需）
  → S1 选择科目与专业（10 选 1，配"哪个专业好过？"智能建议）
  → S2 确认考试日（默认 2026-11-07）
  → S3 每日可用时长（滑块）
  → S4 学习时段偏好（决定推送时间）
  → S5 摸底测评（30 题 / 可跳过）
  → 🎉 生成「我的作战地图」
       ├─ 180 天五阶段甘特图
       ├─ 模块学习顺序 + 排序理由（§6.3）
       ├─ 当前过线概率基线
       └─ [开始今天的第一个任务]
  → 首页 · 今日任务
```

**关键指标**：onboarding 完成率 ≥ 70%，平均耗时 ≤ 3 分钟。

### 9.2 每日学习主循环（App 的核心循环）

```
打开 App → 首页「今日」
  ├─ 进度环：3/8 已完成
  ├─ 🔴【复习】12 题 · 约 8 分钟   ← 置顶，硬约束
  ├─ 【新学】财政-税收分类 · 约 15 分钟
  ├─ 【错题】5 题 · 约 4 分钟
  └─ 【测试】章节测 · 约 6 分钟
       │
       ▼
  点击【复习】→ 答题流（间隔重复）
       │  每题：确信度自评 → 作答 → 提交 → 即时反馈 → 四级评分
       ▼
  完成 → 成果卡（答对 X / 连续 Y 天 / 掌握 Z 考点）← 峰终定律
       ▼
  返回首页，进度环更新
       ▼
  继续下一个任务包 or 结束（无挽留弹窗）
```

### 9.3 上传资料 → AI 出题（M7 完整链路）

```
「我的」→「我的题库」→「+ 新建题集」
  → 选择输入方式：[上传文件] [拍照] [粘贴文本] [AI 直接出题]
  → 上传 PDF（如"2026教材-财政-第3章.pdf"）
  → 处理中（进度提示：解析中 → 识别题目 → AI 生成解析，预计 40 秒）
       ⚠️ 处理期间允许用户离开，完成后推送通知
  → 📋 预览页（关键！不静默入库）
       共识别 23 道题
       🟢 识别良好 18 题  🟡 请核对 4 题  🔴 建议检查 1 题
       [按置信度筛选] [批量操作]
       每题卡片：题干/选项/答案/解析/知识点/置信度
  → 用户逐题核对 or 批量确认
  → [确认导入 21 题]
  → 进入题集「2026教材-财政-第3章」
  → 可立即发起练习 / 纳入复习调度
```

### 9.4 全真模考（M5）

```
「模考」Tab → 选择试卷（教研组卷 / 真题卷 / 连考模式）
  → 考前三确认页：
       ⚠️ 全真模拟，请确保在安静环境下连续 90 分钟作答
       ○ 严格模式（推荐）：按题型分段锁定，进入下一题型后不可返回
       ○ 宽松模式：可自由跳转修改
       [开始考试]
  → 机考界面（§M5-F1 还原）
       倒计时 / 题号栏 / 标记 / 强调显示 / 计算器 / 分栏切换
  → 题型切换强确认（严格模式）
  → [交卷] → 检查清单（未作答 X 题 · 标记 Y 题）→ 二次确认
  → 即时出分 → AI 复盘报告（6 维度，含「遗憾分分析」）
  → [把薄弱点加入计划]
```

---

## 10. UI 设计规范


### 10.1 设计系统（Design Tokens）

**色彩（浅色模式）**

| Token                 | 值         | 用途                          |
| --------------------- | --------- | --------------------------- |
| `color-primary`       | `#2B5CE6` | 主色：知识蓝，传达专业与信任。用于主按钮、进度、选中态 |
| `color-primary-light` | `#E8EEFE` | 主色浅底：选中背景                   |
| `color-success`       | `#16A34A` | 正确/掌握                       |
| `color-warning`       | `#F59E0B` | 薄弱/提醒                       |
| `color-danger`        | `#DC2626` | 错误/薄弱严重                     |
| `bg-primary`          | `#FFFFFF` | 页面背景                        |
| `bg-secondary`        | `#F5F7FA` | 卡片/分组背景                     |
| `text-primary`        | `#1A1D26` | 主文本（**对比度 ≥ 7:1**，长文阅读）     |
| `text-secondary`      | `#5A6172` | 次要文本（对比度 ≥ 4.5:1）           |
| `text-tertiary`       | `#9AA1B1` | 辅助文本                        |

**深色模式**（夜间刷题刚需）：背景 `#12141A`，卡片 `#1C1F27`，主文本 `#E8EAED`，主色降饱和为 `#5B82F0`（避免深色背景下高饱和蓝色刺眼）。

> ⚠️ **无障碍硬性要求**：正确/错误判定**绝不能只靠颜色**。必须同时提供：图标（✓ / ✗）+ 文字标签 + 背景纹理差异。约 8% 男性存在色觉障碍，红绿色盲用户必须能正常刷题。

**字体与排版**

| Token | 值                                      | 说明                |
| ----- | -------------------------------------- | ----------------- |
| 题目正文  | 17-18px / 行高 1.7                       | 考试阅读量大，字号不能小      |
| 选项正文  | 16px / 行高 1.6                          |                   |
| 精讲正文  | 16px / 行高 1.75                         | 长文阅读需要更大行高        |
| 辅助文字  | 13-14px                                |                   |
| 字体    | 系统字体（iOS: PingFang SC / Android: 思源黑体） | 不引入自定义字体，保证性能与一致性 |

**间距与栅格**：8pt 栅格系统。页面边距 16px，卡片间距 12px，卡片内边距 16px，圆角 12px（卡片）/ 8px（按钮）/ 24px（底部弹窗）。

**触控目标**：所有可点击元素最小 **44×44pt**；选项行高度 **≥56px**；底部主按钮高度 52px，距屏幕底部 16px（拇指热区）。


### 10.2 核心组件规范

| 组件             | 规范                                                                                 |
| -------------- | ---------------------------------------------------------------------------------- |
| **题号格**        | 36×36px（模考）/ 44×44px（练习，更好点击）。状态：白底未答 / 蓝底已答 / 橙底当前 / 三角标记                         |
| **选项行**        | 整行可点击，左侧单选圆圈 ⚪ 或多选方框 ☐，选中后背景 `primary-light` + 左侧 3px 主色竖条。提交后：正确项绿底 + ✓，错误项红底 + ✗ |
| **进度环**        | 首页顶部，直径 88px，环宽 8px，中心显示"3/8"。完成时播放 0.6s 填充动效                                      |
| **掌握度条**       | 5 档色带（灰/红/橙/浅绿/深绿）+ 百分比数字 + 档位文字（不依赖颜色）                                            |
| **底部 Tab**     | 5 项固定，图标 + 文字，选中态为填充图标 + 主色，高度 56px + 安全区                                          |
| **空状态**        | 所有列表必须有空状态插画 + 说明 + 主行动按钮（如错题本为空："太棒了，还没有错题"）                                      |
| **加载态**        | 骨架屏（不用转圈），AI 生成场景使用"思考中"动效 + 进度文案                                                  |
| **Toast / 弹窗** | 学习流程中**禁用打断式弹窗**（§5.3、M9-F1）。仅交卷、删除、**购买去广告、恢复购买**等关键操作使用模态确认 |
| **广告位**（v1.2 新增，v1.3 修订） | 严格遵循 M9-F1 铁律与 M9-F3 逐位约束。**开屏广告**：全屏素材 + 右上角跳过按钮（≥44×44pt，2s 后出现）+ 底部「广告」标识；**严禁**跳过按钮过小、二次确认、遮挡功能入口。**激励视频入口**：采用「描边按钮 + 🎬 图标」样式，**视觉权重低于主行动按钮**（不得比"继续学习"更醒目）；**离线/加载失败时不渲染任何占位**（M9-F11） |

### 10.3 页面级规范（关键页面）

**首页（学习 Tab）**

```
状态栏
┌──────────────────────────────────┐
│ 距考试 65 天 🔥            ⚙️     │  倒计时（持续张力）
├──────────────────────────────────┤
│        ╭─────────╮                │
│        │  3 / 8  │  今日任务      │  进度环（蔡格尼克）
│        ╰─────────╯  剩余 32 分钟  │
├──────────────────────────────────┤
│ 🔴 复习 12题 · 8分钟      [开始]  │  ← 置顶，红色警示
│ 📘 新学 税收分类 · 15分钟  [开始]  │
│ ❌ 错题 5题 · 4分钟        [开始]  │
│ 📝 章节测 · 6分钟          [开始]  │
├──────────────────────────────────┤
│ 📊 本周：掌握度 62% → 68%  ↑6%    │  进步反馈
└──────────────────────────────────┘
```

**答题页**：见 §M3-F2 布局图。原则：单屏一题、整行可点、提交后即时反馈、解析渐进披露。

### 10.4 动效与反馈

- 动效时长：微交互 150-250ms，页面转场 300ms，进度填充 600ms。**禁止超过 400ms 的动画**（备考用户对延迟敏感）。
- 触觉反馈：答对 = 轻震动 10ms，答错 = 双震动，完成任务包 = 中等震动。**可在设置中关闭**。
- 音效：默认关闭，可选开启（避免公共场合尴尬）。

---


## 11. 数据模型（核心实体）

> 设计原则：**多考试可扩展**（NG5 要求）。考试、科目、知识树均为可配置实体，不硬编码"中级经济师"。

```
Exam（考试）
  exam_id, name, 年度, 考试日期, 报名截止, 成绩滚动规则

Subject（科目）
  subject_id, exam_id, name, 时长(分钟), 总分, 合格线, 题型配置

QuestionType（题型配置）
  type_id, subject_id, name(单选/多选/案例), 题量, 每题分值,
  评分规则(错选0分/少选0.5分), 是否可跨题型返回

KnowledgeNode（知识点，树形）
  node_id, subject_id, parent_id, level(模块/章/知识点),
  name, 权重, 近5年频次, 重要度星级

Question（题目）⭐核心
  question_id, subject_id, type, stem, options[], answer[],
  difficulty, bloom_level, 分值权重,
  knowledge_node_ids[]（多绑，支持跨章节）,
  explanation: { per_option[], key_point, trap_words[] },
  ⭐ source_level: S|A|B|C（§8.6.1 可信度）
  ⭐ ai_metadata: { model, confidence, 校验状态, 生成时间 }
  is_real_exam, exam_year, 考查频次, 易错标签[],
  status(正常/待复核/已下架)
  owner_type: 官方|用户自建, owner_user_id

UserQuestionState（用户-题目状态，FSRS）
  user_id, question_id,
  fsrs: { difficulty, stability, retrievability, due_date, last_review },
  答题历史: [{ 时间, 用户答案, 正误, 用时, 确信度, 错误归因 }],
  状态: 未做|错题库|已攻克|收藏

UserKnowledgeState（用户-知识点掌握度）
  user_id, node_id, mastery_score(0-100), 档位,
  题目数, 正确率, 最后练习时间, 预测遗忘临界日

StudyPlan（学习计划）
  plan_id, user_id, exam_id, subject_ids[], 目标分,
  每日容量(分钟), 阶段[], 计划开始日, 状态

DailyTaskPack（每日任务包）
  pack_id, plan_id, date, 类型(复习/新学/错题/测试/模考),
  关联内容[], 预计耗时, 完成状态, 完成时间

WrongQuestion（错题）
  见 UserQuestionState 中状态=错题库

QuestionSet（用户题集，M7）
  set_id, user_id, name, 题目数, 来源(上传/AI生成/手动), 创建时间

Exam（模考记录）
  record_id, user_id, 卷id, 科目, 模式(严格/宽松),
  得分, 各题型得分, 各模块得分, 用时, 时间分配明细[],
  未作答题数, 标记题数, AI复盘报告

AIProvider（AI 服务商注册表，v1.5：**内置常量，非数据库实体**）
  provider_id, name, api_base, 
  是否内置, 场景标签[], 排序权重, 启用状态
  ⚠️ 内置服务商由我们配置；用户可自行添加（自定义 OpenAI 兼容端点）

AIConfig（AI 接入配置，v1.5，⚠️ 加密存储于系统钥匙串，**不入业务库**）
  provider_id, api_key(加密), base_url, model, 是否已测试通过
  ⚠️ 硬约束（v1.5）：**不存在任何额度字段**（无 quota_remaining / quota_total 等）。
     Key 只存本机钥匙串，不上传、不绑定身份、**不进备份文件**（NG14）

AIConversationCache（AI 响应缓存，仅本地）
  cache_key（题目 ID + 提问类型）, 响应内容, 生成时间, 命中次数
  ⚠️ 仅缓存"题目解析"类结果，动机是**替用户省 token 与提速**（v1.4 曾作为降本手段）；
     **用户自由对话内容不缓存、不落库**
```


**关键设计决策说明**：

1. `Question.source_level` 是一等公民字段——可信度必须在数据层就存在，而不是展示层打补丁。
2. `Question.knowledge_node_ids` 支持多绑：真实考试中大量题目跨知识点，单绑会丧失交错练习能力。
3. FSRS 状态挂在 **"用户 × 知识点"** 而非仅"用户 × 题目"：同一知识点换一道新题，记忆调度仍应延续。

### 11.1 本地持久化映射（v1.1）

**存储分层**

| 层            | 技术选型                                               | 内容                 | 是否备份  |
| ------------ | -------------------------------------------------- | ------------------ | ----- |
| **结构化主库**    | **SQLite**（iOS: SQLite.swift / GRDB；Android: Room） | 上表全部实体             | ✅ 备份  |
| **全文检索**     | SQLite **FTS5**（jieba 预分词）                         | 题目、精讲、用户上传资料的检索索引  | ❌ 可重建 |
| **向量检索（可选）** | `sqlite-vec` + ONNX 小模型                            | 语义检索增强包            | ❌ 可重建 |
| **附件文件**     | App 私有目录文件系统                                       | 上传的 PDF/图片原件、生成的导图 | ⚠️ 可选 |
| **缓存**       | 主库独立表 / 文件缓存                                       | AI 响应缓存、精讲缓存       | ❌ 不备份 |
| **密钥**       | iOS Keychain / Android Keystore                    | LLM API Key        | 随系统备份 |

**本地化后的模型调整**

| 调整                                  | 说明                                                                                                      |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------- |
| **删除 `User` 相关实体**                  | 无账号体系。原 `user_id` 字段统一替换为常量 `local_user`（保留字段是为了未来扩展时不重构，成本几乎为零）                                        |
| **`Question` 增加 `subject_id` 分区**   | 两科（经济基础 / 人力）数据同库，靠 `subject_id` 区分。内置题库随 App 分发，用户自建题与内置题**同表但有 `owner_type` 区分**，避免升级 App 时内置题库覆盖用户数据 |
| **新增 `BackupSnapshot` 实体**          | 快照 id、创建时间、题量、计划进度摘要、文件路径、类型（自动/手动）                                                                     |
| **新增 `Attachment` 实体**              | 附件 id、文件名、路径、大小、关联题集、是否随备份导出                                                                            |
| **`AIProvider` / `AIConfig`**（v1.5 简化） | **`AIProvider` 降级为内置常量**（`src/domain/ai/providers.ts`），**不入数据库**。**`AIConfig`**（服务商地址 + 模型名 + Key）存系统钥匙串，**不进业务库、不进备份文件**（换设备需重新粘贴 Key，这是无云端架构的必然结果，也避免 Key 随备份文件泄露） |
| **无服务端 → 无服务端数据模型**（v1.5） | ✅ **比 v1.4 更彻底**：v1.4 需要论证"转发层不落库"，v1.5 **根本没有转发层**——**"我们的服务器上没有你的任何数据"从"技术承诺"变成了"物理事实"**，无需证明。 |
| **新增 `AIResponseCache` 实体**         | 缓存 key（题目 id + 能力类型 + prompt 版本）、内容、来源模型、创建时间                                                           |
| **`Question` 增加 `content_version`** | 教材年度版本标识（如 `2026`）。用于教材更新时做 diff 与旧题标记，对应风险 R-09                                                        |

**内置题库的更新策略（关键）**

- 内置题库随 App 分发（打包进安装包或首次启动下载）。
- **用户数据（答题记录、笔记、自建题）与内置题库必须物理隔离或明确标记**，App 更新时**只增不改用户数据**。
- 内容修订（如纠错、教材变动）通过 **增量补丁包**下发：补丁记录 `question_id` + 新内容 + 版本号，应用后合并。
- 用户自建题永远不被任何更新覆盖。

**数据库迁移**

- 采用版本化迁移（`schema_version`），每次结构变更提供升级脚本。
- 迁移前**强制自动快照**（M10-F3 ①），迁移失败可回滚。

---

## 12. 非功能需求

| 类别                                                  | 要求                                                                                                        |
| --------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| **性能**                                              | 冷启动 ≤ 2s；答题页切换 ≤ 200ms（本地预加载下一题）；首页渲染 ≤ 1s；列表滚动 60fps；**全本地读写，无网络等待**                                     |
| **AI 响应**                                           | 对话首字延迟 ≤ 1.5s（流式）；AI 出题 20 题 ≤ 60s（支持后台异步，完成后通知）；解析生成后本地缓存，二次查看零延迟                                        |
| **离线能力（v1.1）**                                      | ✅ **除 AI 生成类功能与广告外，全部功能离线可用**（答题、复习、计划、自建题库、模考、数据看板、掌握度诊断、概率预测）。                                             |
| ⚠️ 需联网：AI 对话、AI 出题、AI 生成解析（首次）、应用更新、内购校验、**广告加载（v1.2）**。           |                                                                                                           |
| 降级要求见 §8.8.4——**任何需要联网的功能，离线时必须给出可执行的替代路径，不得出现死胡同**。**广告的降级要求见 M9-F11：加载失败时静默不渲染，不影响任何学习功能** |                                                                                                           |
| **广告性能（v1.2 新增，v1.3 强化）** 🔒                       | **开屏广告位于冷启动关键路径，标准比内容区更严**：① **冷启动 ≤2s 的指标优先于广告加载**——广告与 App 初始化**并行进行**，**1.5s 内未就绪则直接跳过开屏**（绝不让用户"白屏等广告"）；② 广告 SDK **延迟初始化**（内容区广告位首次到达时才初始化）；**严禁参与首屏关键渲染**；③ 内容区单次加载超时 5s 放弃；连续 3 次失败本会话熔断；④ **SDK 崩溃必须被错误边界隔离，不得导致 App 崩溃**；⑤ 离线时零占位渲染；⑥ **热启动（后台切回）不展示开屏** |
| **商业内容合规（v1.2，v1.3 微调）**                            | 虚拟商品**必须走平台 IAP**，严禁引导站外支付；**恢复购买入口常驻「我的」一级页面**；所有广告显著标注「广告」；激励视频明示时长与奖励；购买页明示价格/有效期/**不包含内容**；退款指引清晰；接广告 SDK 若使用设备标识符**必须弹窗征求同意**（iOS ATT / 安卓 OAID），推荐非个性化投放（M9-F6） |
| **BYOK 安全（v1.3 新增）** 🔒                              | API Key 必须存系统钥匙串（iOS Keychain / Android Keystore），**禁止写入 UserDefaults / SharedPreferences / 日志**；网络请求日志必须过滤 Authorization 头 |
| **🔒 AI 客户端安全（v1.5 替代原"中转层安全"）**              | ① **Key 存储**——系统钥匙串（iOS Keychain / Android Keystore），**禁止**写入 UserDefaults / SharedPreferences / 日志；② **日志脱敏**——网络请求日志必须过滤 `Authorization` 头；③ **Key 不回传**——**本产品无服务端，Key 在技术上不可能外流**；④ **AI 异常隔离**——所有 AI 调用须被兜住，不得冒泡为页面级错误（NG16） |
| **可靠性**                                             | 答题进度实时落盘（**答题中杀进程/来电中断，重进必须恢复到中断处**）；模考过程防丢失（每 30s 本地快照 + 断网不影响计时）；数据库迁移前强制快照                             |
| **🔒 数据安全（v1.1）**                                   | 数据仅存设备本地，无服务端；LLM API Key 存系统钥匙串；备份文件可选 AES-256 加密；AI 请求走 HTTPS；**日志中禁止出现 API Key 与题目正文（防止脱敏遗漏）**         |
| **🔒 商业化合规（v1.2 新增，v1.3 微调）**                          | 虚拟商品**必须走平台 IAP**，严禁引导站外支付（平台强制）；**恢复购买入口常驻「我的」一级页面**（平台 + 法律双强制）；所有广告显著标注「广告」；激励视频明示时长与奖励；**开屏广告必须可跳过、≤5 秒**；购买页明示价格/有效期/**不包含内容**；退款指引清晰；接广告 SDK 若使用设备标识符**必须弹窗征求同意**（iOS ATT / 安卓 OAID），推荐采用非个性化投放方案（M9-F6） |
| **🔒 AI 服务合规（v1.3 新增 / v1.5 定稿）**                                | **v1.5 只有一档要求（大幅简化）**：<br>① **告知义务**：设置页明确告知"AI 服务由你选择的第三方提供，请求由你的设备直接发送，我们不接收、不存储"；<br>② **免责声明**：**"AI 输出内容不代表本产品的立场，我们不对其准确性负责，请以教材与官方文件为准"**（法务必审）；<br>③ **返利披露（若启用）**：推广链接必须标注「推广」并披露合作关系，不得因返利影响排序（**建议先按"无返利"上线**）；<br>④ **备案义务**：**不适用**——我们不在《生成式人工智能服务管理暂行办法》的服务提供者链路上（原 R-20 撤销）。 |
| **合规**                                              | 《个人信息保护法》：无账号即无个人信息收集，隐私政策明示"数据仅存本机"，提供数据导出（备份文件）与清除全部数据入口；**广告数据边界单列说明（M9-F11）**；《生成式人工智能服务管理暂行办法》：AI 生成内容显著标识、内容安全审核（见 §8.6.5）；《广告法》第二十四条：广告不得与通过率关联（NG3）；《互联网广告管理办法》：广告标识义务 |
| **兼容性**                                             | iOS 15+ / Android 9+；支持深色模式；支持系统字号 200% 不破版；支持 TalkBack / VoiceOver                                       |
| **埋点（v1.1 重构）**                                     | 见 §13.4：**本地统计为主 + 用户主动上报为辅**，默认不上传任何行为数据                                                                 |
| **包体积**                                             | 主包（含两科内置题库 + 内置 RAG 语料）控制在 **150MB 以内**；向量检索增强模型作为可选下载（+25-90MB）                                          |
| **存储空间**                                            | App 安装后初始占用 ≤ 150MB；提供存储管理页；缓存超 1GB 时提示清理                                                                 |

---

## 13. 指标体系

### 13.1 北极星指标

> **NSM = 周活跃「有效学习用户」数**（一周内 ≥4 天活跃，且完成 ≥20 题主动回忆练习）

### 13.2 三层指标体系

| 层级        | 指标             | 定义                | 基线/目标              |
| --------- | -------------- | ----------------- | ------------------ |
| **北极星**   | 周活跃有效学习用户数     | 见上                | 上线 3 个月达 5 万       |
|           |                |                   |                    |
| **驱动指标**  |                |                   |                    |
| （学习科学有效性） | 7 日知识点保持率      | 学后 7 天同知识点正确率     | 目标 ≥ 75%           |
|           | 复习任务完成率        | 完成复习量 / 计划复习量     | 目标 ≥ 80%           |
|           | 错题攻克率          | 已攻克 / 累计错题        | 目标 ≥ 70%           |
|           | 掌握度月增幅         | 全科目加权掌握度月度提升      | 目标 ≥ 15pp/月        |
|           | 元认知校准偏差        | 确信度 vs 实际正确率的偏差   | 目标 < 15%           |
|           |                |                   |                    |
| **驱动指标**  |                |                   |                    |
| （产品使用）    | Onboarding 完成率 | 完成 5 步向导占比        | ≥ 70%              |
|           | 次日/7日/30日留存    | 常规留存              | 次留 ≥ 45%，7 留 ≥ 30% |
|           | 日均有效学习时长       | 答题+复习时长（不含看课）     | ≥ 22 分钟            |
|           | 连续学习 ≥21 天用户占比 | 习惯养成指标            | ≥ 40%              |
|           | 模考参与率          | 周活跃用户中完成 ≥1 次模考占比 | P3 期 ≥ 60%         |
|           | AI 功能渗透率       | 用过 AI 功能的活跃用户占比   | ≥ 60%              |
|           |                |                   |                    |
| **健康指标**  |                |                   |                    |
| （质量与风险）   | ⚠️ AI 内容争议率    | 被点"有疑问"的题 / 总答题量  | **< 3%（红线）**       |
|           | AI 内容纠错确认率     | 复核确认有误 / 争议量      | < 30%              |
|           | 题库错误率（S/A级抽检）  | 抽检发现错误占比          | < 0.5%             |
|           | 崩溃率 / ANR      | 常规                | < 0.2% / < 0.1%    |
|           | 客服工单率          | 千用户工单数            | < 5                |
|           | **⚠️ 广告相关卸载率（v1.2 替换"退订率"）** | 卸载前使用时长 < 3 天且触达过广告的用户占比 | **< 1%（红线）**       |
|           | **⚠️ 广告差评占比（v1.2 新增）** | 商店评论中提及广告的负面评论占比  | **< 5%（红线）**，>10% 立即降频甚至下线 |
|           | **⚠️ 学习流程广告违规次数（v1.2 新增）** | 在 T1 禁投区内曝光广告的次数 | **必须为 0**（技术事故，一次即告警） |
|           |                |                   |                    |
| **健康指标**  |                |                   |                    |
| （商业化，v1.2 新增，v1.3 扩充） | **ARPDAU**     | 日活跃用户平均日收入        | ≥ ¥0.15（v1.3 上调）  |
|           | **开屏广告收入占比**     | 开屏收入 / 广告总收入      | 40-45%（>60% 说明结构过于依赖单一位置） |
|           | **开屏跳过率**         | 跳过次数 / 开屏曝光次数     | 监控即可；>95% 说明素材质量差  |
|           | **开屏加载成功率**       | 实际曝光 / 冷启动次数      | ≥ 85%（低于此值说明超时标准过严或填充率不足） |
|           | **BYOK 配置率**       | 已配置 Key 的活跃用户 / 活跃用户 | ≥ 25%；< 10% 说明引导不到位 |
|           | **激励视频人均日观看**    | 活跃用户日人均次数         | 目标 1.4-2.1 次（上限 6 次） |
|           | **广告填充率**         | 成功加载 / 请求次数       | ≥ 70%（上线 30 天后）    |
|           | **去广告购买率**        | 累计购买 / 累计活跃用户（3 个月内） | ≥ 3%               |
|           | **恢复购买成功率**       | 成功恢复 / 发起恢复       | ≥ 95%（核心体验指标）     |
|           |                |                   |                    |
| **结果指标**  |                |                   |                    |
| （滞后，考后统计） | **真实通过率** ⭐    | 上报成绩的**活跃学习用户**中两科均过线占比 | 目标 ≥ 68%           |
|           | 模考-实考偏差        | 末次模考分 vs 真实分      | ±6 分内              |
|           | 付费 vs 免费通过率差    | 购买去广告用户通过率 - 免费用户通过率 | **应趋近于 0**（若差异过大，说明免费版被刻意削弱，需复盘） |
|           | NPS            | 考后调研              | ≥ 45               |
|           | 口碑推荐率          | 通过用户中推荐给他人的占比     | ≥ 30%              |

### 13.3 数据口径与评估节奏

- **评估节奏**：领先指标每周看；健康指标每周看（AI 争议率**每日**监控，超阈值告警）；结果指标考后 30 天统计（成绩公布后发起调研 + 成绩上报激励）。
- **AI 争议率告警机制**：单日 > 5% → 触发告警；连续 3 日 > 3% → 自动暂停 AI 出题功能并切换为人工审核模式。
- **真实通过率如何获取**：考后在 App 内提供"成绩上报"入口（激励：参与抽奖 + 免广告天数，**v1.5 起不再送 AI 额度**——无额度体系），支持手动填写分数或上传成绩截图（本地 OCR 识别，仅上传识别后的数字）。需明确告知用途并获得授权。

### 13.4 埋点方案（v1.1 本地架构重构）⚠️

> **这是本地化最容易被忽视、但对产品迭代影响最大的问题。**  
> 无服务端 = 无法自动采集用户行为 = 传统 DAU/留存/转化漏斗全部拿不到。必须提前设计替代方案，否则上线后就是"盲飞"。

**三层数据方案**

| 层级          | 方式                                           | 覆盖范围                               | 默认状态            |
| ----------- | -------------------------------------------- | ---------------------------------- | --------------- |
| **L1 本地统计** | 全部数据本地计算，在 M8 数据看板中展示给用户自己                   | 用户自己的全量学习数据                        | 始终开启            |
| **L2 平台数据** | 从 App Store Connect / Google Play Console **及广告平台后台**获取 | 下载量、活跃设备数、留存（平台口径）、销售额、**广告收入/eCPM/填充率/展示量**、崩溃率 | 自动（平台提供）       |
| **L3 主动上报** | 用户开启「帮助改进产品」后，定期上传**匿名聚合**数据                 | 功能使用率、学习行为统计、AI 争议率                | **默认关闭**，用户主动开启 |

**L3 主动上报的设计要点**（决定这个方案能不能成）：

1. **上报什么**：聚合统计数据（如"本周完成 87 题、复习 42 题、使用 AI 12 次、触发疑问 1 次"）+ 设备型号 + App 版本 + 科目。**不含**任何个人标识、不含题目正文、不含笔记内容、不含 AI Key。
2. **随机 device_id**：上报时生成随机匿名 ID，**不与任何真实身份绑定**，且可在设置中重置。
3. **明确的价值交换话术**（不说"我们想收集数据"，而说"你会得到什么"）：
   > 「开启后，你的 anonymous 学习数据会帮助我们改进产品——比如发现某个考点大家普遍容易错，我们就会为它制作专项讲解。  
   > 可以随时关闭，关闭后不再上传。」
4. **预期开启率**：参考同类产品的隐私开关，保守估计 **15-25%**。这意味着 L3 数据只能作为**趋势参考而非精确指标**，决策时要留余量。
5. **产品指标如何看**（务实口径）：
   - 北极星（周活跃有效学习用户）→ 用 **L3 上报样本推算** + **平台 DAU/WAU 交叉验证**
   - **广告收入与 eCPM → 广告平台后台最准（L2）**，不依赖埋点。
   - **去广告购买率 / 恢复购买成功率 → 平台后台（L2）**，不依赖埋点。
   - **广告体验指标（广告相关卸载率、差评占比）→ L3 + 商店评论人工归类**。这两个指标**关系产品生死，必须每周人工看一次商店评论**，不能只依赖数据。
   - AI 争议率 → L3 样本，且这个指标对样本偏差不敏感（比例型指标），可靠性较高
   - 学习科学有效性（保持率、攻克率）→ L1 本地计算，**用户自己看得到**，且可通过 L3 抽样汇总

**妥协与取舍（要如实写进文档）**：

- ❌ 无法做精确的用户分群与漏斗分析
- ❌ 无法做 A/B 实验（除非自己实现本地分流 + L3 上报，可行但精度低）
- ✅ 换来的是零隐私争议、零数据合规成本、用户信任——**这与产品的核心卖点一致**，是正确的取舍

**建议**：v1.1 阶段接受"粗粒度数据 + 定性反馈（应用商店评论、用户反馈入口）"的组合。等产品跑通、用户量起来后，若确实需要精细化运营，再评估是否引入**可选的匿名分析服务**（到那时再让用户选择，也不迟）。

---

## 14. 风险与对策

| #           | 风险                                       | 影响                         | 概率              | 对策                                                                                                                                         |
| ----------- | ---------------------------------------- | -------------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| R-01        | ⚠️ **AI 内容错误导致用户记错知识点**                  | 致命（产品信任崩塌）                 | 中               | §8.6 四级可信度 + 五道校验关卡 + 争议闭环 + 每日争议率监控告警 + 自动熔断                                                                                              |
| R-02        | ⚠️ **机考规则核实不准**（R6 题型锁定存疑）               | 高（模考失真）                    | 中               | 模考默认严格模式（最严约束不吃亏）+ 宽松模式开关；**上线前必须人工登录中国人事考试网官方模拟系统实测确认**（见 Q1）                                                                             |
| ~~R-03~~        | ~~AI 调用成本失控~~                            | ~~高（毛利）~~                  | ⛔ **v1.5 已消除**  | **纯 BYOK 下 AI 成本恒为 ¥0**（用户自付），**成本失控在结构上不可能发生**。原对策（模型分级路由等）作为用户侧省钱手段可选保留 |
| R-04        | 题库内容版权风险                                 | 高（法务）                      | 中               | 自有教研编写为主；真题来源合规审查；用户上传内容版权提示与侵权投诉通道                                                                                                        |
| R-05        | 押题/保过类营销的合规风险                            | 高（监管）                      | 低               | NG3 明确禁止；全部文案法务过审；A6 命名为"考点预测"且带免责声明                                                                                                       |
| R-06        | 用户坚持不下来（P8 痛点未解）                         | 中（留存）                      | 高               | 计划动态降档、补卡机制、峰终定律成果卡、连续天数激励、中断后无指责回归                                                                                                        |
| R-07        | 模考成绩预测不准，用户预期落空                          | 中（口碑）                      | 中               | 明确概率口径与免责；只基于 S/A 级题目计算；持续用真实成绩回流校准模型                                                                                                      |
| R-08        | 两科内容深度不足（范围收窄后，深度成为唯一竞争维度）               | 中（口碑）                      | 中               | 范围从 10 专业收到 2 科后，用户对内容深度的期望值上升。**经济基础 6 模块 + 人力 19 章必须全量覆盖且题量充足**（建议每章 ≥ 80 题）；人力按 2026 新框架构建，建立教材变动专题                                     |
| R-09        | 教材年度变动导致内容过期                             | 中                          | 高（每年必变）         | 建立"教材变动 diff"流程：新教材发布后 2 周内完成考点 diff、标注变动点、生成"变动点专项"。**人力 2026 框架已重构，是活的教科书案例**                                                            |
| **R-10** 🔒 | ⚠️ **本地数据丢失**（卸载 App / 换机 / 设备损坏 / 系统崩溃） | **致命**（用户的备考资产全没了，且必然引发差评） | 中               | **最高优先级对策**：① 三重备份机制（M10-F3）；② 达 500 题未备份即触发温和提醒；③ 系统级云备份兜底；④ **卸载前的最后挽留**（iOS 无法拦截，但 Android 可监听卸载意图弹出"记得先备份"）；⑤ 首次启动引导页明确告知"数据存本机，请定期备份" |
| ~~**R-11**~~ 🔒 | ~~内置 AI 通道的 Key 被盗刷~~                        | ~~高（成本）~~                    | ✅ **v1.5 彻底消除**  | **v1.3 因"不做中间商"消除 → v1.4 因中转部分回归 → v1.5 再次消除，且这次是结构性的**：我们**不持有任何 AI 通道令牌**（没有中转层），用户 Key 由系统钥匙串保管且**只在他的设备上**。**"我们的 Key 被盗刷"这一风险从根上不存在。** |
| **R-12** 🔒 | 本地 App 被破解 / 内购绕过                        | 中（收入）                      | 高               | 本地校验的固有弱点，无法根治。缓解：平台收据二次校验、定期联网复核（联网时）。**v1.5 删除"服务端可选校验"这一缓解手段**（违背 NG9/NG15）。**接受一定比例的破解率，把它当作获客** |
| **R-13** 🔒 | 系统级备份不可靠（iCloud/Google 备份是黑盒，用户可能没开启）    | 中                          | 中               | 系统备份只作兜底，**不能作为主方案宣传**。UI 中明确引导用户主动导出备份文件，不夸大系统备份的作用                                                                                       |
| **R-14**    | 人力教材 2026 框架重构，若沿用旧框架资料则全盘皆错             | **高**（内容正确性）               | **高**（网上资料多为旧版） | ① 知识树严格按新三部分框架构建；② 教研必须以 **2026 版教材** 为准，禁止用旧资料；③ 建立术语变更表（如"履历分析→传记资料分析"）并做旧题数据清洗；④ 上线前教研交叉验证章节编号与分值                                       |
| **R-15** 🔒 | ⚠️ **广告伤害体验，导致口碑崩塌**（v1.2 新增，本模式最大风险）      | **致命**（用户流失 + 商店差评 + 品牌受损） | 中               | **产品对策（v1.3 重写）**：① M9-F1 三条铁律（学习心流零广告、主动触发优先、不买学习结果）；② **开屏受 10 条硬约束**（≤5s、可跳过、跳过按钮 ≥44×44pt、离线直接跳过、热启动不展示）；③ **位置从 5 个精简为 4 个**，宁可少赚不用低效位置；④ **渐进释放**（D0 纯净 → D1 开屏 → D3 成果卡 → D6 完全体），广告出现全程可预期；⑤ 每日激励视频上限 6 次；⑥ 疲劳熔断（连续 3 天零点击自动降频）。**监控对策**：① 广告相关卸载率 < 1% 红线；② 商店评论中广告负面占比 < 5%；③ **每周人工阅读商店评论**（数据看得到趋势，看不到情绪）；④ **开屏跳过率**作为素材质量与用户耐受度信号。**兜底对策**：任一红线被突破 → **立即降频 50%，两周内若无改善则直接下线该位置**。宁可少赚，不可失信 |
| ~~**R-16**~~ 🔒 | ~~AI 中转成本随用户增长线性放大~~（v1.2 提出 → v1.3 消除 → v1.4 复活 → **v1.5 永久消除**） | ~~中（毛利）~~                | ✅ **v1.5 已消除** | **v1.5 撤销中转，AI 成本恒为 ¥0，不随用户规模变化。** 三次反复的教训：只要 AI 请求经过我们的服务器，这个风险就会回来；**只要它是纯 BYOK，这个风险就不可能存在。** 本项**不再重新评估**。 |
| ~~**R-20**~~ 🔒 | ~~中转转发层的合规与隐私风险~~（v1.4 新增，**v1.5 撤销**） | ~~高（备案义务 + 隐私声明 + 品牌信任）~~ | ✅ **v1.5 已消除** | **撤销中转后，我们完全不在 AI 请求链路上**，因此不构成《生成式人工智能服务管理暂行办法》意义上的"服务提供者"，**备案义务判定不再适用**。连带消除的还有：分路径话术的虚假宣传风险、内容安全过滤责任、品牌关联风险。**这是 v1.5 最重要的收益之一。** |
| **R-17** 🔒 | ⚠️ **无账号下购买丢失**（换机/换平台账号/渠道停服）            | 高（退款 + 投诉 + 一星差评）           | 中               | ① **恢复购买入口常驻一级页面**（M9-F6）；② 购买前先 restore，**防止重复扣款**；③ **购买凭据随备份文件迁移**（加密存储 + 平台二次校验，独创兜底）；④ 离线宽限期 30 天不做强锁；⑤ 「我的」页常驻显示购买状态与有效期；⑥ 提供人工找回通道（凭支付凭证截图）。**核心原则：宁可放过少量盗版，绝不误伤一个付费用户**                                    |
| **R-18**    | ⚠️ **广告平台选型与产品隐私卖点冲突**（v1.2 新增）           | 中（品牌一致性）                    | 中               | 本产品核心卖点是"数据只在你手机里"。若接入广告 SDK 后大量采集设备标识符做定向投放，**卖点被击穿**。对策：① **优先选择支持非个性化投放的平台**，接受 eCPM 低 20-30%；② 广告 SDK 与业务数据技术隔离；③ 隐私政策中**单列广告数据章节**，坦诚说明；④ 广告类目黑名单（屏蔽教育培训竞品、金融、医美、贷款、游戏充值）                                                                |
| **R-19**    | ⚠️ 免费带来低质量用户，拉低北极星与口碑                        | 中（数据失真）                     | 中               | 免费必然带来大批"下载即弃"用户（行业首日卸载率 40-60%）。对策：① 北极星指标用**"周活跃有效学习用户"**（≥4 天 + ≥20 题），天然过滤掉闲逛用户；② 留存指标看**7 日/30 日**而非次日；③ onboarding 5 步向导本身就是筛子，完成率 ≥70% 说明是真实备考人群；④ **不追求下载量 KPI**，只考核有效学习用户                                                      |

---

## 15. 路线图

> **v1.1 排期说明**：本地存储不是"功能"而是"地基"，必须最先做；备份机制从第一天就要有，不能等到数据丢了再补。

### Phase 0 · 技术地基（约 2-3 周）

| 项            | 内容                                        |
| ------------ | ----------------------------------------- |
| 本地存储架构       | SQLite schema（§11.1）、DAO 层、迁移框架（含迁移前自动快照） |
| 数据隔离         | 内置题库 vs 用户数据隔离方案、增量补丁机制                   |
| **备份恢复（P0）** | 自动快照、导出 `.jjbak`、导入恢复、系统云备份标记             |
| 内置题库打包       | 两科题库 + 精讲内容 + RAG 语料的分包方案与首启动导入           |

### MVP（约 8-10 周）—— 验证「学习科学 + 全真模考」核心假设

| 模块     | 范围                                       |
| ------ | ---------------------------------------- |
| M1 计划  | 单档模板（T-90 标准版）、每日任务包、基础重排                |
| M2 知识树 | **仅《经济基础知识》** 六模块全量知识树 + 图文精讲            |
| M3 刷题  | 章节练 + 今日复习（FSRS-6）+ 即时解析（**静态解析，不接 AI**） |
| M4 错题  | 错题归集 + 延迟重做调度                            |
| M5 模考  | **全真机考界面 + 严格/宽松模式 + 基础复盘**（核心卖点，MVP 必做） |
| M10 备份 | Phase 0 已完成，MVP 阶段做完整验收                  |
| **不做** | AI 全部、自建题库、数据看板、人力科目、商业化                 |

**MVP 成功标准**：7 日知识点保持率 ≥ 65%（行业纯刷题约 35-40%）；模考完成率 ≥ 70%；卸载重装后数据可完整恢复。

### V1.0（约 8-10 周）—— 完整功能 + 商业化上线

| 模块    | 内容                                                                 |
| ----- | ------------------------------------------------------------------ |
| M2    | **《人力资源管理》全量知识树（按 2026 新框架）+ 教材变动专题**                              |
| M6 AI | AI 全矩阵（A1 出题 / A2 解析 / A3 教练 / A4 私教）+ **纯 BYOK 接入（服务商列表 + Key 管理 + 图文教程 + 静态示例）**（M9-F7） |
| M7    | 自建题库（上传 + OCR + AI 生成 + 校验流水线）                                     |
| M8    | 数据看板 + 过线概率预测（**纯本地实现**）                                           |
| **M9** | **开屏 SDK + 激励视频 SDK + 渐进释放引擎 + ¥68 去广告 IAP + 奖励中心 + 恢复购买（M9-F6）** |
| M5 增强 | 两科连考模式 + 完整 AI 复盘报告（含「遗憾分分析」）                                      |
| §8.6  | AI 治理体系完整落地（四级可信度 + 五道校验关卡）                                        |

> **M9 上线顺序（v1.3 更新）**：由于渐进释放机制本身已承担了"由少到多"的节奏控制，**M9 的广告位可以一次性全部开发完成**，无需再分三批上线——**发布节奏由代码内的 D0/D1/D3/D6 时间表控制，而非由发版控制**。
>
> 这与 v1.2 的"三批渐进上线"是不同思路：v1.2 靠**发版**控制节奏（风险在于老用户不会立即升级，导致版本间体验不一致）；v1.3 靠**代码内时间表**控制（每个用户无论何时安装，都经历同样的渐进过程）。**后者更可控，也更公平。**
>
> **开发顺序建议**（先验证风险最高的部分）：
> 1. **开屏 SDK 接入 + 超时跳过逻辑**（风险最高，涉及冷启动性能，必须最早上真机验证）
> 2. 渐进释放引擎（纯本地状态机，逻辑简单但需严格单测）
> 3. 激励视频 SDK + 位置①
> ~~4. **AI 中转转发层**~~ ⛔ **v1.5 删除**（无服务端组件）。AI 部分改为纯前端工作：BYOK 客户端 + 设置页 + 图文教程页 + 静态示例页。**开屏 SDK 性能验证成为唯一的技术验证前置项。**
> 5. 奖励中心（位置②）+ 去广告 IAP + 恢复购买
> 6. **AI 服务商列表 + BYOK 配置向导**（M9-F7，前端工作量较大，但独立于广告，可并行）

### V1.0 冻结的交付边界（v1.4 定稿）

> 为保证"下一步开始实现"时有清晰的范围，以下为**实现阶段的明确边界**：

| 归属 | 内容 |
| --- | --- |
| ✅ **V1.0 必做** | 两科完整知识树与题库；FSRS-6 复习调度；四阶段计划引擎；全真机考模考（含两科连考）；6 维复盘；过线预测；AI 四种能力（出题/解析/教练/私教）**（纯 BYOK）**；**开屏 + 成果卡 + 奖励中心（3 个广告位）+ ¥68 去广告**；备份/恢复/换机；本地数据看板 |
| ⏸️ **V1.0 不做（明确推迟）** | iPad/横屏适配（Q7）；A5 记忆助手与 A6 考点预测；局域网直传换机；匿名上报（L3）；视频微课；其他 8 个专业；Web 端；账号体系 |
| 🔒 **实现期的硬约束** | ① 学习流程零广告；② AI 转发层不留存任何对话内容；③ 无账号、无云端业务数据；④ 广告加载失败绝不影响学习功能 |

### V1.1（约 4-6 周）

- A5 记忆助手（口诀/导图）、A6 考点预测
- 局域网直传换机（M10-F4）
- 「帮助改进产品」匿名上报（§13.4）+ 成绩上报闭环
- 教材年度变动 diff 工具
- 可选：本地向量检索增强包（提升 RAG 质量）

### V2.0（待定）

- 扩展其他专业（工商管理 → 金融 → 财政税收，按需求量排）
- A7 审题训练、M11 学习搭子
- 考后延伸：职称继续教育、高级经济师

---

## 16. 待决问题（Open Questions）

> 🔒 **v1.4 定稿说明**：本版为**PRD 最终版**，所有待决问题已由产品方给出**明确决策**（下述"结论"列）。仅保留 3 项**必须依赖外部输入**的问题（法务、商务、实测），它们**不阻塞开发启动**，但需在对应里程碑前闭环。

### 16.1 已决断（v1.4 定稿，可直接进入实现）

| # | 问题 | **决策结论** | 决策依据 |
| --- | --- | --- | --- |
| **Q1** | 机考是否"按题型分段锁定"？ | **采用「双模设计」，不赌规则。** 默认走**严格模式**（按题型分段、不可回退），同时在设置中提供**宽松模式**（可自由跳转）开关。上线前若实测确认规则，再决定默认值 | 现有资料矛盾，实测窗口在 10 月，与开发并行；双模成本极低（仅涉及导航状态机），**用可控的复杂度换掉一个阻塞型不确定性** |
| **Q3** | 教材版权与真题使用边界 | **硬性规则（不依赖法务即可开工）**：① **内置内容 100% 自研**（精讲、考点摘要、对比表、公式手册、自行整理并重新表述的真题）；② **绝不内置教材原文**；③ 用户上传教材仅作**其个人本地 RAG 语料**，不上传、不分享；④ **不作"押题/命中率"表述**（NG3） | 自研内容路径清晰、无授权依赖，可立即开工；版权风险通过"零原文内置"从根本上规避 |
| **Q6** | FSRS-6 中文客观题参数调优 | **先上默认参数，后按数据标定。** 上线即用 FSRS-6 默认权重；积累 ≥ 500 名用户 × 30 天数据后，用本地数据做一次参数拟合 | FSRS 默认参数在通用场景表现已显著优于 SM-2；调优收益是"锦上添花"，不应阻塞主线 |
| **Q7** | iPad / 横屏支持 | **V1.0 明确支持（支持，但非优化）。** 保证 iPad 上**可正常使用**（不崩、不错位），但**不做横屏专项布局**。专注手机竖屏体验 | 本地架构下多端数据不互通，iPad 用户无法与手机共享进度 → 投入产出比低；但"可用"是底线，不能假装没有这类用户 |
| **Q9** ⚠️ | AI 接入方案选择 | 🔄 **v1.5 最终决断：纯 BYOK**（**撤销 v1.4 的中转双路径**）。<br>· **唯一路径**：用户自选服务商 → 自填 API Key → 设备直连，**无限使用、不经我们**；<br>· **未配置时**：AI 不可用，但通过**静态示例 + 图文教程 + 三处明示**完成价值传递；<br>· **无额度、无中转、无服务端**。 | v1.4 的账算得没错（¥1.0/用户买触达率），**但它默认了"有人值守"这个前提**。对个人开发者，**任何需要值守的服务都是成本黑洞**——不是钱的黑洞，是心力的黑洞。v1.5 放弃触达率，换回**零成本 + 零运维 + 零法务风险**。**本项已彻底关闭，不再重新评估。** 详见 M9-F7 |
| **Q11** | 本地数据库是否加密 | **默认不加密；备份文件导出时提供加密选项（默认开启）。** 数据库依赖系统沙箱保护；备份文件为可迁移资产，必须默认加密 | 数据库加密牺牲 5-15% 读写性能，且系统沙箱已提供主要保护；风险真正集中在"备份文件外泄"这一场景，精准加密比全库加密更合理 |
| **Q12** | 题库打包 vs 首启下载 | **混合方案**：核心题库（两科高频考点 + 近 5 年真题）**打包进安装包**；扩展题库与知识树详情**首次启动后台静默下载** | 打包保证"下载即可用"（备考用户对首启体验极敏感）；扩展内容下载控制包体积在商店 OTA 限制内 |
| **Q13** | Android 渠道 IAP 方案 | **主通道：各国内商店自有支付**（华为/小米/OPPO/vivo）；**统一抽象层**屏蔽差异。**无需 Google Play** | 国内 Android 渠道本就以厂商商店支付为主；抽象层避免逐个适配的重复劳动 |
| **Q14** ⚠️ | 广告平台选型 | **决策：优先选支持非个性化投放的平台 + 分平台并行。** 具体：① 首选**支持非个性化投放**（eCPM 低 20-30% 可接受，见 Q18）；② **同时接入 2 家做填充率兜底**；③ **类目黑名单必配**（屏蔽教育培训竞品、金融、医美、贷款、游戏充值）；④ 必须支持**自定义跳过按钮样式**（Q19 的底线要求） | 单平台填充率不足会直接导致收入波动；双平台 + 类目黑名单是行业标准做法。**注意：平台商务谈判可与开发并行，先用测试广告位开发** |
| **Q15** | ¥68 / ¥128 定价 A/B | **按 ¥68 上线，不作 A/B。** 上线 3 个月后若去广告购买率 > 5%，**评估上调至 ¥88**（新用户新价，老用户不受影响） | 无服务端 A/B 精度低、样本积累慢；先上线再观察更务实。**从 ¥68 起而非更高，是为了先积累购买习惯与口碑** |
| **Q16** | ~~免费 AI 额度定 5 次/日~~ | ✅ **v1.5 最终结论：不做任何 AI 额度**（日额度、一次性额度、平台额度**全部取消**）。平台无额度概念，AI 使用量由用户自己的服务商账号决定。 | v1.2 的日额度 = 持续成本（已证伪）；v1.4 的一次性额度 = 获客成本（对个人开发者仍是负担）；**v1.5 直接取消额度体系，问题不存在** |
| **Q17** | 去广告"本考季"有效期定义 | **硬截止（当年 12-31）+ 60 天保护期。** 即：**若购买日在考前 60 天内（约 10 月中之后），有效期自动延长至次年 6 月 30 日** | 纯硬截止会产生"11 月购买、12 月过期"的荒谬体验；纯滚动 12 个月又让"本考季"概念失效。**硬截止 + 保护期兼顾二者** |
| **Q18** | 非个性化投放的 eCPM 损失权衡 | **决策：优先非个性化，可接受 ≤ 25% 的 eCPM 损失**（原定 15%，上调） | 本产品核心卖点是"数据只在你手机里"（NG10/R-18）。**品牌一致性的长期价值 > 短期单价差额**。25% 是新划的容忍上限，超过则重新评估 |
| **Q19** | 开屏跳过按钮的位置与样式 | **决策（不可协商的底线）**：**右上角**、**≥44×44pt 热区**、**展示 2s 后出现**、**点击立即进入**、**不遮挡任何功能入口**、**不误导**（不做"伪跳过"引导点击）。<br>**加分项**：跳过按钮带**倒计时数字**（如"3s 跳过"），比纯图标更友好也更少误触 | 右上角符合国内用户肌肉记忆；倒计时提升确定性感受。**若某平台无法满足底线 → 不接该平台的开屏位**（此时宁可用激励视频补收入） |
| **Q20** | 渐进释放时间表是否按活跃度动态调整 | **决策：以「活跃天数」为主，设自然日兜底。** 即 `阶段推进条件 = 活跃天数达标 OR 自然日达 2 倍阈值（取先到者）`。<br>例：D3 阶段 = 活跃 3 天，或自然日 6 天。<br>⚠️ **v1.5 实现期补充**：自然日兜底**必须带「距上次活跃 ≤ 3 天」前置条件**，且活跃天数为 0 时兜底不生效（见 M9-F4 关键设计约束 5）。 | 纯活跃天数会让低频用户长期处于"低广告"状态（收入受损）；纯自然日又会让"装了两周只用一天的"用户突然面对全部广告位（体验受损）。**取先到者，兼顾两侧**。但**无条件的兜底会误伤"长期放置后回归"的用户**（实测：放置 19 天回归会被直接顶到完全体），故加前置条件区分「间歇性活跃」与「流失后回归」 |
| **Q21** | AI 相关合规（**v1.5 大幅降级，不再是阻塞项**） | **决策：仅保留两条轻量义务。**<br>① **告知 + 免责**：设置页告知"AI 由第三方提供、请求由设备直发、我们不接收不存储"；免责声明法务必审；<br>② **返利披露**（若启用）：推广链接标注「推广」并披露合作关系，**建议先按"无返利"上线**。<br>**备案义务判定不再适用**（我们不在服务提供者链路上，R-20 撤销）。 | **v1.5 的架构本身就是最彻底的合规方案**——不接触用户数据、不转发请求、不存储内容，**合规压力随着中转的撤销一并消失**。原"保留可退守纯 BYOK 的能力"这一韧性设计，现已直接成为产品本身。 |

### 16.2 需外部输入（不阻塞开发启动，但需在里程碑前闭环）

| # | 问题 | 需要的输入 | 最迟闭环时间 | 若未闭环的兜底方案 |
| --- | --- | --- | --- | --- |
| **Q10** ⚠️ | 2026 版《人力资源管理》教材 | **教研须取得中国人事出版社 2026 版教材**，并交叉验证章节编号与分值权重 | **M2 开发启动前**（约第 3-4 周） | 若教材未到，**先做《经济基础知识》全量**，人力科目知识树延后但仍按新框架预留结构（数据模型不受影响） |
| ~~**Q21-a**~~ | ~~中转层的备案判定~~ | ⛔ **v1.5 已删除**（无中转层，判定不适用） | — | — |
| **Q14-a** | 广告平台商务条款 | eCPM 报价、分成比例、类目屏蔽能力确认、结算周期 | **代理接入前**（约第 4 周） | 先用测试广告位完成集成开发，商务条款到位后切换正式 ID |

### 16.3 已解决 / 已失效（历史记录）

| # | 问题 | 状态 |
| --- | --- | --- |
| ~~Q2~~ | ~~首期覆盖哪些专业~~ | ✅ **已解决（v1.1）**：《经济基础知识》+《人力资源管理》 |
| ~~Q4~~ | ~~LLM 成本能否支撑 ¥268 全程班~~ | ✅ **已失效**（v1.1 改 BYOK 后成本模型重构） |
| ~~Q5~~ | ~~"不过重学"条款与风控~~ | ✅ **已解决（v1.1）**：改为「二战支持计划」 |
| ~~Q8~~ | ~~用户上传资料的存储成本~~ | ✅ **不适用（v1.1）**：本地存储无云端成本 |
| ~~Q9（v1.3 结论）~~ | ~~纯 BYOK + 搭桥引导，不做中转~~ | ✅ **v1.5 回归此结论**（v1.4 的中转仅存活一版即被撤销，见 16.1 Q9） |
| ~~Q16（v1.3 结论）~~ | ~~一次性 20 次体验额度~~ | ✅ **v1.5 彻底取消**（连一次性额度也不要了，见 16.1 Q16） |

---

### 定稿签署（v1.4）

| 项 | 内容 |
| --- | --- |
| **版本状态** | ✅ **PRD 定稿，可进入功能实现阶段** |
| **未决事项** | 无阻塞性未决项。仅 3 项外部输入待闭环（Q10 教材 / Q21-a 法务 / Q14-a 商务），均有兜底方案 |
| **实现期最高优先级** | ① **开屏 SDK 冷启动性能验证**（唯一的硬技术前置项）；② BYOK 客户端 + 设置页 + 教程页 + 示例页（纯前端，无后端依赖） |
| **实现期不可逾越的约束** | 学习流程零广告 / **无任何需要我方服务器在线的功能（NG15）** / 无账号与无云端业务数据 / 广告与 AI 失败均不影响学习 / **AI 必须三处明示"可选增强"（NG16）** |
| **下一动作** | 按 §15 的开发顺序启动。**开屏 SDK 性能验证为唯一前置项**；AI 侧无任何法务/后端阻塞，可直接实现。 |
---

## 附录 A：文档修订记录

| 版本             | 日期         | 修订内容                                                                                                                                                | 作者  |
| -------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| v1.0           | 2026-09-03 | 初稿，含完整需求、AI 设计与治理方案                                                                                                                                | 产品通 |
| **v1.1**       | 2026-09-03 | **架构级重构**：                                                                                                                                          |     |
|                |            | ① 转为**纯本地存储**（无远程数据库、无账号、无同步）→ 重写 M9 商业化、M10 数据与备份、§11.1 本地持久化、§12 非功能、§13.4 埋点方案，新增 §8.8 AI 联网边界设计                                                      |     |
|                |            | ② 科目范围收窄为**《经济基础》+《人力》** → 新增 §1.1-B 人力科目结构（2026 新框架）                                                                                               |     |
|                |            | ③ 新增风险 R-10~R-14（本地数据丢失、Key 盗刷、内购破解、教材框架重构等）④ 路线图重排（新增 Phase 0 技术地基）⑤ 待决问题更新：Q2/Q4/Q5/Q8 已解决或失效，新增 Q9~Q13                                          |     |
| **v1.2**       | 2026-09-20 | **商业模式换轨：App 免费 + 广告变现 + 付费去广告**                                                                                                                    | 产品通 |
|                |            | ① **M9 全章重写**（v1.1 的"平台 IAP 买断制" → "免费 + 广告驱动"）：新增 M9-F0 收入结构、M9-F1 **三条铁律**、M9-F2 免费版权益矩阵、M9-F3 五个广告位逐个设计、M9-F4 频次控制、M9-F5 ¥68 去广告定价、M9-F6 **无账号下的购买/恢复/风控**、M9-F7 AI 成本闭环、M9-F8 二战支持计划、M9-F10 财务模型、**M9-F11 离线广告行为**、M9-F12 联动检查清单 |     |
|                |            | ② **核心能力全部免费**：题库、模考、6 维复盘、FSRS 复习、计划引擎、过线预测不再锁付费墙（新增 NG11、NG12）                                                                                     |     |
|                |            | ③ §5.3 反暗黑模式追加 6 条广告相关禁止项；④ §2 目标调整；⑤ §7.0 功能地图「订阅」→「奖励中心」；⑥ §13.2 指标体系替换；⑦ 新增风险 **R-15~R-19**；⑧ 路线图给出三批渐进上线顺序；⑨ §16 新增 Q14~Q18                                       |     |
| **v1.3**       | 2026-09-20 | **广告结构重构 + AI 退出中间商**                                                                                                                              | 产品通 |
|                |            | ① **引入开屏广告为变现主力**（M9-F3 位置⓪）：设计 10 条硬约束（≤5s、2s 后可跳过、跳过按钮 ≥44×44pt、离线直接跳过、热启动不展示、冷启动不阻塞首屏等）；**修订 M9-F1 的 T2 铁律**（由"只做主动触发"改为"主动触发优先 + 开屏为唯一被动例外"） |     |
|                |            | ② **主动削减 2 个广告位**：删除「今日结算页加练位」（污染正向收尾）与「内容页原生卡片」（eCPM 低且最易引发反感），广告位由 5 个 **精简为 4 个**                                                                |     |
|                |            | ③ **取消"考前 7 天全部关闭广告"**（会让广告的出现不可预测，产生"被欺骗感"），改为 **M9-F4 渐进释放机制**：D0 纯净 → D1 开屏 → D3 成果卡 → D6 完全体，一次只引入一个新位置，基于活跃天数计算                                                       |     |
|                |            | ④ **AI 改为纯 BYOK + 搭桥引导**（M9-F7 重写）：平台**不做 AI 中间商**，取消持续免费额度（改为一次性 20 次体验），**明确否决轻量中转方案**；新增"内置通道配置指引"（选服务商 → 直达官网 → 粘贴测试），收益为 **AI 成本归零**（从 ¥9 万/月 → ≈¥0）<br>📌 *此条在 v1.4 被推翻、在 **v1.5 被重新确认并强化***                     |     |
|                |            | ⑤ 新增 **NG13**（不做 AI 中间商）；§8.7 成本红线重写；§8.8.3 数据外流边界强化（我方完全不在 AI 链路上）；§8.8.4 新增"未配置 Key"与"额度用完"降级路径                                              |     |
|                |            | ⑥ §5.3 反暗黑模式修订（开屏由禁止改为"允许但受 10 条硬约束"，新增"禁止广告出现/消失不可预期"）；§10.2 组件规范更新                                                                       |     |
|                |            | ⑦ §12 新增**开屏性能要求**（冷启动 ≤2s 优先于广告加载，1.5s 未就绪直接跳过）与 **BYOK 安全要求**；§13.2 指标扩充（开屏占比/跳过率/加载成功率/BYOK 配置率），ARPDAU 目标 ¥0.11 → **¥0.15**                 |     |
|                |            | ⑧ §14 风险：**R-15 重写**（对策更新为开屏硬约束 + 渐进释放）；**R-16 标记为已消除**（AI 成本归零）📌 *v1.4 复活、v1.5 再次消除*；R-18 补充开屏相关考量                                                                      |     |
|                |            | ⑨ §15 路线图：M9 由"三批发版控制"改为"**代码内时间表控制**"，并给出 5 步开发顺序；§16 **Q9 标记已解决**、Q16 标记失效、新增 **Q19~Q21**（跳过按钮样式、渐进释放时间表校准、AI 返利合规）                    |     |
| ~~**v1.4**~~   | ~~2026-09-20~~ | ⛔ **本版决策已被 v1.5 撤销**（AI 中转双路径方案作废，回归纯 BYOK）。**保留记录仅供追溯。** 原文：~~AI 接入方案定稿：BYOK + 中转双路径（推翻 v1.3 的"明确否决中转"）＋ 全部待决问题决断~~                                                                                             | 产品通 |
|                |            | ① **M9-F7 整节重写**：AI 改为**双路径并行**——**路径 B 中转**（新手默认：服务商列表 → 选择 → **直接可用**，无需注册/无需 Key，赠 50 次额度）＋ **路径 A BYOK**（进阶：自填 Key，无限使用、不经我们）。新增**四条路径对比**、**与 NG9 的关系澄清**（无状态转发层不构成"应用服务端"）、**成本/额度/防刷/降本三招**设计、**按路径分别的降级与免责**。**显式记录推翻 v1.3 决策的理由**（v1.3 误以为"中转=承担成本"，实际漏斗断点在"配置 Key"这一步） |     |
|                |            | ② **服务商列表支持"直接调用"**：去技术化的 5 家服务商卡片（DeepSeek/Kimi/通义千问/智谱/OpenAI），带场景化标签；**服务商排序由客观评分卡决定（中文 40%/成本 30%/稳定 30%），返利不得进入排序权重**；保留 BYOK 的"直达官网申请 Key（带推广标识）"外链与图文教程 |     |
|                |            | ③ **成本模型重构（M9-F10）**：AI 成本由 v1.3 的 ≈¥0 改为 **≈¥1.0/新用户**（缓存降本后 ≈¥0.65）；新增**成本量级测算表**（1万/5万/10万新增用户的占比）与**两道自动止损闸**（额度梯度收紧：>5万降至30次、>10万降至15次；**成本/收入 >20% 自动收紧、>30% 暂停赠送**）。**新增 P0 监控指标**：中转额度消耗率、AI 中转成本占比 |     |
|                |            | ④ **§3 非目标修订**：**NG13** 由"不做 AI 中间商"改为"**不做 AI 额度生意，只做技术通道**"（明确四条红线）；**新增 NG14**（不做需要账号的 AI 会员/订阅制） |     |
|                |            | ⑤ **§8.7 成本红线重写**（双路径成本表 + 自动止损）；**§8.8.3 数据外流边界按路径分列**（BYOK 不经我们 / 中转经转发但**不留存**），**明确禁止用 BYOK 话术描述中转**（虚假宣传风险）；**§8.8.4 降级路径修正**（未配置 Key 不再"待激活"，默认中转可用；额度耗尽 → 三条出路）；**§8.8.5 新增 6 条中转专项研发要求** |     |
|                |            | ⑥ **§14 风险**：**R-16 重新激活**（AI 成本随增长线性放大，配三层闸门对策）；**新增 R-20**（中转层合规与隐私风险，**本版最大法务风险**：备案判定 + 不留存可证明 + 分路径披露 + 内容安全过滤） |     |
|                |            | ⑦ **§15 路线图**：M6 AI 改为"双路径接入"；开发顺序扩为 **6 步**（新增"AI 中转转发层"，且**法务与安全评审先行**）；**新增「V1.0 冻结的交付边界」表**（必做 / 不做 / 硬约束），为进入实现阶段划定范围 |     |
|                |            | ⑧ **§16 待决问题全面决断（本版核心动作之一）**：重构为三区——**16.1 已决断**（Q1 双模设计、Q3 零原文内置、Q6 先默认后标定、Q7 支持但非优化、Q9 双路径、Q11 备份加密、Q12 混合打包、Q13 国内商店支付、Q14 非个性化优先+双平台、Q15 ¥68 先行、Q16 一次性50次、Q17 硬截止+60天保护、Q18 容忍25%损失、Q19 跳过按钮底线、Q20 活跃天数为主+自然日兜底、Q21 分三步稳妥路线）；**16.2 需外部输入**（Q10 教材 / Q21-a 法务 / Q14-a 商务，均附兜底方案）；**16.3 已解决/已失效**；新增**「定稿签署」表** |     |
|                |            | ⑨ §5.3 / §10.2 / §13.2 联动同步；附录 A 补 v1.4 记录 |     |
| **v1.5**       | 2026-09-20 | **撤销 AI 中转，全面回归无服务端（推翻 v1.4 变更 E）＋ AI 定位降级为"可选增强"**                                                                                            | 产品通 |
|                |            | ① **M9-F7 整节重写**：AI 改为**纯 BYOK**——删除"路径 B 中转"全部设计（服务商列表直用、50 次额度、令牌下发、防刷、降本三招、限额闸门）。新增三节核心内容：**① AI 的产品定位（可选增强，非常核心）**、**② 零服务端成本的补偿设计**（静态示例解析 + Key 图文教程 + 三处明示"不影响学习" + 可选返利）、**③ 未配置降级路径**。**显式记录推翻 v1.4 的理由**（中转与无服务端基石冲突；v1.4 的成本模型默认了"有人值守"这个对个人开发者不成立的前提） |     |
|                |            | ② **§3 非目标**：**NG13 回归并强化**（不做任何 AI 转发/代理/中转 + 无额度体系 + 六条红线）；**NG14 扩围**（AI 能力不得与付费/账号/等级挂钧）；**新增 NG15**（不做任何需要我方服务器在线的功能 —— **架构总闸**）；**新增 NG16**（不把 AI 当作可靠承诺 + AI 异常不得阻断学习路径） |     |
|                |            | ③ **成本模型归零（M9-F10）**：AI 成本由 v1.4 的 ≈¥1.0/新用户 改为 **¥0（永久，不随规模变化）**；删除成本量级测算表与两道自动止损闸；财务指标表中"中转额度消耗率""AI 中转成本占比"两项**删除**，"BYOK 配置率"由 KPI 降为**仅监控、不设目标** |     |
|                |            | ④ **§8.8 联网边界单路径化**：§8.8.3 数据外流表**删除路径 B 行与用量计数行**，恢复单列表述；隐私话术由"按路径分述"简化为**单一句式**（"请求由你的设备直接发送，我们不经过、看不到、不留存"）；§8.8.4 降级表**删除额度耗尽与通道故障行**，改写为"未配置 Key 是正常状态（非错误态）"；§8.8.5 删除全部中转专项要求，新增 **AI 异常隔离硬约束** |     |
|                |            | ⑤ **§8.6.5 / §8.7 / §11 / §12 联动**：删除中转免责版本（只留 BYOK 免责）；§8.7 成本红线删除止损闸并说明 AI 成本永久为 ¥0；§11 数据模型 **`AIProvider` 降级为内置常量**、**删除 `AIQuota` 实体**、`AIAccess` 改名 `AIConfig` 并明确"无任何额度字段、不进备份"；§12 删除"中转层安全"整条，改为"AI 客户端安全" |     |
|                |            | ⑥ **§M9-F3 广告位精简为 3 个**：**删除"位置③ 看视频换 AI 额度"**（无额度体系，该场景不存在）。连带收益：**消除了 v1.4 中"看视频换额度净亏"的问题**（当时收入 ¥0.04 < 成本 ¥0.10）。广告位现为：**开屏 / 成果卡 / 奖励中心** |     |
|                |            | ⑦ **§14 风险表**：**R-20 撤销**（不在 AI 链路上，备案义务不适用 —— 这是 v1.5 最重要的收益）；**R-16 永久消除**（AI 成本不随规模变化，标注"不再重新评估"）；**R-11 结构性消除**（我们不持有任何通道令牌）；**R-03 消除**（AI 成本恒为 ¥0）；R-12 删除"服务端可选校验"这一违背 NG9 的缓解手段 |     |
|                |            | ⑧ **§15 路线图**：M6 AI 改为"纯 BYOK 接入 + 图文教程 + 静态示例"；开发顺序**删除"AI 中转转发层"步骤**（无服务端组件），**开屏 SDK 性能验证成为唯一技术前置项**；V1.0 交付边界表同步修订（AI 标注纯 BYOK、广告位标注 3 个） |     |
|                |            | ⑨ **§16 待决问题**：**Q9 最终决断为纯 BYOK 并彻底关闭**（"本项不再重新评估，除非产品从个人开发变为有专职运维的团队产品"）；**Q16 升级为"不做任何 AI 额度"**（日额度、一次性额度、平台额度全部取消）；**Q21 大幅降级**（备案判定不适用，仅剩告知+免责与返利披露两条轻量义务，优先级 P0 → P2）；**删除 Q21-a**（中转层备案判定，不再适用） |     |
|                |            | ⑩ **代码侧同步实现**：删除 `app/server/` 整目录（含中转服务 3202 文件）、`src/infrastructure/ai/aiRouter.ts`、`src/domain/ai/quota.ts`；新建 `src/infrastructure/ai/aiClient.ts`（纯 BYOK 客户端）；重写 `src/domain/ai/providers.ts`（删 `relaySupported`，加 `consoleUrl` 与 `AI_IS_OPTIONAL_NOTICE` 常量）；重写 `AiSettingsPage.vue`（单卡片布局）；改造 `AiPage.vue`（去除额度概念）；**新增 `src/domain/ai/samples.ts` + `AiSamplePage.vue` + `AiKeyTutorialPage.vue`**（零服务端补偿方案） |     |
| **v1.5+**      | 2026-09-20 | **广告模块进入编码阶段后发现的实现期校正（2 处，不改变产品方向）**                                                                                                                | 产品通 |
|                |            | ① **修正自然日兜底的「意外加速」漏洞**（M9-F4 关键设计约束 5）：兜底原为无条件生效，导致「只用了 1 天就放置、19 天后再打开」的用户被自然日兜底（19 ≥ 6×2=12）**直接顶到阶段 3 完全体**，与 M9-F4 明文要求「放置 30 天后开始用应从阶段 0 开始」**直接冲突**。修正：兜底增加前置条件 **距上次活跃 ≤ 3 天**；且**活跃天数为 0 时兜底不生效**（一次都没学过，不该被推进广告阶段）。代码落点 `releaseEngine.ts` 的 `NATURAL_FALLBACK_MAX_GAP_DAYS` / `daysSinceLastActive()`。附 **Q20 同步更新** |     |
|                |            | ② **清理 `ai_quota` 广告位残留**（4 处）：v1.5 将广告位由 4 个减为 3 个后，代码中仍残留 `ai_quota` —— ① `AdSlot` 联合类型；② `SLOTS_BY_STAGE[3]`；③ 单测断言；④ `RemoveAdsPage.vue` 权益文案。全部清理并与「3 个广告位」对齐，**代码注释中写明禁止重新引入**。同时修正一处旧单测断言（原断言「阶段 3 新增 AI 额度位」，v1.5 阶段 3 不再新增广告位） |     |
|                |            | ③ **广告模块代码侧实现**（本轮）：新增 `src/domain/ads/frequencyCap.ts`（M9-F4 ① 六条频次规则完整落地：开屏 12 次/日、激励 6 次/日、90s 间隔、同位置 3 次/日、疲劳熔断、模考日降频）；新增 `src/infrastructure/ads/adAdapter.ts`（适配器接口 + 错误边界 + 超时熔断）+ `mockAdAdapter.ts`（行为可配置仿真器）；新增 `src/services/ads.ts`（AdService，闸门串联顺序：环境 → 去广告 → 释放 → 频次 → 熔断 → 调用）+ `adAdapterFactory.ts`（接入真实 SDK 时**唯一需要改的文件**）；新增 `src/pages/mine/RewardCenterPage.vue`（位置② 宿主）+ 4 个广告组件（`AdSlotView` / `SplashAd` / `ResultCardAd` / `AdErrorBoundary`）+ `AdNoticeDialog.vue`（首次启动告知）；扩展 `releaseEngine.ts` 支持「买断 + 限期体验」两种去广告来源；单测由 26 项增至 **77 项** |     |

> ⚠️ **广告 SDK 接入状态说明（v1.5+ 重要的待办标记）**：
> 当前采用「**适配器层 + Mock 实现**」策略 —— 上层业务（渐进释放 / 频次控制 / 三个广告位 UI / 超时降级 / 错误隔离 / 失败即赠送）已在浏览器环境完整跑通并单测覆盖，但**尚未接入任何真实广告 SDK**。
>
> 未接入的原因与前置条件：
> 1. **芒果联盟 SDK 是闭源商业聚合 SDK，且为 Native（Android/iOS）形态** —— 与当前 PWA 形态存在差异，需先安装 Capacitor 并生成原生工程（`@capacitor/core` + `@capacitor/android` 目前**未安装**）。
> 2. **需要注册后才可获得 `APPID` + `AppKey` + `PlacementId`** —— 目前尚未申请。
> 3. ⚠️ **包名必须在芒果联盟后台创建应用时确定且创建后不可修改**（当前 `capacitor.config.ts` 中为 `cn.jingshi.ai`）—— 申请前需确认此包名正确。
> 4. ⚠️ **合规红线：SDK 初始化必须在用户同意隐私政策之后执行**。合规落点已预留（`AdNoticeDialog.vue`），但需与 onboarding 流程的隐私同意步骤对接。
>
> 接入方式：按 `src/services/adAdapterFactory.ts` 头部注释的 6 步操作，**业务层零改动**。

## 附录 B：参考来源

1. 河南省人事考试中心《关于做好 2026 年度初级、中级经济专业技术资格考试考务工作的通知》（人考中心函〔2026〕24 号）
2. 鄂尔多斯市人力资源和社会保障局《2026 年度初级、中级经济专业技术资格考试报名的通知》
3. 中国人事考试网机考模拟系统操作指南（2026 年度）
4. Roediger, H. L., & Karpicke, J. D. (2006). Test-enhanced learning. *Psychological Science*
5. Rohrer, D., & Taylor, K. (2007). The shuffling of mathematics problems improves learning. *Instructional Science*
6. Cowan, N. (2001). The magical number 4 in short-term memory. *Behavioral and Brain Sciences*
7. FSRS (Free Spaced Repetition Scheduler) - 开源间隔重复调度算法
8. 《生成式人工智能服务管理暂行办法》《中华人民共和国个人信息保护法》《中华人民共和国广告法》
9. **《互联网广告管理办法》**（v1.2 新增 —— 广告标识义务、激励广告规范）
10. **App Store 审核指南 3.1.1**（v1.2 新增 —— 虚拟商品 IAP 强制、恢复购买要求）
11. **IAB Rewarded Video / 激励视频广告行业基准**（v1.2 新增 —— eCPM 与用户接受度参考）

---

**⚠️ 重要提示**：本文档中的考试规则（§1.1）基于 2026 年度公开考务文件整理。考试政策存在年度调整可能，**产品上线前必须由教研团队以中国人事考试网最新官方文件为准复核一遍**，特别是 Q1 的机考交互规则。
