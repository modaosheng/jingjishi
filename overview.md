# 广告模块落地（第 1–4 步）：频次控制 + 适配器层 + 三个广告位 UI

> 日期：2026-09-20 ｜ 范围：广告功能
> 验证：**类型检查零错误 ｜ 单测 77/77 ｜ 构建成功 ｜ 11 条路由冒烟 200**

---

## 一、你问的问题：没有广告位 ID，能不能先开发？

**能，这轮已经做完了。** 但有两件事需要先纠正：

| 你的认知 | 实际情况 |
| --- | --- |
| 芒果联盟**开源**广告 SDK | ❌ 它是**闭源商业聚合 SDK**（芒果TV 旗下）。无源码、无社区版 |
| 有 ID 就能接 | ⚠️ 它是 **Native SDK**（Android/iOS）。当前是 PWA，**要先装 Capacitor + 真机**才能跑 |

**真正的阻塞不是「没有广告位 ID」，而是「Capacitor 还没装」。** 广告位 ID 反而是最后一个才需要的东西。

### 采用的策略：适配器层 + Mock

```
上层业务（今天全部做完，零改动）
  ├─ 渐进释放 / 频次控制 / 免广告判定       ← 纯本地逻辑，77 项单测兜底
  ├─ 三个广告位 UI + 超时降级 + 失败即赠送
  └─ 崩溃隔离错误边界
        ↓ 只依赖一个窄接口 AdAdapter
   ┌─ MockAdapter    ← 现在用这个，浏览器全套跑通（含 5 种故障仿真）
   └─ MangoAdapter   ← 拿到 ID 后新增，**只改 adAdapterFactory.ts 一个文件**
```

---

## 二、本轮交付

### 第 1 步：清掉 4 处 `ai_quota` 残留

v1.4 撤销 AI 额度体系时的漏网之鱼，与 v1.5「3 个广告位」不一致：

- `releaseEngine.ts` — `AdSlot` 联合类型、`SLOTS_BY_STAGE[3]`
- `tests/releaseEngine.test.ts` — 单测断言（含一处旧断言「阶段 3 新增 AI 额度位」）
- `RemoveAdsPage.vue` — 权益文案 `<li>移除 AI 额度广告位</li>`

已在代码注释中写明**禁止重新引入**。

### 第 2 步：补全 M9-F4 频次控制（原为完全缺失）

新建 `src/domain/ads/frequencyCap.ts`，落地 PRD 全部六条规则：开屏 ≤12 次/日、激励视频 ≤6 次/日、90 秒最短间隔、同位置 ≤3 次/日、疲劳熔断（连 3 天零点击降频 50%）、模考日降为 3 次。

### 第 3 步：适配器层 + Mock 实现

| 文件 | 职责 |
| --- | --- |
| `infrastructure/ads/adAdapter.ts` | 接口定义 + `withAdErrorBoundary()` 错误边界 |
| `infrastructure/ads/mockAdAdapter.ts` | 行为可配置仿真器，模拟 success/offline/timeout/no_fill/**crash** 五态 |
| `services/ads.ts` | `AdService`，闸门串联 + 硬超时 + 会话熔断（连 3 次失败禁用） |
| `services/adAdapterFactory.ts` | **接入真实 SDK 时唯一需要改的文件**（头部含 6 步操作说明） |

`AdService` 闸门顺序（这个顺序本身是产品决策）：

```
环境支持 → 已去广告 → 渐进释放 → 频次额度 → 会话熔断 → 调用适配器 → 记账
```

「已去广告」排在「渐进释放」**之前** —— **付费用户的判断路径最短**，不该因为任何广告逻辑多绕一步。

### 第 4 步：三个广告位 UI + 告知弹窗

| 组件/页面 | 挂载点 | 关键约束 |
| --- | --- | --- |
| `SplashAd.vue` | `App.vue` 全局层 | 1.5s 硬超时、仅冷启动（sessionStorage 判定）、可跳过、超时即 abort |
| `ResultCardAd.vue` | `QuizPage.vue` 完成结算视图 | 挂在**主按钮之后**（T2 主动触发优先）、绝不插入答题过程（T1 心流禁投区） |
| `RewardCenterPage.vue` | 新路由 `/mine/reward-center` | 用户完全主动进入、任务墙 + 免广告天数兑换 |
| `AdSlotView.vue` | 三处共用 | 未释放/已去广告/超限 → **整个节点不渲染**；失败 → 静默移除，**绝不出现"加载失败"** |
| `AdErrorBoundary.vue` | 两处包裹 | SDK 崩溃隔离，子树抛错不冒泡 |
| `AdNoticeDialog.vue` | `App.vue` | 首次启动合规告知，只展示一次 |

同时扩展了 `releaseEngine.ts`：去广告状态升级为「买断 + 限期体验」双来源（此前只有二元布尔值，无法承载奖励中心的核心机制）。`MinePage.vue` 新增「奖励与购买」一级区块。

---

## 三、过程中发现并修正的 3 个实质问题

### 1. 🔴 自然日兜底会产生「意外加速」（真实漏洞）

**场景**：用户 9/1 下载、只用了 1 天就放置，9/20 再打开 → 活跃天数 1（本该停阶段 1），但自然日 19 ≥ 6×2=12 → **被兜底直接顶到阶段 3 完全体**。

**与 PRD M9-F4 明文冲突**：「放置 30 天才开始用，应从阶段 0 开始」。

**修正**：兜底增加前置条件 **「距上次活跃 ≤ 3 天」**，且**活跃天数为 0 时兜底不生效**。由此区分「间歇性活跃」（周末学习者，兜底正常生效）与「长期放置后回归」（兜底失效）。

已回写 PRD（变更 E+、M9-F4 约束 5、Q20）。

### 2. 🟡 免广告天数「两头都算一端」，多发 1 天

`grantAdFreeDays(1)` 后剩余显示 2 天、`grantAdFreeDays(30)` 显示 31 天。根因是 `trialUntil` 语义未定清。

**修正**：明确 `trialUntil` = **最后一个有效日的日期键**（不是失效时刻）。

### 3. 🟡 连续兑换奖励会「吞掉一天」

`grantAdFreeDays(1)` 后再 `grantAdFreeDays(7)`，用户只得 7 天而非 8 天 —— 续期从「原到期日当天」重算，把已有一天覆盖了。

**修正**：续期起点改为「原到期日的**次日**」。

> 这两个 bug 在真机上极难发现（用户只会觉得"怎么少了一天"），但单测里一眼暴露。这就是为什么本轮把单测从 26 项做到 77 项。

---

## 四、验证结果

| 项目 | 结果 |
| --- | --- |
| `npm run typecheck` | ✅ 零错误 |
| `npm test` | ✅ **77/77**（原 26 项） |
| `npm run build` | ✅ 成功（新页面 chunk 均产出） |
| 路由冒烟 | ✅ 11 条全部 200 |
| 产物校验 | ✅ `reward_center` / MockAdapter 正确进入 `services` 分包 |

### 新增测试基建

Node 测试运行器不认 `@/` 别名，也不做扩展名补全 —— 导致业务模块无法被直接单测。

**解决**：`tests/_aliasLoader.mjs` + `_registerLoader.mjs`（Node 原生 Loader Hooks，**零第三方依赖**），业务代码保持 Vite 风格不变。已接入 `npm test`。

---

## 五、留给你决定 / 后续要做的事

### ⚠️ 需要你现在确认的

**包名 `cn.jingshi.ai`** —— 芒果联盟要求**包名与 `applicationId` 完全一致，且创建后无法修改**。申请前请确认这个包名不会再改。

### 第 5–6 步（等条件具备）

1. 装 Capacitor（`@capacitor/core` + `@capacitor/android` + `@capacitor/cli`）→ 生成原生工程
2. 注册芒果联盟 → 拿 `APPID` / `AppKey` / `PlacementId`
3. 写 `MangoAdapter`（按 `adAdapterFactory.ts` 头部 6 步）→ **业务层零改动**
4. ⚠️ **合规红线**：SDK 初始化必须在**用户同意隐私政策之后**。落点已预留（`AdNoticeDialog`），但需与 onboarding 隐私同意步骤对接

### 仍在待办清单的旧项

- `RemoveAdsPage.vue` 的 ¥68 支付接入 + 恢复购买 + 凭证本地校验（M9-F6 全套，当前是占位页）
- 广告偏好设置页（个性化广告开关默认关闭、类目偏好 —— PRD M10-F6）
- 开屏 SDK 冷启动性能验证（**唯一的技术前置项**，需真机 + SDK）
- 奖励中心的任务判定接入真实数据（当前 `done` 状态是本地占位，未接学习数据）

### 一个诚实的说明

奖励中心的任务墙目前是**可点的 UI 骨架**，`claim()` 直接发放天数。真实的任务判定（「今日全部任务包完成」需读计划引擎、「连续打卡 7 天」需读活跃记录）**尚未接线** —— 这部分等第 5 步做完、整体联调时一起处理更合适，现在接容易返工。
