# 上线审计 + 三项阻塞补齐 + GitHub Actions 云构建

> 日期：2026-09-21 ｜ 范围：上线就绪度评估 → 补阻塞 → 打通云构建链路
> 验证：**类型检查零错误 ｜ 单测 117/117 ｜ 构建成功 ｜ git 首次提交完成**

---

## 一、先回答你的问题

### 1. 是否达到上线标准？

**功能实现完整度很高，但有 5 项阻塞。其中 4 项已在本轮解决，剩 1 项需要你的数据。**

| # | 问题 | 状态 |
| --- | --- | --- |
| 1 | 无 Capacitor 依赖、无 android/ 工程 | ✅ 已解决 |
| 2 | **题库是程序化生成的占位数据** | ⚠️ **待你提供真题** |
| 3 | 无任何隐私政策同意流程 | ✅ 已解决 |
| 4 | 备份加密未实现（PRD Q11） | ✅ 已解决 |
| 5 | 无网络状态主动检测（M9-F11） | ✅ 已解决 |

**已完整实现的部分**（不是骨架）：FSRS-6 复习调度、四阶段计划引擎、全真模考（含两科连考）、
六维复盘（含过线概率计算）、过线预测、本地数据看板、AI 四种能力（A1出题/A2解析/A4私教）。

### 2. 广告在什么阶段接入？

**现在就可以接入，代码侧已经全部就绪。**

准确地说，广告的接入分三层，**前两层本轮已完成**：

| 层级 | 内容 | 状态 |
| --- | --- | --- |
| **第 1 层：业务逻辑** | 渐进释放 / 频次控制 / 三广告位 UI / 超时降级 / 崩溃隔离 / 失败即赠送 | ✅ 完成，117 项单测覆盖 |
| **第 2 层：合规前置** | 隐私政策同意闸门（SDK 初始化的法定前提） | ✅ 完成 |
| **第 3 层：SDK 对接** | 芒果联盟 `MangoAdapter` | ⬜ 等你拿到 `APPID`/`AppKey`/`PlacementId` |

第 3 层是**纯替换工作**：按 `adAdapterFactory.ts` 头部注释操作，**业务层零改动**。

⚠️ 但有个**顺序约束**：芒果联盟是 Native SDK，必须先在真机跑通，所以**必须先完成云构建 → 装机验证**，才能接 SDK。

### 3. GitHub Actions 云构建可行吗？

**完全可行**，这正是你当前情况下的最优解。

| 本机缺什么 | GitHub Actions 提供 |
| --- | --- |
| JDK | `actions/setup-java` → Temurin JDK 21 |
| Android SDK | `android-actions/setup-android` + runner 预装 |
| Gradle | 项目自带 wrapper（`gradlew` + jar 已在库） |

免费额度：公开仓库无限，私有仓库 2000 分钟/月（一次构建约 4-5 分钟）。

---

## 二、本轮补齐的三项阻塞

### 1. 隐私政策同意流程（合规红线）

广告 SDK 的硬要求：**用户同意隐私政策之前，不得初始化任何采集设备信息的第三方 SDK**。
无数 App 因"启动即初始化广告 SDK"被下架。

- `domain/privacy/policy.ts` — 结构化政策内容（7 章节，含单列的「广告与第三方 SDK」章节）
- `domain/privacy/consent.ts` — 同意状态管理
  - **版本不匹配 = 未同意**（政策变更必须重新确认 —— 不能因为"他一年前点过一次"就默认同意新条款）
- `pages/privacy/PrivacyConsentPage.vue` — 同意页，**「不同意」是真实可选**（不是只留一个按钮逼用户同意）
- `pages/privacy/PrivacyPolicyPage.vue` — 全文页，随时可从「我的」访问
- 路由守卫**最高优先级**接入，白名单仅同意页与政策页

### 2. 备份加密（PRD Q11）

- `domain/services/backupCrypto.ts` — **PBKDF2-SHA256（250,000 次迭代）+ AES-256-GCM**
- 导出**默认加密**；导入**自动识别**密文/明文（不让用户先回答"这是加密的吗"）
- GCM 认证标签天然防篡改 —— 这是「购买状态随备份迁移」的安全前提
- 算法参数写进文件（不是写死在代码），将来提升迭代次数时旧备份仍可解

### 3. 网络状态检测（PRD M9-F11）

- `domain/net/networkStatus.ts`
- 离线时**主动跳过**广告，而不是等 SDK 超时 —— 用户看到的是「App 正常」而非「加载失败」
- 无法判断网络时**保守返回在线**（宁可多试一次，也不要永久不展示广告）

---

## 三、顺带修掉的 3 个真 bug

| 问题 | 影响 | 修复 |
| --- | --- | --- |
| **自然日兜底「意外加速」** | 只用 1 天、放置 19 天后回归 → 被直接顶到阶段 3 完全体，违反 PRD「放置 30 天应从阶段 0 开始」 | 兜底加「距上次活跃 ≤ 3 天」前置条件 |
| 免广告天数差一 | `grantAdFreeDays(1)` 后显示 2 天 | `trialUntil` 语义改为「最后有效日」 |
| 连续兑换吞一天 | 1 天 + 7 天 = 实际只得 7 天 | 续期从「原到期日**次日**」起算 |

后两个在真机上极难发现（用户只会觉得"怎么少了一天"），是写单测时暴露的 ——
这也是单测从 26 项做到 117 项的价值。

---

## 四、云构建链路（已就绪，等你推送）

### 已产出

| 文件 | 作用 |
| --- | --- |
| `.github/workflows/android.yml` | 主流水线。debug（自动跑，零配置）+ release（手动勾选，需签名） |
| `.github/SIGNING.md` | 签名配置完整指南（含 keystore 生成与安全说明） |
| `app/android/app/build.gradle` | 接入 signingConfigs，**无签名文件时不报错**（本地开发无感） |
| `app/capacitor.config.ts` | 补充包名锁定警告与启动耗时预算说明 |

### 关键技术参数（改动前必读）

| 组件 | 版本 | 原因 |
| --- | --- | --- |
| JDK | **21** | Capacitor 8 / AGP 8.13 要求，用 17 会编译失败 |
| Android SDK Platform | **36** | 对应 compileSdk/targetSdk 36 |
| Node | 22 | Vite 8 要求 ≥ 20.19 |

### 🔴 过程中发现并修正的一个严重问题

`app/.gitignore` 里有一条 `android/`，**把整个安卓工程排除了**。

这是 Capacitor 之前留下的条目（当时把原生目录当构建产物）。但 **Capacitor 的 `android/` 是源码不是产物** ——
含 gradle wrapper、AndroidManifest、签名配置。排除它会导致 CI **必然失败**（找不到 `gradlew`）。

已修正为产物级排除，`android/` 现在 53 个文件全部入库，CI 必需文件逐一核验通过。

---

## 五、你需要做的（3 步）

### 第 1 步：创建 GitHub 仓库

打开 https://github.com/new ，**不要**勾选 "Add a README"（保持空仓库）。

### 第 2 步：推送代码

在项目根目录执行（把 `<你的用户名>` 换成实际值）：

```bash
git remote add origin https://github.com/<你的用户名>/jingshi-ai.git
git push -u origin main
```

> 首次推送会要求登录。GitHub 已不支持密码推送，需要 Personal Access Token：
> Settings → Developer settings → Personal access tokens → 生成（勾选 `repo` 权限），
> 推送时用 Token 当密码。

### 第 3 步：触发构建

推送后会自动触发一次构建（因为 `app/src/**` 有变更）。
也可随时手动触发：**Actions** 标签 → 左侧选 **Build Android APK** → 右侧 **Run workflow**。

构建完成后，在运行页面底部 **Artifacts** 区下载 `jingshi-ai-debug-<commit>`。

---

## 六、还需要你决定 / 提供的事

### ⚠️ 需要你确认（阻塞 SDK 对接）

**包名 `cn.jingshi.ai`** —— 芒果联盟后台创建应用后**永久不可修改**。
一旦注册就锁死，注册前请确认这个名字不再改。

### ⚠️ 需要你提供（上线最大软阻塞）

**真实题库数据**。目前题目由 `buildQuestions()` 用模板 + 随机选项生成，
功能验证够用，但**绝不能上线**——用户拿到手会发现题目不像真题。

这是本轮唯一无法自行推进的事项：真题内容在物理上不存在于开发环境中。
一旦数据到位，导入脚本与题库适配是立刻可以完成的工程工作。

### 真机验证项（PRD 标注的唯一技术前置）

启动耗时预算：原生启动屏 1200ms + 广告就绪等待 1500ms = **最坏 2.7s**，
超出 PRD §12 的 2s 目标。**装机后请实测**，若超标优先下调 `launchShowDuration`。

---

## 七、验证结果

| 项目 | 结果 |
| --- | --- |
| `npm run typecheck` | ✅ 零错误 |
| `npm test` | ✅ **117/117**（原 26 项） |
| `npm run build` | ✅ 成功 |
| git 首次提交 | ✅ 181 文件 / 41,551 行 / 零产物混入 |
| 仓库体积 | 7.0 MB（不含 node_modules/dist） |
| CI 必需文件核验 | ✅ gradlew / wrapper.jar / package-lock.json 全部在库 |
