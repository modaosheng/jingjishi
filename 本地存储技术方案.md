# 《经师 AI》本地存储技术方案

> 配套文档：`中级经济师App_PRD_v1.0.md`（v1.1 本地化双科版）
> 适用范围：纯本地存储架构下的 iOS / Android 客户端
> 版本：v1.0 · 2026-09-03

---

## 0. 一句话架构

**两个库 + 一个文件目录 + 一个钥匙串。**

```
bundled.db（只读）  ──►  题目与精讲内容（随包分发，可整包替换）
user.db（读写）     ──►  用户的一切：答题记录、FSRS、错题、笔记、自建题、计划
files/（读写）      ──►  附件：上传的 PDF/图片、快照、缓存
Keychain/Keystore  ──►  LLM API Key
```

核心原则：**内置内容可随意替换，用户数据永不覆盖**。这两类数据物理分离，是本地架构下数据安全的基石。

---

## 1. 技术选型

| 层 | iOS | Android | 说明 |
|---|---|---|---|
| **数据库** | SQLite（系统内置） | SQLite（系统内置） | 不引入 Realm/ObjectBox，SQLite 生态最成熟、备份导出最直接（就是一个文件） |
| **ORM / 访问层** | **GRDB.swift** 或 SQLite.swift | **Room** | GRDB 对 SQLite 控制力强，适合复杂查询与 ATTACH；Room 是安卓官方方案 |
| **全文检索** | SQLite **FTS5** | SQLite **FTS5** | 需开启 FTS5 编译选项（系统 SQLite 通常已支持，Room 需确认） |
| **中文分词** | jieba（入库时预分词） | jieba / 或 bigram | ⚠️ FTS5 默认 unicode61 分词器对中文无效。**推荐 bigram（二元分词）**：零依赖、体积小、对短查询召回好，考试场景够用 |
| **向量检索（可选增强）** | `sqlite-vec` + ONNX Runtime（multilingual-MiniLM） | 同左 | 作为**可选下载包**，不进主包。v1.1 可先只上 FTS5 |
| **加密** | 见 §7（默认不加密 DB） | 同左 | 备份文件导出时可选 AES-256 |
| **密钥存储** | Keychain | Android Keystore + EncryptedSharedPreferences | 仅存 LLM API Key |
| **压缩/打包** | ZIPFoundation | java.util.zip | 备份文件打包 |

**不推荐**：Core Data（备份导出麻烦）、Realm（文件结构复杂、跨平台导出不直观）、UserDefaults/SharedPreferences 存业务数据（性能与容量都不合适）。

---

## 2. 存储目录布局

### iOS

```
~/Library/Application Support/          ← 被 iCloud 备份
├── bundled.db                          内置题库（只读，随包）
├── user.db                             用户主库 ✅备份
├── user.db-wal / user.db-shm           WAL 文件（随主库）
├── rag_fts.db                          全文检索索引（可重建，✅备份但可排除）
├── attachments/                        上传的原件 ⚠️ 体积大，建议标记不备份
│   └── {set_id}/{file_id}.pdf
├── snapshots/                          自动快照
│   ├── daily_20260903.db
│   └── weekly_2026w36.db
└── exports/                            导出的备份文件暂存

~/Library/Caches/                       ← 系统可清理，不备份
├── ai_response/                        AI 响应缓存
└── images/                             图片缓存

Keychain                                ← 随系统加密备份
└── llm_api_key
```

**iCloud 备份标记**（重要，避免撑爆用户空间）：
```swift
//  attachments/ 与 Caches/ 排除出 iCloud 备份
var url = attachmentsURL
var resourceValues = URLResourceValues()
resourceValues.isExcludedFromBackup = true
try url.setResourceValues(resourceValues)
```
> ⚠️ `Caches/` 目录系统会自动清理，不能放任何需要持久化的数据。`Application Support/` 才是正确位置。

### Android

```
/data/data/{pkg}/
├── databases/
│   ├── user.db                         用户主库 ✅备份
│   └── rag_fts.db
├── files/
│   ├── bundled.db                      内置题库（首次启动从 assets 复制）
│   ├── attachments/                    上传的原件
│   └── snapshots/                      自动快照
├── cache/                              缓存（系统可清理）
└── shared_prefs/                       轻量配置（非敏感）

Android Keystore                        LLM API Key
```

**Android 备份配置**（`android:allowBackup="true"` + 自定义规则）：
```xml
<!-- res/xml/backup_rules.xml -->
<full-backup-content>
    <include domain="database" path="user.db"/>
    <include domain="file" path="snapshots"/>
    <exclude domain="file" path="attachments"/>   <!-- 大文件排除 -->
    <exclude domain="file" path="bundled.db"/>    <!-- 可重新解压 -->
</full-backup-content>
```

---

## 3. 双库设计：内置内容 vs 用户数据

### 3.1 为什么分两个库

| 方案 | 优点 | 缺点 |
|---|---|---|
| ❌ 单库（内置题导入主库） | 查询简单 | App 更新内置题库时极易误伤用户数据；无法整包替换内容 |
| ✅ **双库（推荐）** | 内置内容整包替换，用户数据物理隔离，永不覆盖 | 需跨库查询 |

**跨库查询用 SQLite 的 ATTACH 解决**，成本很低：

```sql
ATTACH DATABASE '/path/to/bundled.db' AS bundled;

-- 查询某知识点的题目（内置 + 自建）
SELECT q.*, s.difficulty AS user_difficulty, s.due_date
FROM bundled.question q
LEFT JOIN main.user_question_state s ON s.question_id = q.id
WHERE q.knowledge_node_id = 'econ_03_02_01'
UNION ALL
SELECT q.*, s.difficulty, s.due_date
FROM main.question q          -- 用户自建题也在 user.db
LEFT JOIN main.user_question_state s ON s.question_id = q.id
WHERE q.knowledge_node_id = 'econ_03_02_01';
```

**实践建议**：在 App 启动时执行一次 `ATTACH`，整个生命周期保持连接，避免反复 attach/detach。

### 3.2 built-in 库更新机制

```
App 更新（新版本含 bundled_v2.db）
  → 启动时检查 bundled 内容版本号（存在 bundled.meta 表）
  → 若版本更高：
      1. 校验新库完整性（checksum）
      2. 原子替换 bundled.db（先写 bundled.db.new，再 rename）
      3. 打开主库，执行 ATTACH
      4. ✅ 用户数据（user.db）完全不受影响
  → 若题目 ID 有变更，通过 bundled.meta 中的 id_mapping 表做历史记录迁移
```

**关键点**：题目 ID 必须**稳定且全局唯一**（建议 UUID 或 `科目_模块_章_序号` 的确定性 ID），否则内置库更新后用户的答题记录会全部失联。

### 3.3 用户数据永远不被覆盖

| 数据 | 位置 | App 更新时 |
|---|---|---|
| 答题记录、FSRS 状态、错题、笔记 | user.db | 仅做 schema 迁移，**绝不删除** |
| 自建题 | user.db 的 `question` 表（`owner_type='user'`） | 绝不删除 |
| 上传的原件 | files/attachments/ | 绝不删除（除非用户主动清理） |
| 内置题 | bundled.db | 整体替换 |
| AI 响应缓存 | cache/ | 可清空 |

---

## 4. 核心表结构（DDL 摘要）

> 完整数据模型见 PRD §11。此处给出对本地存储有性能/结构影响的关键表。

```sql
PRAGMA journal_mode = WAL;          -- 写并发性能关键
PRAGMA synchronous = NORMAL;        -- 平衡性能与安全
PRAGMA foreign_keys = ON;

-- ============ 用户主库 user.db ============

-- 元信息
CREATE TABLE app_meta (
    key   TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
-- schema_version, device_id, created_at, plan_id, last_backup_at, ai_quota_used ...

-- 用户自建题（含 AI 生成题）；内置题在 bundled.db
CREATE TABLE question (
    id                  TEXT PRIMARY KEY,          -- 稳定唯一 ID
    subject_id          TEXT NOT NULL,             -- 'econ_base' | 'hr'
    type                TEXT NOT NULL,             -- 'single'|'multi'|'case'
    stem                TEXT NOT NULL,
    options             TEXT NOT NULL,             -- JSON 数组
    answer              TEXT NOT NULL,             -- JSON 数组（多选）
    difficulty          INTEGER NOT NULL DEFAULT 3, -- 1-5
    bloom_level         TEXT,                      -- remember/understand/apply/analyze
    explanation         TEXT,                      -- JSON：逐项解析
    source_level        TEXT NOT NULL,             -- 'S'|'A'|'B'|'C'  ← 可信度是一等公民
    owner_type          TEXT NOT NULL,             -- 'official'|'user'|'ai'
    ai_metadata         TEXT,                      -- JSON：模型/置信度/校验状态
    knowledge_node_ids  TEXT NOT NULL,             -- JSON 数组，支持多绑
    content_version     TEXT,                      -- '2026'
    status              TEXT NOT NULL DEFAULT 'active', -- active/pending_review/removed
    created_at          INTEGER NOT NULL,
    updated_at          INTEGER NOT NULL
);
CREATE INDEX idx_q_subject_node ON question(subject_id, knowledge_node_ids);
CREATE INDEX idx_q_owner ON question(owner_type, source_level);

-- 用户-题目记忆状态（FSRS）
CREATE TABLE user_question_state (
    question_id    TEXT PRIMARY KEY,
    fsrs_difficulty REAL NOT NULL DEFAULT 5.0,     -- D
    fsrs_stability  REAL NOT NULL DEFAULT 0.0,     -- S
    fsrs_retrievability REAL NOT NULL DEFAULT 0.0, -- R
    due_date       INTEGER NOT NULL,               -- Unix ms，复习到期日
    last_review_at INTEGER,
    review_count   INTEGER NOT NULL DEFAULT 0,
    lapse_count    INTEGER NOT NULL DEFAULT 0,
    status         TEXT NOT NULL DEFAULT 'new',    -- new/learning/review/relearning
    is_wrong       INTEGER NOT NULL DEFAULT 0,     -- 是否在错题本
   攻克_count       INTEGER NOT NULL DEFAULT 0     -- 连续答对次数，用于移出错题本
);
CREATE INDEX idx_uqs_due ON user_question_state(due_date);   -- 今日复习队列的核心索引
CREATE INDEX idx_uqs_wrong ON user_question_state(is_wrong);

-- 答题流水（高频写入，数据量最大）
CREATE TABLE answer_log (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    question_id   TEXT NOT NULL,
    answered_at   INTEGER NOT NULL,
    user_answer   TEXT NOT NULL,        -- JSON
    is_correct    INTEGER NOT NULL,
    confidence    TEXT,                 -- 'sure'|'unsure'|'noidea'  确信度自评
    duration_ms   INTEGER NOT NULL,
    mode          TEXT NOT NULL,        -- review/chapter/special/exam/...
    session_id    TEXT,                 -- 模考场次，用于模考复盘
    error_reason  TEXT                  -- 错误归因
);
CREATE INDEX idx_log_q ON answer_log(question_id, answered_at);
CREATE INDEX idx_log_time ON answer_log(answered_at);
CREATE INDEX idx_log_session ON answer_log(session_id);

-- 用户-知识点掌握度（冗余表，避免实时聚合）
CREATE TABLE user_knowledge_state (
    node_id         TEXT PRIMARY KEY,
    mastery_score   REAL NOT NULL DEFAULT 0,      -- 0-100
    level           TEXT NOT NULL,                -- 未学/薄弱/一般/良好/掌握
    question_count  INTEGER NOT NULL DEFAULT 0,
    correct_count   INTEGER NOT NULL DEFAULT 0,
    last_practice_at INTEGER,
    predict_forget_at INTEGER                     -- 预测遗忘临界日
);

-- 备份快照
CREATE TABLE backup_snapshot (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    type          TEXT NOT NULL,        -- 'auto_daily'|'auto_weekly'|'manual'|'pre_migration'
    created_at    INTEGER NOT NULL,
    file_path     TEXT NOT NULL,
    size_bytes    INTEGER NOT NULL,
    stats         TEXT NOT NULL         -- JSON：题量/错题数/计划进度，用于时间机器展示
);
CREATE INDEX idx_snap_time ON backup_snapshot(created_at);

-- 附件
CREATE TABLE attachment (
    id           TEXT PRIMARY KEY,
    set_id       TEXT,
    file_name    TEXT NOT NULL,
    rel_path     TEXT NOT NULL,
    size_bytes   INTEGER NOT NULL,
    mime_type    TEXT NOT NULL,
    created_at   INTEGER NOT NULL
);

-- AI 响应缓存（省 token，也支持离线查看）
CREATE TABLE ai_response_cache (
    cache_key    TEXT PRIMARY KEY,      -- hash(question_id + ability + prompt_version)
    ability      TEXT NOT NULL,         -- 'explain'|'generate'|'mnemonic'
    content      TEXT NOT NULL,
    model        TEXT NOT NULL,
    created_at   INTEGER NOT NULL
);
```

**设计要点说明**：

1. `answer_log` 是数据量最大的表（一天 100 题 × 180 天 = 1.8 万行，两年也不到 10 万行，SQLite 毫无压力）。**不要过早分表**。
2. `user_question_state.due_date` 上的索引是**今日复习队列**的核心，每次打开首页都要查，必须有。
3. FSRS 的三个参数（D/S/R）直接建列，避免 JSON 解析开销。
4. `ai_response_cache` 用 `prompt_version` 参与 cache key，prompt 迭代后自动失效旧缓存，避免出现"改了 prompt 但还读旧缓存"的诡异 bug。

---

## 5. 备份文件格式

### 5.1 文件结构

备份文件本质是一个 ZIP，扩展名 `.jjbak`：

```
经师AI备份_20260903_1243题.jjbak
├── manifest.json          元数据（明文，用于快速预览）
├── user.db                用户主库（已 VACUUM + 一致性检查）
├── attachments/           可选
│   └── ...
└── checksum.sha256        完整性校验（user.db + attachments 分别记录）
```

### 5.2 manifest.json

```json
{
  "format_version": "1.0",
  "app_version": "1.0.0",
  "schema_version": 3,
  "created_at": "2026-09-03T11:20:00+08:00",
  "device": {
    "platform": "iOS",
    "model": "iPhone14,3",
    "os_version": "18.2",
    "device_id": "ANON-UUID-xxxx"
  },
  "subjects": ["econ_base", "hr"],
  "stats": {
    "questions_answered": 1243,
    "wrong_count": 87,
    "notes_count": 32,
    "self_built_questions": 156,
    "exam_records": 3,
    "plan_progress": 0.34,
    "study_days": 41
  },
  "includes_attachments": false,
  "attachment_count": 0,
  "encrypted": false,
  "size_bytes": 8388608
}
```

**manifest 明文很重要**：用户在恢复前能看到"这个备份有多少题、什么时候的、来自哪台设备"，避免恢复错文件。

### 5.3 导出流程

```swift
func exportBackup(includeAttachments: Bool, password: String?) async throws -> URL {
    // 1. 创建临时工作目录
    let workDir = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString)

    // 2. 一致性快照：用 SQLite 的 backup API（不是直接 copy 文件！）
    //    ⚠️ 关键：App 正在运行，直接 copy .db 文件可能拿到撕裂状态
    //    必须用 sqlite3_backup_init/step/finish 做在线备份
    let db = try DatabasePool(path: userDBPath)
    try db.backup(to: workDir.appendingPathComponent("user.db"))

    // 3. 生成 manifest
    let manifest = buildManifest(includingAttachments: includeAttachments)
    try writeJSON(manifest, to: workDir/"manifest.json")

    // 4. 可选：复制附件
    if includeAttachments { try copyAttachments(to: workDir/"attachments") }

    // 5. 计算 checksum
    try writeChecksums(workDir)

    // 6. 打包 ZIP
    var zipURL = try zip(directory: workDir, named: suggestedFileName(manifest))

    // 7. 可选：AES-256 加密整个 zip
    if let pwd = password { zipURL = try encrypt(zipURL, password: pwd) }

    // 8. 记录到 backup_snapshot 表，更新 last_backup_at
    return zipURL   // 交给系统分享面板
}
```

> ⚠️ **最容易踩的坑**：直接 `FileManager.copyItem` 复制正在被 WAL 模式使用的 `.db` 文件，可能备份出一个损坏或数据不全的库。**必须用 SQLite Online Backup API**（iOS 的 GRDB 有 `db.backup(to:)`，Android 可用 `sqlite3_backup_*` 或先 `VACUUM INTO`）。
>
> 简单替代方案：`VACUUM INTO '/path/backup.db'` —— 一条 SQL 搞定，会生成一个压缩且一致性的数据库副本。推荐这个，最简单可靠。

### 5.4 导入恢复流程

```
用户选择 .jjbak 文件
  → 读取 manifest（若为加密文件，先解密）
  → 展示确认页：
       「该备份创建于 2026-09-03，来自 iPhone 14
         包含：1243 题答题记录 · 87 道错题 · 156 道自建题 · 3 次模考记录
         ⚠️ 恢复将覆盖当前设备上的全部学习数据，此操作不可撤销」
       [取消] [确认恢复]
  → 校验 checksum
  → 检查 schema_version 兼容性
       ├─ 兼容 → 直接恢复
       └─ 备份来自更高版本 → 提示"请先更新 App"
  → 关闭数据库连接，替换 user.db
  → 重新打开，执行必要的 schema 迁移
  → 重启 App 至首页，Toast：「已恢复 1243 题学习记录」
```

**恢复前强制为当前数据创建一次 `pre_migration` 快照**——万一用户恢复错了，还能回滚。这是低成本高价值的保险。

---

## 6. 自动快照（时间机器）

```
触发时机：
  ├─ 每日首次启动（type=auto_daily）
  ├─ 单次会话答题 ≥ 50 题（type=auto_daily，覆盖当日）
  ├─ 每周日（type=auto_weekly，长期保留）
  ├─ 数据库迁移前（type=pre_migration，强制）
  └─ 恢复/回滚前（type=pre_migration，强制）

保留策略：
  ├─ auto_daily   保留最近 7 份
  ├─ auto_weekly  保留最近 4 份
  ├─ pre_migration 保留最近 3 份
  └─ manual       不自动清理（用户主动导出的，不占用设备空间）

清理：
  每次创建新快照后执行，按类型保留数量删除最旧的
  同时检查总占用，超过 500MB 时优先清理 auto_daily
```

**"时间机器" UI**（PRD M10-F3 ①）：
按时间轴列出快照，每个展示创建时间与统计摘要。用户选择后展示"该时点 vs 当前"的差异（答题数、错题数、计划进度），确认后回滚。

> 实现提醒：快照用 `VACUUM INTO`，生成的库是压缩后的，通常比原库小 30-50%，7 份快照的空间成本可接受。

---

## 7. 加密决策

| 对象 | 是否加密 | 理由 |
|---|---|---|
| **user.db（本地）** | ❌ **默认不加密** | ① 系统沙箱已提供进程级隔离；② 加密会损失 5-15% 读写性能（答题是高频写入场景）；③ 一旦密钥丢失数据全丢，风险反大于收益 |
| **备份文件（导出）** | ⚠️ **可选 AES-256** | 备份文件会离开设备（发微信、传网盘），**这才是真正的风险点**。提供加密选项，但必须强提示"密码遗忘无法恢复" |
| **LLM API Key** | ✅ **必须加密** | Keychain / Keystore，禁止写入数据库、SharedPreferences、日志 |
| **崩溃/日志** | ✅ 脱敏 | 日志中禁止出现 API Key、题目正文、用户笔记 |

**为什么本地库默认不加密**——本地架构下，数据的主要威胁不是"设备被物理攻破"，而是"备份文件在传输中泄露"。把加密资源花在备份文件上，性价比更高。

若后续确实需要 DB 加密（如面向企业用户），再引入 SQLCipher，但需要设计密钥管理方案（不能硬编码）。

---

## 8. 性能要点

| 场景 | 优化 |
|---|---|
| **首页渲染**（今日复习队列） | `user_question_state.due_date` 索引 + 限制返回量（首页只取前 50 条，进入答题页再分页加载） |
| **答题页切换 ≤200ms** | 预加载下一题：完成当前题提交时，后台线程提前查出下一题数据 |
| **答题写入** | 单次提交只写 2 行（answer_log + user_question_state 更新），包在同一事务。**不要每次答题都重算掌握度**——掌握度用批量任务（每 20 题或会话结束时）异步重算 |
| **FSRS 计算** | 纯 CPU 计算，单次 < 1ms，无需优化。但**批量重算**（如首次启动导入 1 万题记录）要放后台线程并分批 |
| **全文检索** | FTS5 + bigram 分词；索引表与内容表分离，避免主表膨胀 |
| **数据库体积** | 定期 `PRAGMA incremental_vacuum`；导出备份前 `VACUUM INTO`（顺带压缩） |
| **启动速度** | ATTACH bundled.db 放在后台线程（首屏不强依赖）；WAL 模式下不要在主线程做 checkpoint |

**必须避免**：
- ❌ 在主线程做任何数据库查询
- ❌ 答题时同步重算全量掌握度（会卡顿）
- ❌ 每次答题都写一条 `user_knowledge_state` 更新（改为批量）

---

## 9. Schema 迁移

```sql
-- 迁移登记表（user.db 内）
CREATE TABLE schema_migration (
    version    INTEGER PRIMARY KEY,
    applied_at INTEGER NOT NULL,
    name       TEXT NOT NULL
);
```

**迁移流程**：
```
启动 → 读取 user_meta.schema_version
     → 若 < 目标版本：
        1. 【强制】创建 pre_migration 快照
        2. 按序执行迁移脚本（每个脚本在一个事务内）
        3. 更新 schema_version
        4. 失败 → 自动回滚到 pre_migration 快照，提示用户"数据已保护，请更新 App"
```

**迁移铁律**：
1. **只增不改**：优先加表、加列（带默认值）、加索引，避免改列类型与删列。
2. **破坏性变更延后**：真要删表删列，保留旧表两个版本后再清理。
3. **迁移前必快照**：这是最后一道防线。
4. **迁移脚本必须可重入**：用 `IF NOT EXISTS`、`INSERT OR IGNORE`。

---

## 10. 内置题库打包策略（Q12 相关）

| 方案 | 包体积 | 首启动 | 更新 | 建议 |
|---|---|---|---|---|
| 全部打包 | 大（可能 100MB+） | 立即可用 | 需发版 | 小题库可行 |
| 全部首次下载 | 小 | 需等待下载 | 可热更 | 依赖网络，弱网体验差 |
| ✅ **核心打包 + 扩展下载** | 适中 | 立即可用 | 核心随版，扩展可热更 | **推荐** |

**具体建议**：
- **打包进安装包**：《经济基础》六大模块 + 《人力》三大部分的**章节练习题（每章约 40 题）** + 全部精讲内容 + RAG 语料。预估 60-80MB。
- **首次启动下载**（WiFi 提示，可选跳过）：真题卷、模考卷、扩展题目（每章补至 80+ 题）。
- **分包下载**：《人力》内容可独立下载——只考经济基础的用户不必下载人力包。

**控制包体积的技巧**：
- 题目正文与解析用压缩存储（gzip 后存 BLOB），读取时解压。文本压缩率通常 3-5 倍。
- 图片（思维导图等）用 WebP，且按需下载。
- 精讲内容中的示例图片用 SVG 而非 PNG。

---

## 11. 关键风险清单（研发 checklist）

| # | 风险 | 检查项 |
|---|---|---|
| 1 | ⚠️ **直接 copy 运行中的 SQLite 文件导致备份损坏** | 必须用 `VACUUM INTO` 或 Online Backup API。**这是最可能踩的坑** |
| 2 | ⚠️ **内置题 ID 变更导致用户答题记录失联** | 题 ID 必须稳定唯一；内置库更新时提供 id_mapping |
| 3 | ⚠️ **App 更新覆盖用户数据** | 双库隔离；更新时只替换 bundled.db；发布前必测"覆盖安装"场景 |
| 4 | ⚠️ **数据放在 Caches/ 目录被系统清理** | 持久化数据一律放 Application Support/ 或 files/ |
| 5 | ⚠️ **API Key 写进日志或 SharedPreferences** | Code Review 专项检查；日志统一脱敏 |
| 6 | ⚠️ **FTS5 中文分词失效** | FTS5 默认分词器不支持中文，必须用 bigram 或预分词。**务必实测中文检索能搜到** |
| 7 | ⚠️ **数据库迁移失败导致 App 崩溃白屏** | 迁移前快照 + 失败自动回滚 + 兜底提示 |
| 8 | ⚠️ **备份恢复后 schema 不兼容** | manifest 记录 schema_version，恢复前校验 |
| 9 | ⚠️ **iCloud/系统备份撑爆用户空间** | attachments/ 与 Caches/ 正确标记 `isExcludedFromBackup` |
| 10 | ⚠️ **主线程数据库操作导致卡顿** | 所有 DB 操作强制走后台队列；调试期开启主线程 DB 访问告警 |

---

## 12. 与 PRD 的对应关系

| 本文档章节 | PRD 章节 |
|---|---|
| §3 双库设计 | §11.1 本地持久化映射 |
| §5-6 备份与快照 | §M10-F3 三重备份机制、§M10-F4 换机迁移 |
| §7 加密决策 | §M10-F6 设置与隐私 |
| §2 目录布局 | §M10-F5 存储管理 |
| §10 内置题库打包 | §12 非功能需求·包体积 |
| §11 风险清单 | §14 风险 R-10 |

---

**最后一句提醒**：本地架构下，**备份功能的优先级等同于核心学习功能**。建议在 Phase 0 就把导出/导入跑通并实测——包括"导出 1 万题记录 → 卸载 App → 重装 → 恢复"的完整链路。这个链路没跑通前，不要开始堆功能。
